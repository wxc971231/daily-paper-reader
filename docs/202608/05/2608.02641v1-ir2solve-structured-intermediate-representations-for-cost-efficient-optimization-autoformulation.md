---
title: "IR2Solve: Structured Intermediate Representations for Cost-Efficient Optimization Autoformulation"
title_zh: IR2Solve：用于成本高效优化自动公式化的结构化中间表示
authors: "Penglin Zhu, Linhai Zhang, Jungang Xu, Xinchi Wei, Xiuqi Wu"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2608.02641v1"
tags: ["query:llm-opt-code"]
score: 10.0
evidence: 利用LLM将自然语言优化问题转化为带模式约束的中间表示并编译成求解代码
tldr: LLM直接生成优化问题求解代码易出现编译、索引和语义错误，且迭代修复成本高。IR2Solve提出中间表示优先的自动建模流程，先用一次语义调用生成受模式约束的ModelIR，再经确定性验证和编译。在多个基准上目标正确性达到先进水平，且单实例仅需一次语义调用，相比对比系统节省3.3至22.9倍token。表明结构化中间表示结合后处理能实现精度与成本的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02641v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1796, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02641v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 816, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02641v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 821, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02641v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1297, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 816, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1586, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1765, \"height\": 461, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1568, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 858, \"height\": 411, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 754, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 473, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 565, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 440, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 541, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02641v1/table-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 783, \"height\": 268, \"label\": \"Table\"}]"
motivation: 直接生成求解器代码易错且迭代修复成本高，需更稳健的低成本自动建模途径。
method: IR2Solve单次语义调用生成结构化的ModelIR，经确定性验证与编译，用标量约束表示有限索引族。
result: 在六个基准上目标正确性优秀，单实例一次语义调用，token消耗比对比系统低3.3至22.9倍。
conclusion: 结构化中间表示加确定性后处理，提供了可靠且低成本的优化自动建模新方案。
---

## 摘要
大型语言模型（LLM）可以将自然语言优化问题翻译为可直接求解的公式，但直接代码生成较为脆弱：模式、索引和语义错误可能导致编译失败、模型不可行或目标不正确，而迭代修复、搜索和多智能体工作流则会增加推理成本。我们提出IR2Solve，一种以中间表示为先的自动公式化流水线，它使用一次语义LLM调用来生成受模式约束的ModelIR，随后经过两个确定性阶段：验证和IR到求解器的编译。ModelIR使用受限的类Python表达式字符串显式表示集合、参数、变量、目标和约束。一种具体的标量约束约定将有限的按索引约束族表示为单个条目，减少了自由索引和隐式量化错误，同时简化了下游验证和编译。在六个清理后的优化基准上，IR2Solve实现了较强的目标正确性，并与最近的优化建模系统保持竞争力。对153个IndustryOR和ComplexLP实例的受控消融实验显示，结构化IR接口、标量约束指令和确定性验证带来了顺序性增益。在匹配的十实例成本面板上，IR2Solve每个实例仅使用一次语义调用，而Chain-of-Experts和SAC-Opt每个实例分别使用8次和39次调用，分别消耗IR2Solve的3.3倍和22.9倍token量。这些结果表明，结构化中间表示结合确定性后生成处理，为基于LLM的优化自动公式化提供了实用的准确性-成本权衡。

## Abstract
Large language models (LLMs) can translate natural-language optimization problems into solver-ready formulations, but direct code generation is brittle: schema, indexing, and semantic errors can cause compilation failures, infeasible models, or incorrect objectives, while iterative repair, search, and multi-agent workflows increase inference cost. We present IR2Solve, an intermediate-representation-first autoformulation pipeline that uses a single semantic LLM call to produce a schema-constrained ModelIR, followed by two deterministic stages: verification and IR-to-solver compilation. ModelIR explicitly represents sets, parameters, variables, objectives, and constraints using restricted Python-like expression strings. A concrete scalar-constraint convention represents finite per-index constraint families as individual entries, reducing free-index and implicit-quantification errors while simplifying downstream verification and compilation. Across six cleaned optimization benchmarks, IR2Solve achieves strong objective correctness and remains competitive with recent optimization-modeling systems. A controlled ablation on 153 IndustryOR and ComplexLP instances shows sequential gains from the structured IR interface, the scalar-constraint instruction, and deterministic verification. On a matched ten-instance cost panel, IR2Solve uses one semantic call per instance, whereas Chain-of-Experts and SAC-Opt use 8 and 39 calls per instance and consume 3.3 and 22.9 times the token volume of IR2Solve, respectively. These results show that structured intermediate representations, combined with deterministic post-generation processing, provide a practical accuracy-cost trade-off for LLM-based optimization autoformulation.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **背景**：在运筹学中，将现实世界的自然语言问题描述转化为正式的优化模型（变量、目标、约束）是标准工作流的第一阶段，也是关键瓶颈。第二阶段（求解）已高度自动化，但第一阶段（自动公式化，autoformulation）仍严重依赖人工建模专家。
- **痛点**：LLM 具有语义与推理能力，可尝试将自然语言直接映射为求解器代码，但**直接代码生成是脆弱的**——常出现模式（schema）错误、索引错误、形状错误、语义错误，导致编译失败、模型不可行或目标不正确。
- **成本困境**：现有系统（多智能体协作、反思、搜索、迭代修复）虽能提升准确率，但显著增加了 API 调用次数、token 消耗与工作流复杂度，形成**准确率-成本之间的权衡**，制约大规模部署。
- **核心信息**：IR2Solve 主张通过**显式的中间表示层**（ModelIR）+ **确定性后处理**（验证器与编译器），在保持较低推理成本的同时获得具有竞争力的建模准确率，为 LLM 自动建模提供一种实用、可审计的中间路线。

## 2. 提出的方法论

### 2.1 总体思想
- 将自动公式化分解为两步映射：  
  **d → m → c**（自然语言描述 → 数学公式化 → 可执行求解器实例）
- 传统代码优先系统将两步合并在一次 LLM 输出中；IR2Solve 让 **m 成为显式产物**，且 m → c 由**确定性程序**完成，随机性只保留在 d → m 阶段。

### 2.2 ModelIR（结构化中间表示）
- 一种受模式约束的 JSON 表示，包含六个顶层字段：`meta`、`sets`、`params`、`vars`、`objective`、`constraints`。
- 目标与约束表达式使用**受限的类 Python 字符串**：仅允许算术、括号、已声明的参数/变量引用、显式索引，以及绑定在已声明有限集合上的 `sum`/`quicksum` 生成器；禁止 import、控制流、求解器调用等程序结构。
- **标量约束约定**：每条约束条目表示**一个具体的标量关系**，禁止自由索引与隐式"对 all i"语义；有限按索引约束族显式展开为多条条目。这消除了编译期的索引作用域推断，便于逐条检查，但会随索引积增大输出长度（论文明确承认这是有代价的设计选择）。
- 论文以引理形式论证：当正确表述集合被表示语言覆盖时，条件限制可提升正确率；以命题形式证明有限集上的全称约束族与显式合取等价。

### 2.3 确定性验证器（Verifier）
- 固定顺序的 7 组守卫式重写规则，**不调用 LLM**：
  1. 集合/键名规范化（str 化）
  2. 二维参数归一化（支持 "a,b" 等键形式）
  3. 方阵缺失对角补零（启发式）
  4. 自由索引展开（按集合元素逐条生成约束）
  5. sum 调用语法规范化
  6. 变量类型完整性校正（连续→二进制/整数）
  7. 等式方向校正（"at most"→≤、"at least"→≥）
- 验证器是确定性组合：**bm = V(m) = TK ∘ ⋯ ∘ T1(m)**，不引入概率分布或候选重采样。

### 2.4 确定性编译与求解
- 编译器在仅含已声明对象与白名单聚合函数的环境中解析表达式，实例化集合/参数/变量，设置目标，逐条生成 Gurobi 约束。
- 相同 IR + 相同编译器版本 → 相同的 Gurobi 模型，保证可精确重放；每条约束可回溯到 IR 条目。

### 2.5 算法流程（单次语义调用）
```
ir ← GenerateIR(d)          // 一次语义 LLM 调用
ir ← Verify(ir)             // 确定性，无模型调用
c ← CompileToGurobi(ir)     // 确定性
return Solve(c)
```

## 3. 实验设计

### 3.1 数据集与基准
- 采用 survey-cleaned 发布的六个基准：
  | 数据集 | 规模 |
  |---|---|
  | NL4Opt | 214 |
  | IndustryOR | 42 |
  | EasyLP (MAMO) | 545 |
  | ComplexLP (MAMO) | 111 |
  | NLP4LP (OptiMUS 谱系) | 178 |
  | ReSocratic | 403 |
- 评估协议：GPT-4o-2024-08-06、temperature 0、单次生成、Gurobi 60 秒/实例限制；报告 Build / Solved / **目标正确率**（|z−z⋆| ≤ 1e−4 + 1e−6|z⋆|）。

### 3.2 对比方法
- **代码优先**：CAFA（外部基线）
- **多智能体/反思**：Chain-of-Experts、OptiMUS-0.3
- **迭代修正**：SAC-Opt
- **微调**：ORLM-LLaMA-3 8B
- **基础提示**：Standard、CoT
- MCTS 搜索系统与 NEMO 因协议不对齐未列入主表，仅作背景讨论。

### 3.3 消融实验（153 实例 = 42 IndustryOR + 111 ComplexLP）
- **R0**：直接生成 Gurobi 代码，一次调用
- **R1**：结构化表达式字符串 IR，无标量约束指令，无验证器
- **R2**：+ 标量约束指令
- **R3**：+ 确定性验证器（最终配置）

### 3.4 成本实验（COST-10 面板）
- 从 214 个 NL4Opt 实例中按源文本长度分 10 等频箱、每箱抽 1 例；仅用于测量推理成本，不用于排名准确率。

## 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量、训练时长或具体硬件配置（作者在附录中明确指出归档元数据未包含 CPU、内存、操作系统版本、Python 版本及精确 Gurobi 构建，故不报告未经核实的值）。
- 可确认的推理侧配置：**GPT-4o-2024-08-06** 官方 API、temperature 0、单样本、无 provider fallback、无语义重试；求解端为 **Gurobi**，每实例 60 秒时间限制。
- 由于论文没有训练微调模型（区别于 ORLM 等微调工作），其算力需求主要体现在 API 推理调用上。

## 5. 实验数量与充分性

- **主对比**：6 个清理基准 × 9~10 种方法（部分来自外部报告），覆盖了当前主流方法族（代码优先、多智能体、迭代修正、微调）。
- **消融**：153 实例上的 4 级嵌套（R0→R1→R2→R3），给出顺序性增益、配对 McNemar 精确检验（p = 1.1e−6、1.5e−5、<1e−19）与 bootstrap 95% 置信区间，统计严谨。
- **成本**：COST-10 面板对 4 种方法（CAFA、IR2Solve、CoE、SAC-Opt）的调用数与 token 数做了匹配测量。
- **评估充分性评价**：
  - **优点**：消融设计区分了接口级、指令级、组件级三层贡献；配对了 R2→R3 以保证验证器对比不增加模型调用；执行结果（Build/Solved/目标正确）分层清晰。
  - **局限**：消融只覆盖 IndustryOR 与 ComplexLP（论文自我承认）；外部基线行（survey 值与 SAC-Opt 值）的工作流细节与本地评估不完全一致，主表比较是"清理分割下的scoped比较"而非完全受控的 head-to-head；R0→R1 是捆绑式对比（schema+表达式语言+提示词+编译器同时变化），无法归因到单一因素。

## 6. 主要结论与发现

- **主表结果**：IR2Solve 在六个基准上均高于 survey 报告的基线行；与 SAC-Opt 报告值相比，IR2Solve 在 IndustryOR（64.3% vs 63.8%）和 EasyLP（97.4% vs 96.5%）更高，SAC-Opt 在 NL4Opt、ComplexLP、NLP4LP、ReSocratic 更高——总体处于同一竞争力区间。
- **消融结果**（目标正确率）：
  | 变体 | IndustryOR | ComplexLP | Pooled |
  |---|---|---|---|
  | R0 | 7.1% | 7.2% | 7.2% |
  | R1 | 50.0% | 30.6% | 35.9% |
  | R2 | 64.3% | 55.0% | 57.5% |
  | R3 | 64.3% | 70.3% | 68.6% |
  - 结构化 IR 接口提供最大顺序增益（+28.8 pp pooled）；标量约束指令 +21.6 pp；验证器 +11.1 pp（数据集依赖：IndustryOR 无增益，ComplexLP +15.3 pp）。
  - 结构违规从 522 降至 39 再降至 12；Build 率从 R1 44.4% 升至 R3 79.1%。
- **成本结论**：IR2Solve 每实例 1 次语义调用、16,031 总 token；CoE 8 次调用、53,459 token（3.3×）；SAC-Opt 39 次调用、367,028 token（22.9×）。
- **总体判断**：结构化中间表示 + 确定性后处理，能在可接受的准确率水平上显著降低推理成本，并带来可检查性（inspectability）与可重放性（replayability）优势。

## 7. 优点

- **架构清晰**：d→m→c 的分解将"建模不确定性"与"实现不确定性"分离，随机性只保留在语义解释阶段，机器相关阶段完全确定。
- **一次调用、成本可控**：无迭代、无搜索、无多智能体协调，推理成本可预测；是论文最突出的工程价值。
- **可审计性**：ModelIR 是天然的人可读中间产物，约束可逐条追溯，编译结果可精确重放；确定性验证器由 7 条带守卫的规则构成，行为可解释、可单元测试。
- **消融设计严谨**：嵌套变体 + 配对检验 + bootstrap 置信区间；R3 复用 R2 的精确输出，保证验证器对比无额外模型调用。
- **客观性**：多处主动划清边界——不声称完全受控的 superiority，不把 COST-10 准确率用于排名，不把目标值一致等同于语义等价，不推断未实测系统的成本。
- **表达约束的设计自觉**：论文明确说明有限集标量展开的等价性及其扩展成本，而非盲目宣称该表示具有普遍优势。

## 8. 不足与局限

- **表示覆盖有限**：ModelIR 仅面向有限 LP/MILP（参数至多二维、对象至多二维索引），不原生支持非线性、随机、鲁棒、动态、锥/半定、无限维或连续索引模型。
- **标量展开成本**：大索引笛卡尔积下显式展开会急剧增加输出长度，引入截断风险；论文承认这是针对基准规模的实用选择而非通用最优。
- **消融覆盖不足**：机制消融仅覆盖 2/6 个数据集；R0→R1 是捆绑对比，未隔离 schema、表达式语言、提示词、确定性编译各自的贡献；未对比 LLM 编译器与确定性编译器的差异。
- **主表可比性弱**：survey 基线、SAC-Opt/OptiMUS 报告值与本地评估在解码参数、求解协议、运行聚合方式上不一致；表 2 是 scoped 比较而非严格 head-to-head。
- **成本面板与准确率面板分离**：COST-10 只用于成本测量，六基准准确率与 COST-10 成本绘制在同一图中属"部署代理"而非同实例 Pareto 证明；CoE 的 token 数来自 evaluator 提前终止的观测值，是其完整工作流成本的下界。
- **验证器启发性**：部分规则（方阵补零、完整性与方向性校正）依赖生成文本中的线索词而非独立的事实来源，个别重写可能不保证语义正确；论文也承认验证器无法重建 IR 中缺失的约束或误读的目标。
- **评估标准粗糙**：目标正确率是粗粒度端点——可行域不同但最优值相同的模型会被判为正确；未做人工语义标签裁决。
- **可复现性信息缺失**：未报告硬件、GPU、CPU、内存、精确求解器构建版本等工程细节，影响完全复现。

（完）
