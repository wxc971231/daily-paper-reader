---
title: LLM-Guided Graph Generation for Structure-Based Local Improvement Methods
title_zh: 基于LLM引导的图生成用于基于结构的局部改进方法
authors: "Hai Xia, Vaidyanathan Peruvemba Ramaswamy, Stefan Szeider"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13333v3"
tags: ["query:llm-opt-code"]
score: 8.0
evidence: 基于LLM为MiniZinc优化问题生成引导图以改进搜索
tldr: "大规模邻域搜索中变量选择通常随机，缺乏跨领域结构指导。本文提出自动化流水线，用LLM依据语义指南生成图生成器，将MiniZinc问题实例映射为统一加权图，节点为决策变量、边为约束关系，从而指导SLIM框架进行变量选择。在20个MiniZinc竞赛问题上，算法选择对Gurobi基准达到39.6%平均问题加权胜率，比最佳单配置19.3%高出一倍多，且有44.0%提升空间。该工作证明了LLM语义生成能自动实现有效的结构特征提取，为约束优化提供通用范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13333v3/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1700, \"height\": 1315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13333v3/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1711, \"height\": 677, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13333v3/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 836, \"height\": 383, \"label\": \"Table\"}]"
motivation: 现有大规模邻域搜索的变量选择多随机化，忽视问题结构特征，难以跨领域高效优化。
method: 用语义指南提示LLM生成图生成器，将MiniZinc实例转为统一加权图，提取图特征指导SLIM变量选择。
result: "在20个MiniZinc问题上，算法选择对Gurobi基准达39.6%平均问题加权胜率，较最佳单配置19.3%翻倍，事后消融显示44.0%提升空间。"
conclusion: 表明LLM语义生成可自动提取问题结构特征，驱动高效的算法选择与配置，为约束优化提供通用自动化方案。
---

## 摘要
大规模邻域搜索通常随机选择决策变量的子集进行迭代优化。为了高效解决各类问题，研究者倾向于设计考虑不同领域结构特征的变量选择策略。本文构建了一个自动化流水线，该流水线对MiniZinc格式的所有问题均不依赖具体问题。通过使用我们的语义指南提示LLM，我们引导LLM生成一个图生成器，该生成器将任何问题类型的实例映射为均匀加权图，其中节点表示决策变量，边表示约束关系。这些与问题无关的图指导我们基于结构的局部改进（SLIM）框架进行变量选择。同时，加权图使得所有问题实例能够共享相同的通用图表示，从中可以提取相同的图特征并用于配置选择。我们在20个MiniZinc竞赛问题的实例上评估了我们的流水线，发现算法选择相对于一次性Gurobi基线实现了39.6%的平均问题加权胜率，是最佳单一配置（19.3%）的两倍多。事后配置和特征消融表明有高达44.0%的提升空间，证明了基于LLM的语义生成能够为约束优化实现有效的自动化结构和特征提取。

## Abstract
Large neighborhood search normally selects a random subset of decision variables for iterative optimization. To efficiently solve various problems, researchers tend to design variable selection strategies that take into account structural features across different domains. In this paper, we build an automatic pipeline that is problem-agnostic to all problems in the MiniZinc format. By prompting an LLM with our semantic guidelines, we guide the LLM to produce a graph generator that maps any instance of a problem type to a uniform weighted graph, where nodes represent decision variables and edges represent constraint relationships. These problem-agnostic graphs guide our structure-based local improvement (SLIM) framework for variable selection. Meanwhile, the weighted graph enables all problem instances to share the same generic graph representation, from which the same graph features can be extracted and used for configuration selection. We evaluated our pipeline on instances across 20 MiniZinc competition problems, finding that algorithm selection achieves a 39.6% average problem-weighted win rate against a one-shot Gurobi baseline, more than doubling the best single configuration (19.3%). A post-hoc configuration and a feature ablation indicate a headroom of up to 44.0%, demonstrating that LLM-based semantic generation enables effective automated structure and feature extraction for constraint optimization.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：大规模邻域搜索（LNS）是求解组合优化难题的重要元启发式方法，其效率高度依赖于每轮迭代中变量的选择（邻域选择）。传统做法多采用随机变量选择，忽略了实例特征与问题结构，浪费求解时间；而基于结构的局部改进方法（SLIM）依赖领域专家手工设计不同问题的变量选择策略和邻域构造方式，工程量巨大且难以跨领域复用。
- **拟解决的关键问题**：如何摆脱专家手工设计的痛点，构建一个**问题无关（problem-agnostic）** 的自动化流水线，让不同领域的组合优化问题能共享统一的图结构表示，从而指导 SLIM 进行有效的结构感知变量选择，并实现跨问题算法配置选择。
- **整体含义**：本文提出以 LLM 作为“语义编译器”，从 MiniZinc 约束模型自动生成图生成器，将任意实例映射为统一加权图。该工作证明了 LLM 语义生成能够自动完成结构特征提取与配置选择的结合，为约束优化提供了一种通用、可扩展的自动化新范式。

## 2. 论文提出的方法论

- **核心思想**：用 LLM 阅读 MiniZinc 模型，结合人工设定的语义指南生成 Python 程序（图生成器），程序将问题实例转化为**统一加权图**——节点代表决策变量（带重要性权重与域大小），边代表变量间的约束关系（带耦合强度）。该图与具体问题域无关，使后续 SLIM 与算法选择均可跨问题通用。
- **两阶段流水线**：
  - **问题特定阶段**：LLM 根据 MiniZinc 模型和语义指南离线合成图生成器，一次性完成，无需领域专家参与。
  - **问题无关阶段**：变量提取器解析实例中的决策变量、域与数组索引，图生成器据此构造加权图；从图中提取 54 个拓扑与统计特征，用于算法选择器挑选合适的 SLIM 配置。
- **语义图生成指南**（四条关键准则）：
  1. 与目标函数直接相关的组件权重 ≥ 0.6，确保优先被优化；
  2. 大型全局约束（如 n > 10 的 alldifferent）使用轻权重 max(0.1, 1/n) 防止团主导，小型约束（n ≤ 5）保持强耦合（≥ 0.8）；
  3. 不允许孤立节点，确保所有变量都有被选入邻域的可能；
  4. 权重有界，多边聚合采用 W = 1 − ∏ᵢ(1 − wᵢ)，保证不超过 1。
- **通用 SLIM 框架**：在统一加权图上运行两种提取策略：
  - **BFS 提取**：按节点权重随机选起点，沿边按权重偏置随机扩展，兼顾变量重要性与局部约束结构；
  - **LNS 提取（随机提取）**：仅按节点权重随机采样变量，不利用边连接的局部性。
  - 提取过程受预算参数 b 约束（域大小感知的阈值），另有“冻结模式”决定收集的变量是参与优化还是被固定。每次迭代在子问题上添加目标值不劣于当前最优解的边界约束，局部求解器（采用 Gurobi）优化后更新全局解，直至总时间预算或最大迭代次数耗尽。
- **算法配置选择**：
  - SLIM 共配置 30 种组合（局部预算 b ∈ {10, 20, 50, 70, 100, 200}，超时 t ∈ {20, 30, 45, 60} 秒，不同提取策略）；
  - 利用统一图提取的特征，训练**多输出随机森林回归**等 5 个选择器，按问题加权样本权重以平衡类别不平衡；
  - 选择器预测每配置相对于一次性 Gurobi 基线的改进幅度，选中最佳配置运行。

## 3. 实验设计

- **基准数据集**：来自 MiniZinc 竞赛（2008-2025），涵盖 **20 个问题类型**（如 RCPSP、VRP、job-shop、tdtsp、spot5 等），共 700+ 实例（RCPSP 占 38%）。
- **实例筛选标准**：
  1. Gurobi 在 10 分钟内找到可行解但 60 分钟内未能证明最优性；
  2. 每个问题类型至少有 5 个合格实例。
- **对比方法**：
  - **一次性 Gurobi 基线**：以相同总时间预算（60 分钟）直接运行 Gurobi 求解原实例；
  - **SLIM + 30 个单一配置**：各配置独立运行后取每问题最优者；
  - **虚拟最优（virtual best）**：理论上限参考；
  - **5 种算法选择器**：回归、集成、二元选择、分类、两阶段，对比最佳单配置基线。
- **数据划分**：按 70:30 比例分层抽样（按问题类型分层）为训练/测试集。
- **评估指标**：问题加权胜率（problem-weighted win rate），使每个问题类型在评估中贡献相等。

## 4. 资源与算力

- **实验环境**：Sun Grid Engine（SGE）集群，20 个节点，Ubuntu 18.04 LTS，每节点双路 Intel Xeon E5-2640 v4 2.40 GHz CPU，160 GB RAM；SLIM 及相关程序使用 Python 3.6.9。
- **文中未明确说明 GPU 型号与数量**——该工作基于纯 CPU 集群运行，未涉及 GPU 加速或深度模型训练（图生成器由 LLM 离线合成，SLIM 本身是元启发式+精确求解）。
- **LLM 合成开销**：每个问题的图生成器合成通常只需几分钟，但文中未给出集群总机时、具体能耗或完整的训练时长统计。

## 5. 实验数量与充分性

- **主要实验**：
  - 20 个问题类型的 SLIM（30 配置）vs. 一次性 Gurobi 对比（3 个随机种子平均）；
  - 5 种算法选择器 × 3 个随机种子（51/52/53）的对比实验；
  - 事后配置消融（贪心后向消除）与特征消融（54 特征）分析。
- **实验覆盖面**：问题域多样、实例量大（20 个类型 700+ 实例）、多种选择器互相验证，并给出了详细的 win/tie/loss 分解，整体较为充分。
- **客观与公平性评估**：
  - 有明确的筛选标准与分层划分，按问题加权防止大类主导，3 个种子提供稳定性证据；
  - **不足**：仅对比单一基线（Gurobi），缺少与其他 LNS 变体/神经 LNS 的横向对比；单一 LLM（Claude Opus 4.5）生成图，未做跨模型鲁棒性验证；消融属于事后（post-hoc）分析而非端到端训练中的验证，提升空间（44.0%）为理论上限而非实际可达的自动化性能。

## 6. 论文的主要结论与发现

- **问题无关 SLIM 有效**：以每问题最优配置运行，SLIM 在多数问题上胜过一次性 Gurobi，其中 tdtsp、spot5、community-detection、triangular、opd 胜率超过 75%；仅 rectangle-packing 和 VRP 上有超过一半实例输于基线。
- **算法选择显著优于单一配置**：最佳单配置的问题加权胜率仅 17.8-22.3%，而所有 5 种选择器均超过 31%；最优方法每种子胜率集中在 37.9-40.6%（净得分 +14.8-24.5%），不同种子的最佳选择器平均达 **39.6%**。
- **有较大提升空间**：事后配置+特征消融显示理论上限可达 **44.0%**，说明当前 LLM 图生成、特征设计与配置集合仍有可优化的余地。
- **配置对 SLIM 至关重要**：不同搜索策略对最终性能影响巨大，这正支撑了自动配置选择的价值。

## 7. 优点

- **自动化程度高**：一次 LLM 合成后即可全自动处理某一问题类型的任意实例，无需专家介入或逐问题训练，显著降低了 SLIM 应用门槛。
- **问题无关的统一表示**：所有问题共享同一加权图格式，使 SLIM 提取算法和算法选择特征具备极强的跨问题可迁移性，无需按问题类型重新设计。
- **语义信息编码合理**：通过人工设计语义指南（目标相关权重、全局约束降权、边权重聚合公式），将变量重要性与耦合强度有效编码进图结构，避免了盲目随机选择。
- **实验设计严谨**：问题加权评估、分层抽样、3 个种子、多选择器对比、事后消融分析，论证较为充分；数据集已开源在 Zenodo 便于复现。
- **清晰的工程链路**：从 .mzn 模型到图生成、特征提取、配置选择、最终 SLIM 求解，全流程模块化清晰，便于扩展与后续改进。

## 8. 不足与局限

- **图生成质量受 LLM 限制**：生成器是 LLM 对真实约束语义的“近似”，即使经过验证也可能存在偏差导致错误结构编码，直接影响 SLIM 的性能上限。
- **覆盖范围有限**：仅涵盖 MiniZinc 竞赛基准中的 20 个问题，未覆盖更大规模或更深领域差异的问题；且筛选条件排除了一些 Gurobi 无法求解/已证明最优的实例，存在一定的选择性偏差。
- **评估基线单一**：仅与一次性 Gurobi 对比，未与随机 LNS、神经 LNS（Hottung & Tierney 2022）、其他算法选择器（如 AutoFolio、SATzilla）进行实证比较，优势的普适性有待进一步确认。
- **数据不平衡**：RCPSP 占 38%，虽采用问题加权缓解，但跨问题泛化的统计功效仍然受限。
- **选择器性能不稳健**：没有单一选择器在三粒种子上均最优，胜率 31%-41% 波动，说明训练数据规模有限时算法选择的稳定性仍有提升空间。
- **事后消融的局限性**：44.0% 的“headroom”是后验最优推断，实际自动端到端流程难以稳定达到，论文对此表述略显乐观。
- **算力与代价报告不完整**：未给出完整的 LLM API 调用成本、总 CPU 机时和每个实例的平均求解时间，不利于可重现的效能对比。
- **未说明 LLM 生成的图生成器是否需要人为修复/验证**——LLM 输出可能包含逻辑错误，文中仅以“validated”一笔带过，缺少生成器正确性的系统验证方案。

（完）
