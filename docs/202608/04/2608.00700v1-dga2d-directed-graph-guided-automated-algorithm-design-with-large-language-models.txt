Title: DGA_2D: Directed Graph-Guided Automated Algorithm Design with Large Language Models

URL Source: https://arxiv.org/pdf/2608.00700v1

Published Time: Tue, 04 Aug 2026 00:53:17 GMT

Number of Pages: 46

Markdown Content:
# DGA 2D: Directed Graph-Guided Automated Algorithm Design with Large Language Models 

## Jiale Zhao ∗1, Zimu Chen ∗1, Sirui Mao 2, Wentao Yang 2, Yuxiang Bai 1. Liyuanjun Lai 1†

1School of Automation Science and Electrical Engineering, Beihang University, Beijing, China 

2School of Computer Science and Engineering, Beihang University, Beijing, China {Zhaojl27, 24373298, 24371337, 24371256, lailiyuanjun}@buaa.edu.cn, 

Abstract 

The rapid development of Large Language Models (LLMs) has opened new avenues for Automated Heuristic Design (AHD) for solving NP-hard combinatorial optimization prob-lems (COPs). However, existing LLM-driven AHD methods are largely confined to rigid solver templates, relegating the search process to isolated module tuning. Transitioning to fully autonomous, system-level algorithm design is essen-tial but fraught with low reliability of generated operators, extremely large search spaces, and ineffective credit assign-ment. To overcome these drawbacks, this paper proposes a Directed Graph-Guided Automated Algorithm Design frame-work, termed DGA 2D. It structures the open-ended program space as a directed graph, where each node represents a func-tional operator that can be instantiated using one of mul-tiple candidate code implementations, while directed walks constitute complete algorithmic pipelines. A first-order path-dependent credit assignment mechanism is introduced to eval-uate code variations strictly based on their topological con-text. Extensive experiments across 12 distinct COPs—ranging from complex scheduling to routing—demonstrate the consis-tent empirical advantages of DGA 2D. It reduces the average normalized gap by up to 10.96 percentage points compared to state-of-the-art LLM baselines. 

## 1 Introduction 

Numerous practical combinatorial optimization problems (COPs), such as flexible job-shop scheduling and open ve-hicle routing, are NP-hard. Unless P = NP , no polynomial-time exact algorithms are known for these problems. As these problems are key determinants of the efficiency of many com-plex systems, different kinds of algorithms, such as mathe-matical programming algorithms, evolutionary algorithms, and reinforcement learning algorithms are designed to find high-quality feasible solutions in a limited time. The no-free-lunch theorems suggest that no optimization algorithm is universally superior across all possible prob-lems, motivating the development of methods that exploit problem-specific structure (Wolpert and Macready 1997). Despite the development of numerous problem-specific op-timization algorithms, designing effective and extensible al-gorithms remains difficult and labor-intensive. Moreover, the 

> ∗

These authors contributed equally. 

> †

Corresponding author. High H uman Effort      

> High Performance
> Human-designed
> Framework
> Traditional Heuristic D esign
> Init
> Process Terminate
> Evaluate
> Human-designed
> Operators
> High Scalability
> Low H uman Effort
> High Performance
> LLM-evolved
> Framework
> Autonomous Generation (Ours)
> LLM-evolved
> Operators
> Limited Scalability
> Moderate Human Effort
> Moderate Performance
> Human-designed
> Framework
> Semi-Autonomous Generation (FunSearch, EoH)
> Init
> Process Terminate
> Evaluate
> LLM-evo lved
> Operators
> Combinatorial Optimization Problems
> Diverse Problem Domains
> Job-Shop Scheduling Vehicle Routing
> Graph Optimization Spatial Allocation
> NP-hard
> Solution Space Tree Exponential Growth
> Combinatorial
> Explosion
> Low Scalability

Figure 1: Evolution of Automated Heuristic Design (AHD). 

Left: NP-hard problem domains and exponentially growing solution spaces. Right: Comparison of AHD paradigms. Un-like Traditional Heuristic Design and Semi-Autonomous Generation approaches, our Autonomous Generation 

evolves both the complete algorithmic structure and oper-ators. optima of many classical benchmark problems remain un-known. To achieve a more efficient and convenient heuristic al-gorithm design process, researchers proposed the concept of AHD, which soon became an active research area (Burke et al. 2013). The efforts devoted to AHD can mainly be classi-fied into three routes: heuristic selection, heuristic configura-tion, and heuristic composition. Despite substantial progress, traditional heuristic-design approaches still rely heavily on code modules crafted by human experts (Drake et al. 2020). As summarized in Figure 1, they typically require high hu-man effort and exhibit low scalability , although they can achieve high performance on specific problems. The rapid development of Large Language Models (LLMs) presents a new opportunity for AHD (Liu et al. 2026). LLMs have enabled fully automated generation and refinement of heuristics, along with their associated code im-plementations. Representative methods include FunSearch (Romera-Paredes et al. 2024) and EoH (Liu et al. 2024). Attempts have also been made to couple LLMs with Evo-lutionary Computation (EC) to produce heuristics in a more automated manner (Liu et al. 2024; Dat, Doan, and Binh arXiv:2608.00700v1 [cs.AI] 1 Aug 2026 2025; Chen et al. 2026). However, existing LLM-driven AHD methods operate primarily within rigid solver tem-plates. As illustrated in Figure 1, such semi-autonomous ap-proaches generally require moderate human effort , offer 

limited scalability , and achieve moderate performance .To overcome the above structural bottlenecks, a natural progression is to use LLMs as both the framework architect and the operator designer. As illustrated in Figure 1, allow-ing the LLM to redesign both the algorithmic framework and its operators opens a richer design space, with the goal of achieving low human effort , high scalability , and high performance .However, expanding the evolution target from isolated heuristics to complex algorithmic architectures composed of multiple operators introduces three fundamental challenges. 1. Low reliability of generated algorithms. Complete al-gorithms consist of long, interdependent operator se-quences and are therefore prone to compilation errors, interface mismatches, and logical failures. 2. Extremely large search spaces. Valid and high-performing algorithms are sparse in open-ended program spaces, making efficient exploration difficult. 3. Ineffective credit assignment. Because performance is observed only after evaluating complete algorithms, de-termining which local modifications are responsible for an improvement is difficult. To overcome the above challenges, this paper proposes a directed graph-guided automated algorithm design frame-work, termed DGA 2D, for open-ended algorithm design that optimizes complete algorithms with multiple interconnected operators rather than isolated heuristics. The main contribu-tions of this paper are as follows. 1) It structures the algorithm as a Directed Graph (DG), where individual nodes represent operators, each associ-ated with multiple candidate implementations, while directed walks constitute complete algorithmic pipelines. The LLM drives this evolution through three straightforward actions. First, it edits the actual code inside each step. Second, it rear-ranges how different steps connect to form new topological paths. Third, it picks the most suitable code variation for the specific path being taken. 2) A first-order path-dependent credit assignment mechanism is integrated into the algorithm generation process. Credit is assigned to each code implementation according to its con-text within the evolutionary process, enabling a quantitative assessment of each code implementation’s contribution to the final algorithm. 3) Comprehensive experiments are conducted across 12 rep-resentative COPs, with comparisons against classical solvers, problem-specific learning-based methods, and state-of-the-art LLM-driven AHD frameworks. The results show that DGA 2D consistently outperforms the LLM-driven baselines across four representative benchmarks, reducing the average normalized gap by 9.67 and 10.96 percentage points under DeepSeek-V4-Flash and GPT-5.6 sol, respectively. 

## 2 Related Work 

2.1 Heuristics for Combinatorial Optimization 

Existing heuristic approaches for COPs can be broadly sum-marized into three research lines. Evolutionary algorithms use population-based random search to explore large solution spaces (Huang et al. 2024; Wang et al. 2024). Reinforcement-learning-based methods learn adaptive policies through in-teraction with optimization environments (Ye et al. 2023; Zhang et al. 2024). Machine-learning and deep-learning-based methods use learned models, such as Transformers and diffusion models, to construct or refine solutions (Sun and Yang 2023; Li et al. 2024). These approaches depend heavily on manually designed structures, operators, and learning ob-jectives, which limits their applicability to specific problem classes (Bengio, Lodi, and Prouvost 2021). 

2.2 Automated Heuristic Design 

Driven by the demand for more efficient algorithm de-sign, AHD has emerged as an active research area (Burke et al. 2013). Classical AHD is largely studied through hyper-heuristics, which generally follow three paradigms: heuris-tic configuration, heuristic selection, and heuristic compo-sition. Heuristic configuration tunes parameters or control policies within a predefined algorithmic framework (Brandt et al. 2023; Guo et al. 2025). Heuristic selection adaptively chooses low-level heuristics from a fixed portfolio, increas-ingly using reinforcement learning and combined offline– online experience (Zhao et al. 2023; Pei, Liu, and Mei 2024). Heuristic composition constructs new procedures with data-driven component selection and modular metaheuristic gen-eration (Dokeroglu, Kucukyilmaz, and Talbi 2024; Xue, Shu, and Xia 2025; Contreras Estrada, Stützle, and Pérez Cáceres 2025). Despite reducing manual design effort, these AHD approaches remain constrained by human-defined operators and search spaces (Drake et al. 2020). 

2.3 Automated Heuristic Design Using LLMs 

Recently, LLMs have been applied to AHD, leveraging their zero-shot capabilities in code generation to synthesize algo-rithmic heuristics from natural language (Liu et al. 2026). Early studies explored LLMs for automated heuristic and algorithm generation (Liu et al. 2023). Related work also demonstrated LLM-driven optimization in reward design and black-box optimization (Ma et al. 2023; Yang et al. 2024). Standalone prompt-driven approaches often suffer from lim-ited long-context reasoning and lack systematic exploration mechanisms to escape local optima. To overcome these limitations, recent work has cou-pled LLM-based generation with iterative search, including population-based evolution (Romera-Paredes et al. 2024; Liu et al. 2024; van Stein and Bäck 2025), reflection-guided re-finement (Ye et al. 2024), multi-objective population man-agement (Dat, Doan, and Binh 2025; Yao et al. 2025), and tree-structured exploration (Zheng et al. 2025). In summary, existing methods optimize heuristics within predefined frameworks, which improves reliability but limits their performance. To move beyond these rigid frameworks, recent work explores LLMs as system-level algorithm archi-tects (Chen et al. 2026); however, reliably generating com-plete and consistently competitive algorithms remains chal-lenging. 

## 3 Methodology 

3.1 Overview of the DGA 2D Framework 

The overall framework of DGA 2D is illustrated in Figure 2. It represents the algorithm search space as a DG in which both the topology and the operator implementations are iteratively refined through a closed-loop LLM-driven process. The algorithm generation process consists of four parts: LLM-based problem initialization, graph-guided candidate algorithm construction, evaluation and credit assignment, and credit-guided dual-level evolution, as detailed in the fol-lowing subsections. 

3.2 LLM-based Initialization 

As many algorithms are highly sensitive to specific problem domains, this paper introduces an LLM-driven initialization phase to take the natural language description of target prob-lem as input, setting up the initial hyperparameter space and the foundational operator pool for algorithm evolution, as demonstrated in Figure 2a. The initial hyperparameter space is defined as H =

{K, τ, . . . }, where K ∈ N+ specifies the maximum num-ber of functional operators maintained in the operator pool, and τ ∈ (0 , 1] is the sampling temperature used to balance exploration and exploitation. The foundational operator pool O = {O1, . . . , O m} con-sists of reusable operators selected by the LLM from a fixed repository constructed from existing algorithms and domain-specific literature. Each operator Oi ∈ O is asso-ciated with multiple code implementations that realize fine-grained heuristic actions, such as local-search moves and greedy assignment rules. Together, H and O initialize the evolutionary search in a problem-relevant region of the algo-rithm space. 

3.3 Graph-Guided Algorithm Construction 

As shown in Figure 2b, the proposed DGA 2D evolves both operator implementations and operator connectivity. The re-sulting DG defines a shared search space in which a fixed number of candidate algorithms are generated and subse-quently evaluated according to the procedure defined in Sec-tion 3.4. 

Directed-Graph Search-Space Representation. 

Suppose the maximum generation of the DGA 2D process be tmax . At each generation t ∈ [1 , t max ], the search space is modeled as a directed graph Dt = ( Ot, Et). Each node 

Oi ∈ O t represents a functional operator. A directed edge 

(Oi, O j ) ∈ E t indicates that Oj is executed after Oi.An operator Oi is associated with a pool of alternative code implementations, denoted by 

C(t) 

> i

= {ci, 1, c i, 2, . . . , c i, |C(t) 

> i|

}.

Each operator can be formulated by one implementation in 

C(t) 

> i

. The number of candidate implementations maintained by each operator is bounded by M , i.e., 

|C(t) 

> i

| ≤ M. 

Each code implementation belongs to one and only one operator, so the implementation pools are disjoint: 

C(t) 

> i

∩ C (t) 

> i′

= ∅, i̸ = i′.

Candidate Algorithm Construction. 

For each generation t ∈ [1 , t max ], a candidate algorithm is obtained in two stages: pipeline sampling and algorithm instantiation. First, the algorithmic structure is sampled by a bounded directed walk, which is defined as a pipeline Pt,j .

Pt,j =  Oin , O t,j, 1, . . . , O t,j,L t,j , O out 

 , (1) where Oin and Oout are sentinel nodes representing the input and output of the pipeline, respectively. They do not corre-spond to functional operators and therefore require no code implementations. A valid pipeline satisfies 

(Oin , O t,j, 1) ∈ E t,

(Ot,j,i , O t,j,i +1 ) ∈ E t, i = 1 , . . . , L t,j − 1,

 Ot,j,L t,j , O out 

 ∈ E t,

1 ≤ Lt,j ≤ Lmax .

(2) It is noted that repeated operators are permitted in a pipeline to enable iterative algorithmic structure. At each generation, in total of N pipelines Pt = {Pt, 1, . . . , P t,N }

are sampled according to a credit-guided policy introduced in Section 3.4. Each pipeline is instantiated as a candidate algorithm by selecting a code implementation for each operator involved in the pipeline. Specifically, for each operator Ot,j,i , one imple-mentation ˆct,j,i is sampled according to an implementation-selection probability pt, which is defined in Section 3.4, too. Then, a candidate algorithm is represented as a sequence of code implementations At,j =  ˆct,j, 1, . . . , ˆct,j,L t,j 

, where 

Lt,j denotes the length of the sampled pipeline. Repeating this procedure for j = 1 , . . . , N yields a population of algo-rithms At = {At, 1, . . . , A t,N }.

3.4 Problem Evaluation and Credit Assignment 

To support the pipeline sampling and algorithm instantiation, a credit assignment mechanism is defined for algorithm-level, pipeline-level, and code-implementation-level evaluation. 

Algorithm-level evaluation. An algorithm At,j is evalu-ated on k benchmark instances. For an instance b ∈ [1 , k ],let yt,j,b denote the objective value of the solution ob-tained by At,j , and yref  

> b

denote the reference value, which can be either the optimum or the best known objective value of the instance. A normalization scale is defined as 

sb = max  yref  

> b

, 1 to prevent numerical instability when the reference value is zero or close to zero. Figure 2: Overview of the DGA 2D framework. (a) LLM-driven initialization of problem configuration and operator pool. (b) Candidate algorithm construction by sampling operator structures and implementations. (c) Hierarchical credit assignment based on candidate algorithm performance. (d) Dual-level evolution of operator implementations and algorithmic pipelines guided by aggregated credits. Then, the direction-aware normalized gap is defined as 

gt,j,b =



yt,j,b − yref 

> b

sb

, for minimization ,yref  

> b

− yt,j,b 

sb

, for maximization .

(3) where gt,j,b > 0 indicates a worse performance than the reference, whereas gt,j,b < 0 indicates an improvement upon the reference. Suppose that all benchmark instances are equally impor-tant, the overall fitness of an algorithm is defined as 

St,j = − 1

k

> k

X

> b=1

gt,j,b . (4) A larger St,j indicates better overall performance. 

Terminal reward. As St,j is a normalized value and fol-lows a larger-is-better convention, it is applied as the terminal reward of an algorithm: 

eRt,j = St,j − μt

σt + ε , (5) where μt and σt are the mean and standard deviation of the rewards obtained in generation t, and ε > 0 is a small constant introduced for numerical stability. Thus, eRt,j >

0 indicates the algorithm performs above-average, whereas 

eRt,j < 0 indicates it performs below-average. 

Pipeline-level credit assignment. The standardized termi-nal reward eRt,j is then assigned to the sampled pipeline as its credit and used for updating the pipeline sampling policy (defined as πpipe  

> θ

): 

Lpipe (θ) = − 1

|P t|

X 

> j∈[1 ,N ]

eRt,j log πpipe  

> θ

(Pt,j | D t) . (6) Minimizing this loss encourages higher sampling proba-bilities for pipelines associated with positive pipeline-credit values. The factorization and optimization details of πpipe 

> θ

are provided in Appendix D.1. 

Code-implementation-level credit assignment. The util-ity of an algorithm At,j = ( ct,j, 1, . . . , c t,j,L t,j ) largely de-pends on its code implementation. Suppose ct,j, 0 = cstart 

and the i-th activated transition is et,j,i = ( ct,j,i −1, c t,j,i ).We define the credit of a first-order implementation tran-sition e as Qt(e), which estimates the expected standard-ized terminal reward associated with that transition. Initially, 

Q0(e) = 0 for unseen transitions. For every transition activated in the sampled code im-plementations, its credit is updated using the standardized terminal reward eRt,j :

Qt+1 (et,j,i ) = (1 −αt)Qt(et,j,i )+ αt eRt,j , i = 1 , . . . , L t,j ,

(7) where αt ∈ (0 , 1) is the learning rate. Therefore, we introduce a first-order Markov approxima-tion in defining the code implementation sampling policy as 

πcode  

> t

(ct,j,i | ct,j,i −1) = Softmax 

 Qt(et,j,i )

τ



.

The sampling of ct,j,i is only determined according to its immediate predecessor ct,j,i −1, τ > 0 is the tempera-ture parameter which controls the sharpness of the code-implementation sampling distribution. 3.5 Credit-Guided Dual-Level Evolution 

Suppose all of the activated transitions that involve a code implementation c selected in generation t formulates a tran-sition set E, the empirical credit of c is defined as 

¯Qt(c) = 

P 

> e∈E

nt(e)Qt(e)

P 

> e∈E

nt(e) + ϵ , (8) where e is a transition in E and nt(e) is the activation count of this transition and ϵ > 0 is a small constant used to avoid division by zero. Similarly, the empirical credit of operator 

Oi is defined as 

¯Qt(Oi) = 

P

> c∈C (t)
> i

¯Qt(c)

|C (t) 

> i

| , (9) where c denotes all possible code implementation that can be selected for Oi.The target operator is selected as 

O⋆ = arg min 

> Oi∈O

¯Qt(Oi), (10) Defines the lowest-credit implementation of O⋆ as c−,which can be calculated by 

c− = arg min 

> c∈C (t)
> O⋆

¯Qt(c). (11) If ¯Qt(O⋆) ≥ 0 and |C (t) 

> O⋆

| > 1, c− is deleted. If ¯Qt(O⋆) <

0 and |C (t) 

> O⋆

| < M , a newly generated implementation c+

is added to the candidate pool of O⋆. If ¯Qt(O⋆) < 0 and 

|C (t) 

> O⋆

| = M , c− is replaced by c+.Similarly, suppose a transition between a candidate code 

u ∈ C (t) 

> i

for Oi and a candidate code c ∈ C (t) 

> j

for Oj as 

e = ( u, c ), the transition credit of Oi and Oj can be 

¯Qt(Oi, O j ) = 

P

> u∈C (t)
> i

P

> c∈C (t)
> j

nt(e)Qt(e)

P

> u∈C (t)
> i

P

> c∈C (t)
> j

nt(e) + ϵ . (12) At each generation, we identify the removable edge with the lowest aggregated credit: 

(O− 

> i

, O − 

> j

) = arg min  

> (Oi,O j)∈E t

¯Qt(Oi, O j ). (13) If ¯Qt(O− 

> i

, O − 

> j

) ≥ 0, all existing edges are retained. Other-wise, the edge (O− 

> i

, O − 

> j

) is removed and replaced by a new edge, which is randomly generated by the LLM. The new edge should be currently absent and interface-compatible, so as to ensure a valid pipeline. If no valid edge exists, the DG remains unchanged. In summary, contextual credits guide implementation sampling, while their aggregated statistics drive both implementation-level and pipeline-level evolution. 

## 4 Experiments 

4.1 Experimental Setup 

Benchmarks. This paper evaluates DGA 2D on a com-prehensive benchmark suite covering 12 NP-hard com-binatorial optimization domains: resource scheduling 

(Job-Shop Scheduling (JSP), Flexible Job-Shop Schedul-ing (FJSP), Flow-Shop Scheduling (FSSP), Open-Shop Scheduling (OSSP), Resource-Constrained Project Schedul-ing (RCPSP), and Simple Assembly Line Balancing (S-ALBP)); vehicle routing (the Traveling Salesman Prob-lem (TSP) and the Capacitated Vehicle Routing Problem (CVRP)); spatial allocation (the 3D Bin Packing Problem (3D-BPP) and the Three-Dimensional Container Loading Problem (3D-CLP)); and graph optimization (the Maxi-mum Independent Set (MIS) and Maximum Cut (Max-Cut) problems). Detailed definitions of all 12 problems are provided in Ap-pendix A.2, while the dataset sources, numbers of instances, and problem-size ranges are reported in Appendix E. For de-tailed analysis in the main text, we select one representative domain from each category: FJSP, TSP, 3D-CLP, and MIS .Results on the remaining eight domains are reported in Ap-pendix F.2, which gives full evaluation on the cross-domain generalizability of DGA 2D. For each domain, the instances are divided into an evolu-tion set and a held-out test set. For each domain, the evolution and test sets are mutually exclusive. The test set is used only for final evaluation after the algorithm generation process is completed; detailed split sizes are provided in Appendix E. 

Baselines. DGA 2D is compared against three categories of baselines. (1) Classical solvers: OR-Tools CP-SAT solver is adopted as a general-purpose optimization baseline, together with an Iterated Local Search (ILS) baseline (Lourenço, Mar-tin, and Stützle 2018). (2) Learning-based methods: Rep-resentative neural solvers are involved, including POMO (Kwon et al. 2020) for TSP, ReSched (Xiao et al. 2026) for FJSP, SDDS (Sanokowski et al. 2025) for MIS and BPP-DRL (Hu et al. 2017) for 3D-CLP.(3) LLM-based AHD methods: Recent LLM-based AHD frameworks, including EoH (Liu et al. 2024), MCTS-AHD (Zheng et al. 2025), MEoH (Yao et al. 2025), and ReEvo (Ye et al. 2024), are also introduced for comparison. 

Implementation and Evaluation Protocol. DGA 2D is implemented in Python 3.13, and all experiments are con-ducted on a workstation equipped with an Intel Core i7-14650HX CPU, 16 GB of RAM, and an NVIDIA RTX 4090 GPU. The sampling temperature τ of the initialization process is set to 0.7. For each problem domain, the iterative algorithm evolution workflow is limited to 500 LLM calls. During the algorithm generation process, all generated algorithms are evaluated on the same benchmark instances under an iden-tical runtime protocol. Each of them is given a wall-clock budget of 90 seconds per instance. The ones that fail to com-pile, raise runtime errors, produce infeasible solutions, or exceed the time limit are assigned the lowest fitness. Unless otherwise specified, baseline implementations and hyperparameters follow the official code and the recom-mended settings reported in the corresponding publications. To account for the stochasticity of LLM generation and evolutionary search, we perform 10 independent runs with different random seeds and report the mean and standard deviation across these runs. Table 1: Performance of DGA 2D compared with baselines across combinatorial optimization benchmarks, grouped by LLM backbones. Obj.: Objective value ( ↓ lower is better, ↑ higher is better). Gap (%): normalized gap to the corresponding reference solution. Results are reported as mean ± standard deviation (10 trials). Bold signifies the best overall result and the best LLM-based result under each backbone. Gray indicates the best performance under the same LLM API. 

Method TSP FJSP MIS 3D-CLP 

Obj. ( 10 6) ↓ Gap (%) ± std ↓ Obj.( 10 5) ↓ Gap (%) ± std ↓ Obj.( 10 3) ↑ Gap (%) ± std ↓ Obj.( 10 1) ↑ Gap (%) ± std ↓

Baselines 

Reference 1.237 - 1.009 - 3.244 - 2.696 -ILS 1.240 0.48 1.123 11.31 2.997 8.41 2.577 5.51 Neural* 1.301 5.13 1.935 71.47 2.695 17.97 2.571 5.82 OR-Tools 1.247 0.76 1.076 8.82 3.044 7.45 2.563 6.10 

LLM: DeepSeek-V4-Flash 

EoH 1.496 19.73 ±0.49 1.352 28.76 ±4.45 2.759 16.31 ±0.53 2.670 0.88 ±0.48 MCTS-AHD 1.527 21.22 ±2.24 1.210 24.59 ±5.25 2.733 18.16 ±1.59 2.671 0.90 ±0.28 MEoH 1.484 19.17 ±0.91 1.330 26.37 ±2.33 2.787 16.48 ±0.49 2.672 0.73 ±0.02 ReEvo 1.467 18.20 ±3.91 1.311 24.91 ±5.47 2.614 21.89 ±3.15 2.668 1.07 ±0.26 DGA 2D (Ours) 1.307 5.24 ±2.79 1.107 9.94 ±1.85 2.870 10.40 ±1.80 2.687 0.51 ±0.76 

LLM: GPT-5.6 Sol 

EoH 1.465 20.10 ±1.08 1.327 26.80 ±1.30 2.769 16.68 ±0.29 2.677 0.71 ±0.02 MCTS-AHD 1.558 18.50 ±4.66 1.297 23.32 ±6.58 2.758 15.16 ±1.16 2.668 1.05 ±0.54 MEoH 1.461 20.73 ±3.34 1.309 26.63 ±0.60 2.787 16.41 ±0.41 2.669 1.06 ±0.53 ReEvo 1.482 22.60 ±2.20 1.358 21.60 ±7.06 2.783 16.78 ±0.33 2.662 1.26 ±0.01 DGA 2D (Ours) 1.298 4.70 ±1.71 1.075 6.54 ±2.52 3.057 6.61 ±1.52 2.686 0.53 ±0.37 * “Neural” denotes learning-based methods. 

Evaluation Metrics. We report the objective value (Obj.) and the normalized gap relative to the reference solution. The gap is computed for each test instance after normalizing the optimization direction, so that smaller values consistently indicate better performance. Hence, the reported gap is the average over all test instances. It is also noted that the EP+FF solution is used as the reference for 3D-CLP, while the best-known solutions (BKSs) are used as the references for the rest benchmarks. 

4.2 Comparative Studies 

4.2.1 Convergence Analysis 

Figure 3 illustrates the convergence trajectories of DGA 2Dcompared to the LLM-based AHD frameworks. First, it can be seen that DGA 2D exhibits a pattern of frequent, multi-step descents, ultimately converging to the smallest normalized gap over all instances. Second, the continued improvements observed over a long search horizon suggest that increased structural flexibility contributes to escaping local optima and identifying better solutions in later generations. 

4.2.2 Experiments on Benchmarks 

As shown in Table 1, DGA 2D outperforms all LLM-based AHD baselines across the four benchmarks under both DeepSeek-V4-Flash and GPT-5.6 Sol, using the same prompt template and evaluation budget. Averaged across the four tasks, DGA 2D reduces the normalized gap from 15.69% to 6.02% under DeepSeek-V4-Flash, corresponding to an abso-lute improvement of 9.67 percentage points over the strongest baseline. Under GPT-5.6 Sol, the average gap decreases from 0 100 200 300 400 500                

> Number of LLM Calls
> 10
> 15
> 20
> 25
> 30
> 35
> 40
> 45
> Best-so-far GAP (%)  DGA2D
> EoH
> MCTS-AHD
> MEoH
> ReEvo
> FJSP
> 0100 200 300 400 500
> Number of LLM Calls
> 5
> 10
> 15
> 20
> 25
> 30
> 35
> 40
> Best-so-far GAP (%)  DGA2D
> EoH
> MCTS-AHD
> MEoH
> ReEvo
> TSP
> 0100 200 300 400 500
> Number of LLM Calls
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> Best-so-far GAP (%)  DGA2D
> EoH
> MCTS-AHD
> MEoH
> ReEvo
> 3D-CLP
> 0100 200 300 400 500
> Number of LLM Calls
> 7
> 10
> 15
> 20
> 25
> 30
> Best-so-far GAP (%)  DGA2D
> EoH
> MCTS-AHD
> MEoH
> ReEvo
> MIS

Figure 3: Convergence analysis of DGA 2D compared with baseline frameworks. 15.56% to 4.60%, corresponding to a reduction of 10.96 per-centage points. The consistent gains across both backbones suggest that DGA 2D is not tied to a specific LLM. Compared to conventional baselines, it achieves the small-est gaps on FJSP and 3D-CLP and, with GPT-5.6 Sol, also outperforms ILS and OR-Tools on MIS. Although specialized solvers remain stronger on TSP, DGA 2D demonstrates ro-bust performance across diverse combinatorial optimization problems without relying on manually designed, problem-specific search strategies. 4.3 Ablation Studies 

4.3.1 Structural Flexibility 

To examine the structural flexibility, we compare four struc-tures: Fixed, Linear, Directed Acyclic Graph (DAG), and DG. These variants progressively relax the constraints on operator composition, with DG additionally allowing cyclic transitions. All variants are evaluated under the same search and evaluation budgets. Table 2: Effect of structural representations on algorithmic convergence. Results are reported as mean ± standard devi-ation (10 trials); lower values are better.                     

> Structure Gap (%) CVRP FJSP MIS 3D-CLP
> Fixed 8.62 ±1.34 12.07 ±3.73 9.06 ±1.83 1.32 ±0.22 Linear 0.99 ±0.44 8.45 ±2.61 8.86 ±1.79 0.98 ±0.17 DAG 1.09 ±0.49 5.34 ±0.65 6.95 ±0.41 0.85 ±0.19 DG 0.89 ±0.40 5.07 ±0.20 6.46 ±0.30 0.71 ±0.14

Table 2 shows that increasing structural flexibility gener-ally improves algorithm-generation performance. Across all four benchmarks, the DG representation achieves the lowest test gap, suggesting that cyclic operator transitions provide useful flexibility for expressing iterative refinement, repeated local search, and other complex algorithmic behaviors. Over-all, the results support DG as an effective representation for system-level automated algorithm design. 

4.3.2 Effect of Credit Assignment Mechanisms 

To evaluate the proposed credit assignment mechanism, we experimentally compare different levels of path depen-dence in credit assignment, including zero-order, first-order, second-order, and full-path settings. Table 3: Effect of credit assignment mechanisms on algorith-mic convergence. Results are reported as mean ± standard deviation (10 trials).                      

> Context Gap (%) TSP FJSP MIS 3D-CLP
> Zero 10.73 ±3.82 15.63 ±7.56 19.25 ±0.53 0.82 ±0.12 First 7.07 ±0.75 9.20 ±0.96 16.31 ±4.07 0.65 ±0.11
> Second 8.50 ±1.59 11.44 ±2.21 17.69 ±3.43 2.07 ±0.63 Full 19.80 ±5.21 19.11 ±7.02 23.74 ±7.36 9.72 ±3.06
> Note. Contexts zero, first, and second denote zero-order, first-order, and second-order Markov credit assignment, respectively; Full de-notes full-path credit assignment.

As shown in Table 3, two observations can be made. First, path-dependent credit is more effective than path-independent credit. In particular, first-order credit consis-tently outperforms zero-order credit across all four tasks, indicating that transitions between consecutive code imple-mentations provides a more informative basis for implemen-tation selection. Second, incorporating longer path histories does not nec-essarily improve performance. Both second-order and full-path credits perform worse than first-order credit, with full-path credit producing particularly large gaps. This suggests that excessive path dependence may introduce noisy or sparse scoring signals, leading to unstable search and poor conver-gence. Overall, first-order path dependence provides an effec-tive balance between informative implementation credit and search stability. 

4.3.3 Sensitivity to Operator Pool Capacity 

We evaluate how the heuristic operator pool size ( K ∈{4, 8, 15 , 20 }) affects performance across different problem domains. As shown in Table 4, K controls the degree of flexibility in operator selection. A small K restricts search diversity and may limit the achievable performance, as ob-served for K = 4 across all tasks. Increasing K provides more candidate operators and improves the potential solu-tion quality. However, an excessively large pool also enlarges the search space and may introduce redundant or competing operators. Consequently, different problems favor different Operator-pool capacities: FJSP performs best at K = 15 , whereas CVRP, MIS, and 3D-CLP obtain their best results at K = 20 .Overall, K should balance search flexibility and optimization stability according to the characteristics of each problem. Table 4: Sensitivity analysis of the heuristic operator pool size ( K-Level). Results are reported as mean ± standard deviation (10 trials).                         

> K-Level Gap (%) CVRP FJSP MIS 3D-CLP
> K= 4 16.19 ±3.42 27.01 ±4.01 8.93 ±1.12 1.61 ±0.27
> K= 8 2.07 ±0.51 17.79 ±2.86 7.54 ±0.73 1.63 ±0.19
> K= 15 5.31 ±1.38 12.29 ±1.71 5.50 ±0.91 1.50 ±0.12
> K= 20 1.07 ±0.39 12.34 ±1.51 4.19 ±0.59 1.41 ±0.18

## 5 Discussion and Conclusion 

DGA 2D extends LLM-driven AHD from isolated heuris-tic optimization to system-level algorithm evolution. Its directed-graph representation jointly evolves operator im-plementations and connectivity, while first-order context-dependent credit assignment enables feedback sharing across pipelines without the sparsity of full-path conditioning. Ex-periments across 12 combinatorial optimization problems demonstrate broad applicability, and results on four repre-sentative benchmarks show consistent improvements over LLM-based AHD baselines. Ablation studies further confirm the benefits of directed-graph representations and first-order implementation-transition scoring. DGA 2D remains limited by the computational cost of some generated algorithms, sensitivity to the initial oper-ator pool, and potential cross-module inconsistencies. Fu-ture work will focus on more efficient evaluation, adaptive operator-pool construction, and stronger program-invariant checking. 

## Acknowledgments 

This paper is supported by National Key Research and Devel-opment Program of China (Grant No. 2024YFB3311902), Beijing Natural Science Foundation (Grant No. L241018) and Beijing Nova Program (Grant No. 2024085). 

## References 

Bengio, Y.; Lodi, A.; and Prouvost, A. 2021. Machine Learn-ing for Combinatorial Optimization: A Methodological Tour d’Horizon. European Journal of Operational Research ,290(2): 405–421. Brandt, J.; Schede, E.; Haddenhorst, B.; Bengs, V.; Hüller-meier, E.; and Tierney, K. 2023. Ac-band: A combinatorial bandit-based approach to algorithm configuration. In Pro-ceedings of the AAAI Conference on Artificial Intelligence ,volume 37, 12355–12363. Burke, E. K.; Gendreau, M.; Hyde, M.; Kendall, G.; Ochoa, G.; Özcan, E.; and Qu, R. 2013. Hyper-heuristics: A survey of the state of the art. Journal of the Operational Research Society , 64(12): 1695–1724. Chen, B.; Zhu, S.; Liu, B.; Zhao, Y.; Pu, T.; Li, H.; and Zhu, Z. 2026. A2DEPT: Large Language Model-Driven Automated Algorithm Design via Evolutionary Program Trees. arXiv preprint arXiv:2604.24043 .Contreras Estrada, P.; Stützle, T.; and Pérez Cáceres, L. 2025. Automated Generation of Trajectory-based Metaheuristics for Capacitated Vehicle Routing. In Proceedings of the Ge-netic and Evolutionary Computation Conference Compan-ion , 2580–2583. Dat, P. V. T.; Doan, L.; and Binh, H. T. T. 2025. Hsevo: Elevating automatic heuristic design with diversity-driven harmony search and genetic algorithm using llms. In Pro-ceedings of the AAAI Conference on Artificial Intelligence ,volume 39, 26931–26938. Dokeroglu, T.; Kucukyilmaz, T.; and Talbi, E.-G. 2024. Hyper-heuristics: A survey and taxonomy. Computers & Industrial Engineering , 187: 109815. Drake, J. H.; Kheiri, A.; Özcan, E.; and Burke, E. K. 2020. Recent advances in selection hyper-heuristics. European Journal of Operational Research , 285(2): 405–428. Guo, H.; Ma, Z.; Chen, J.; Ma, Y.; Cao, Z.; Zhang, X.; and Gong, Y.-J. 2025. Configx: Modular configuration for evo-lutionary algorithms via multitask reinforcement learning. In Proceedings of the AAAI Conference on Artificial Intelli-gence , volume 39, 26982–26990. Hu, H.; Zhang, X.; Yan, X.; Wang, L.; and Xu, Y. 2017. Solv-ing a new 3d bin packing problem with deep reinforcement learning method. arXiv preprint arXiv:1708.05930 .Huang, Y.; Zhou, W.; Wang, Y.; Li, M.; Feng, L.; and Tan, K. C. 2024. Evolutionary Multitasking With Centralized Learning for Large-Scale Combinatorial Multiobjective Op-timization. IEEE Trans. Evol. Comput. , 28(5): 1499–1513. Kwon, Y.-D.; Choo, J.; Kim, B.; Yoon, I.; Gwon, Y.; and Min, S. 2020. Pomo: Policy optimization with multiple optima for reinforcement learning. Advances in neural information processing systems , 33: 21188–21198. Li, Y.; Guo, J.; Wang, R.; Zha, H.; and Yan, J. 2024. Fast t2t: Optimization consistency speeds up diffusion-based training-to-testing solving for combinatorial optimization. 

Advances in Neural Information Processing Systems , 37: 30179–30206. Liu, F.; Tong, X.; Yuan, M.; and Zhang, Q. 2023. Algo-rithm evolution using large language model. arXiv preprint arXiv:2311.15249 .Liu, F.; Xialiang, T.; Yuan, M.; Lin, X.; Luo, F.; Wang, Z.; Lu, Z.; and Zhang, Q. 2024. Evolution of Heuristics: Towards Efficient Automatic Algorithm Design Using Large Language Model. In Salakhutdinov, R.; Kolter, Z.; Heller, K.; Weller, A.; Oliver, N.; Scarlett, J.; and Berkenkamp, F., eds., Proceedings of the 41st International Conference on Machine Learning , volume 235 of Proceedings of Machine Learning Research , 32201–32223. PMLR. Liu, F.; Yao, Y.; Guo, P.; Yang, Z.; Lin, X.; Zhao, Z.; Tong, X.; Mao, K.; Lu, Z.; Wang, Z.; Yuan, M.; and Zhang, Q. 2026. A Systematic Survey on Large Language Models for Algorithm Design. ACM Comput. Surv. , 58(8): 218:1–218:32. Lourenço, H. R.; Martin, O. C.; and Stützle, T. 2018. Iterated local search: Framework and applications. In Handbook of metaheuristics , 129–168. Springer. Ma, Y. J.; Liang, W.; Wang, G.; Huang, D.-A.; Bastani, O.; Jayaraman, D.; Zhu, Y.; Fan, L.; and Anandkumar, A. 2023. Eureka: Human-Level Reward Design via Coding Large Lan-guage Models. In 2nd Workshop on Language and Robot Learning: Language as Grounding .Pei, J.; Liu, J.; and Mei, Y. 2024. Learning from offline and online experiences: A hybrid adaptive operator selection framework. In Proceedings of the genetic and evolutionary computation conference , 1017–1025. Romera-Paredes, B.; Barekatain, M.; Novikov, A.; Balog, M.; Kumar, M. P.; Dupont, E.; Ruiz, F. J.; Ellenberg, J. S.; Wang, P.; Fawzi, O.; et al. 2024. Mathematical discoveries from program search with large language models. Nature ,625(7995): 468–475. Sanokowski, S.; Berghammer, W. F.; Wang, H. P.; En-nemoser, M.; Hochreiter, S.; and Lehner, S. 2025. Scalable Discrete Diffusion Samplers: Combinatorial Optimization and Statistical Physics. In The Thirteenth International Con-ference on Learning Representations .Sun, Z.; and Yang, Y. 2023. Difusco: Graph-based diffusion solvers for combinatorial optimization. Advances in neural information processing systems , 36: 3706–3731. van Stein, N.; and Bäck, T. 2025. LLaMEA: A Large Language Model Evolutionary Algorithm for Automatically Generating Metaheuristics. IEEE Trans. Evol. Comput. ,29(2): 331–345. Wang, Y.; Han, Y.; Wang, Y.; Pan, Q.; and Wang, L. 2024. Sustainable Scheduling of Distributed Flow Shop Group: A Collaborative Multi-Objective Evolutionary Algorithm Driven by Indicators. IEEE Trans. Evol. Comput. , 28(6): 1794–1808. Wolpert, D. H.; and Macready, W. G. 1997. No free lunch theorems for optimization. IEEE Trans. Evol. Comput. , 1(1): 67–82. Xiao, X.; Zhang, C.; Song, W.; and Cao, Z. 2026. RESCHED: Rethinking Flexible Job Shop Scheduling from a Transformer-based Architecture with Simplified States. In 

The Fourteenth International Conference on Learning Rep-resentations .Xue, X.; Shu, T.; and Xia, J. 2025. Automated Generation of Hybrid Metaheuristics Using Learning-to-Rank. Algorithms ,18(6): 316. Yang, C.; Wang, X.; Lu, Y.; Liu, H.; Le, Q. V.; Zhou, D.; and Chen, X. 2024. Large Language Models as Optimiz-ers. In The Twelfth International Conference on Learning Representations .Yao, S.; Liu, F.; Lin, X.; Lu, Z.; Wang, Z.; and Zhang, Q. 2025. Multi-objective evolution of heuristic using large lan-guage model. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 39, 27144–27152. Ye, H.; Wang, J.; Cao, Z.; Berto, F.; Hua, C.; Kim, H.; Park, J.; and Song, G. 2024. Reevo: Large language models as hyper-heuristics with reflective evolution. Advances in neural information processing systems , 37: 43571–43608. Ye, H.; Wang, J.; Cao, Z.; Liang, H.; and Li, Y. 2023. Deep-ACO: Neural-enhanced ant systems for combinatorial opti-mization. Advances in neural information processing sys-tems , 36: 43706–43728. Zhang, C.; Cao, Z.; Song, W.; Wu, Y.; and Zhang, J. 2024. Deep Reinforcement Learning Guided Improvement Heuris-tic for Job Shop Scheduling. In The Twelfth International Conference on Learning Representations .Zhao, F.; Liu, Y.; Zhu, N.; Xu, T.; et al. 2023. A selec-tion hyper-heuristic algorithm with Q-learning mechanism. 

Applied Soft Computing , 147: 110815. Zheng, Z.; Xie, Z.; Wang, Z.; and Hooi, B. 2025. Monte Carlo Tree Search for Comprehensive Exploration in LLM-Based Automatic Heuristic Design. In Singh, A.; Fazel, M.; Hsu, D.; Lacoste-Julien, S.; Berkenkamp, F.; Maharaj, T.; Wagstaff, K.; and Zhu, J., eds., Proceedings of the 42nd In-ternational Conference on Machine Learning , volume 267 of 

Proceedings of Machine Learning Research , 78338–78373. PMLR. Supplementary Material 

## A Baseline Frameworks and Selection Rationale 

This section reviews four representative LLM-based automated heuristic design baselines and summarizes the mathematical formulations of the twelve combinatorial optimization problems considered in our experiments. 

A.1 Review of LLM-based AHD Frameworks 

We consider four representative LLM-based automated heuristic design frameworks: EoH (Liu et al.2024), MCTS-AHD (Zheng et al.2025), MEoH (Yao et al.2025), and ReEvo (Ye et al.2024). As summarized in Table 5, these methods represent four complementary directions in heuristic evolution: thought code co-evolution, tree-structured search, multi-objective diversity-aware optimization, and reflection-guided evolution. Table 5: Characteristics of the selected LLM-based AHD baselines.            

> Method Search structure Guidance mechanism Key characteristic EoH Evolutionary search Fitness-guided variation Thought–code co-evolution MCTS-AHD Tree-structured search Visit and value statistics Preservation and revisiting of search branches MEoH Multi-objective search Pareto dominance and AST diversity Diverse non-dominated heuristic set ReEvo Evolutionary search Short- and long-term reflection Reflection-guided crossover and mutation

We select these methods based on methodological representativeness, experimental comparability, and recency. They generate executable heuristics and can be evaluated under a common benchmark set, objective function, and computational budget. To-gether, they cover complementary search structures, feedback mechanisms, representations, and diversity-management strategies, providing a broad basis for evaluating the proposed method. Concurrent with our work, A2DEPT also targets system-level algorithm design through tree-structured evolution of com-plete solver programs. In contrast, DGA 2D represents the solver space as a directed operator graph with operator-specific implementation pools, constructs complete algorithms as directed walks, and assigns context-dependent credit to local imple-mentation transitions. This design supports component reuse and localized evolution of both graph connectivity and operator implementations. We do not include A2DEPT in the empirical comparison because, to the best of our knowledge, no official implementation was publicly available at the time of submission; therefore, we restrict the unified comparison to methods with publicly available implementations. 

A.2 Mathematical Formulations of the 12 COPs 

Throughout this subsection, let B := {0, 1}. For the disjunctive scheduling constraints, M big > 0 denotes a sufficiently large constant. 

A.2.1 Scheduling Problem 

Definition 1 (JSP) . Given a set of jobs J = {1, 2, . . . , n } and a set of machines M = {1, 2, . . . , m }, each job i ∈ J consists of a fixed sequence of m operations. Let σi,r ∈ M denote the machine required by the r-th operation of job i, and let pij denote the processing time of job i on machine j. Let Sij ≥ 0 denote the start time of job i’s operation on machine j.For each pair of jobs i, k ∈ J with i < k and each machine j ∈ M , let xikj ∈ B equal 1 if job i is processed before job k on machine j, and 0 otherwise. The JSP is formulated as 

min Cmax (14) s.t. Si,σ i,r + pi,σ i,r ≤ Si,σ i,r +1 , ∀i ∈ J, r = 1 , . . . , m − 1, (15) 

Sij + pij ≤ Skj + M big (1 − xikj ), ∀i, k ∈ J, i < k, j ∈ M , (16) 

Skj + pkj ≤ Sij + M big xikj , ∀i, k ∈ J, i < k, j ∈ M , (17) 

Si,σ i,m + pi,σ i,m ≤ Cmax , ∀i ∈ J, (18) 

Sij ≥ 0, ∀i ∈ J, j ∈ M , (19) 

xikj ∈ B, ∀i, k ∈ J, i < k, j ∈ M . (20) Definition 2 (FSSP) . Given a set of jobs J = {1, 2, . . . , n } and a set of machines M = {1, 2, . . . , m }, every job must be processed on all machines in the common order 1, 2, . . . , m . Let pij denote the processing time of job i on machine j, and let 

Sij ≥ 0 denote its start time. For each pair of jobs i, k ∈ J with i < k and each machine j ∈ M , let xikj ∈ B equal 1 if job i precedes job k on machine 

j, and 0 otherwise. The FSSP is formulated as 

min Cmax (21) s.t. Si,j +1 ≥ Sij + pij , ∀i ∈ J, j = 1 , . . . , m − 1, (22) 

Sij + pij ≤ Skj + M big (1 − xikj ), ∀i, k ∈ J, i < k, j ∈ M , (23) 

Skj + pkj ≤ Sij + M big xikj , ∀i, k ∈ J, i < k, j ∈ M , (24) 

Si,m + pi,m ≤ Cmax , ∀i ∈ J, (25) 

Sij ≥ 0, ∀i ∈ J, j ∈ M , (26) 

xikj ∈ B, ∀i, k ∈ J, i < k, j ∈ M . (27) 

Definition 3 (FJSP) . Given a set of jobs J = {1, 2, . . . , n } and a set of machines M = {1, 2, . . . , m }, each job i ∈ J consists of an ordered sequence of operations Oi1, O i2, . . . , O in i . Each operation Oij can be processed on any machine in its eligible machine set Mij ⊆ M , with processing time pijk if assigned to machine k ∈ M ij . Let Sij ≥ 0 denote the start time of operation Oij .For every k ∈ M ij , let yijk ∈ B equal 1 if operation Oij is assigned to machine k, and 0 otherwise. For every i, h ∈ J with 

i < h , operations Oij and Ohℓ , and machine k ∈ M ij ∩ M hℓ , let xijhℓk ∈ B equal 1 if Oij precedes Ohℓ on machine k, and 0 otherwise. The FJSP is formulated as 

min Cmax (28) s.t. X

> k∈M ij

yijk = 1 , ∀i ∈ J, j = 1 , . . . , n i, (29) 

Si,j +1 ≥ Sij + X

> k∈M ij

pijk yijk , ∀i ∈ J, j = 1 , . . . , n i − 1, (30) 

Sij + pijk ≤ Shℓ + M big (3 − yijk − yhℓk − xijhℓk ), ∀i,h ∈J, i<h, j=1 ,...,n i, ℓ =1 ,...,n h,k∈M ij ∩M hℓ 

, (31) 

Shℓ + phℓk ≤ Sij + M big (2 − yijk − yhℓk + xijhℓk ), ∀i,h ∈J, i<h, j=1 ,...,n i, ℓ =1 ,...,n h,k∈M ij ∩M hℓ 

, (32) 

Sij + X

> k∈M ij

pijk yijk ≤ Cmax , ∀i ∈ J, j = 1 , . . . , n i, (33) 

Sij ≥ 0, ∀i ∈ J, j = 1 , . . . , n i, (34) 

yijk ∈ B, ∀i ∈ J, j = 1 , . . . , n i, k ∈ M ij , (35) 

xijhℓk ∈ B, ∀i,h ∈J, i<h, j=1 ,...,n i, ℓ =1 ,...,n h,k∈M ij ∩M hℓ 

. (36) 

Definition 4 (OSSP) . Given a set of jobs J = {1, 2, . . . , n } and a set of machines M = {1, 2, . . . , m }, each job i ∈ J has one operation on every machine j ∈ M , with processing time pij . Unlike JSP and FSSP, the machine-processing order of each job is not predefined. Let Sij ≥ 0 denote the start time of job i’s operation on machine j.For every i, k ∈ J with i < k and j ∈ M , let xikj ∈ B equal 1 if job i precedes job k on machine j. For every i ∈ J and 

j, ℓ ∈ M with j < ℓ , let yijℓ ∈ B equal 1 if job i’s operation on machine j precedes its operation on machine ℓ. The OSSP is formulated as 

min Cmax (37) s.t. Sij + pij ≤ Cmax , ∀i ∈ J, j ∈ M , (38) 

Sij + pij ≤ Skj + M big (1 − xikj ), ∀i, k ∈ J, i < k, j ∈ M , (39) 

Skj + pkj ≤ Sij + M big xikj , ∀i, k ∈ J, i < k, j ∈ M , (40) 

Sij + pij ≤ Siℓ + M big (1 − yijℓ ), ∀i ∈ J, j, ℓ ∈ M , j < ℓ, (41) 

Siℓ + piℓ ≤ Sij + M big yijℓ , ∀i ∈ J, j, ℓ ∈ M , j < ℓ, (42) 

Sij ≥ 0, ∀i ∈ J, j ∈ M , (43) 

xikj ∈ B, ∀i, k ∈ J, i < k, j ∈ M , (44) 

yijℓ ∈ B, ∀i ∈ J, j, ℓ ∈ M , j < ℓ. (45) Definition 5 (SALBP-1) . Given a set of tasks V = {1, 2, . . . , n }, where each task i ∈ V has a deterministic task time ti. There is a precedence graph G = ( V, E ), where a directed edge (i, j ) ∈ E indicates that task i must be completed before task j can start. The tasks are to be assigned to a sequence of ordered workstations k ∈ { 1, 2, . . . , m max }. Let mmax be an upper bound on the number of workstations. Let xik ∈ B equal 1 if task i is assigned to workstation k, and 0 otherwise. Let yk ∈ B equal 1 if workstation k is used, and 0 otherwise. The number of utilized workstations is m = Pmmax  

> k=1

yk.Given a fixed cycle time C (the maximum available time at each station), the Simple Assembly Line Balancing Problem of type 1 (SALBP-1) aims to find an assignment of tasks to workstations that minimizes the total number of utilized workstations m. The mathematical model is: 

min 

> mmax

X

> k=1

yk

s.t. 

> mmax

X

> k=1

xik = 1 , ∀i ∈ V

X

> i∈V

ti · xik ≤ C · yk, ∀k ∈ { 1, 2, . . . , m max }

> mmax

X

> k=1

k · xik ≤

> mmax

X

> k=1

k · xjk , ∀(i, j ) ∈ Eyk+1 ≤ yk, ∀k ∈ 1, 2, . . . , m max − 1

xik ∈ { 0, 1}, yk ∈ { 0, 1}

Definition 6 (RCPSP) . Given a project consisting of a set of activities V = {0, 1, . . . , n, n + 1 }, where 0 and n + 1 represent dummy start and end activities with zero duration. Each actual activity i ∈ { 1, . . . , n } has a deterministic processing time di.The precedence constraints among activities are defined by a set of directed edges E, where (i, j ) ∈ E indicates that activity 

j cannot start until activity i is fully completed. The project requires a set of renewable resources R, where each resource 

k ∈ R has a constant maximum capacity Kk per time period. Processing activity i requires rik units of resource k at every time instance during its execution. Let T := {0, 1, . . . , T max } denote the discrete time horizon. Let xit ∈ B equal 1 if activity i

starts at time t, and 0 otherwise. For the dummy activities, set d0 = dn+1 = 0 and r0k = rn+1 ,k = 0 for every k ∈ R. Given a planning horizon Tmax (an upper bound of the makespan), the Resource-Constrained Project Scheduling Problem (RCPSP) aims to determine the start time for each activity to minimize the total project duration while satisfying both precedence and resource constraints. The mathematical model is: 

min 

> T

X

> t=0

t · xn+1 ,t 

s.t. 

> T

X

> t=0

xit = 1 , ∀i ∈ V

> T

X

> t=0

t · xjt ≥

> T

X

> t=0

(t + di) · xit , ∀(i, j ) ∈ E

X

> i∈Vt

X 

> τ=max {0,t −di+1 }

rik · xiτ ≤ Kk, ∀k ∈ R, t ∈ { 0, 1, . . . , T max }

xit ∈ { 0, 1}, ∀i ∈ V, t ∈ { 0, 1, . . . , T max }

A.2.2 Vehicle Routing 

Definition 7 (CVRP) . Given a complete graph G = ( V, E ), where V = {0, 1, . . . , n } is the set of nodes. Node 0 represents the central depot, and C = {1, 2, . . . , n } represents a set of customers. Each customer i ∈ C has a deterministic non-negative demand qi. A fleet of K homogeneous vehicles, each with a maximum capacity Q, is stationed at the depot. The travel cost (or distance) between node i and node j is given as cij . The Capacitated Vehicle Routing Problem (CVRP) aims to find a set of vehicle routes that minimizes the total travel cost, such that every customer is visited exactly once by exactly one vehicle, all routes start and end at the depot, and the total demand on any route does not exceed the vehicle capacity Q. The mathematical model is: 

min X

> i∈V

X

> j∈V

cij · xij s.t. X

> i∈V,i ̸=j

xij = 1 , ∀j ∈ C

X 

> j∈V,j ̸=i

xij = 1 , ∀i ∈ C

X

> j∈C

x0j ≤ K

X

> i∈C

xi0 ≤ Kui − uj + Q · xij ≤ Q − qj , ∀i, j ∈ C(i̸ = j)

qi ≤ ui ≤ Q, ∀i ∈ Cxij ∈ { 0, 1}, ∀i, j ∈ V

Definition 8 (TSP) . Given a complete graph G = ( V, E ), where V = {1, 2, . . . , n } is a set of nodes (or cities) and E is the set of directed edges connecting them. A travel cost or distance cij is associated with each edge (i, j ) ∈ E. The Traveling Salesman Problem (TSP) aims to find a closed tour of minimal total cost that visits every node exactly once and returns to the starting node. The mathematical model (MTZ formulation) is: 

min X

> i∈V

X 

> j∈V,j ̸=i

cij · xij 

s.t. X 

> j∈V,j ̸=i

xij = 1 , ∀i ∈ V

X

> i∈V,i ̸=j

xij = 1 , ∀j ∈ Vui − uj + n · xij ≤ n − 1, ∀i, j ∈ 2, 3, . . . , n (i̸ = j)

xij ∈ { 0, 1}, ∀i, j ∈ V

1 ≤ ui ≤ n − 1, ∀i ∈ 2, 3, . . . , n 

A.2.3 Bin Packing 

Definition 9 (3D-BPP) . Given a set of rectangular items I = {1, 2, . . . , n } and a set of candidate bins K = {1, 2, . . . , m }, each item i ∈ I has fixed length li, width wi, height hi, and volume vi = liwihi. All bins are identical, with length L, width W ,and height H. The Three-Dimensional Bin Packing Problem (3D-BPP) aims to orthogonally pack all items into the minimum number of bins. Let sik ∈ B equal 1 if item i is assigned to bin k, and 0 otherwise, and let uk ∈ B equal 1 if bin k is used. Let (Xi, Y i, Z i)

denote the coordinates of the lower-left-back corner of item i within its assigned bin. For every pair of items i, j ∈ I with i < j ,let aij , b ij , c ij , d ij , e ij , f ij ∈ B represent the six possible relative positions of the two items along the three coordinate axes. The 3D-BPP is formulated as 

min X

> k∈K

uk (46) s.t. X

> k∈K

sik = 1 , ∀i ∈ I, (47) 

sik ≤ uk, ∀i ∈ I, k ∈ K , (48) 

uk+1 ≤ uk, k = 1 , . . . , m − 1, (49) 

Xi + li ≤ L, ∀i ∈ I, (50) 

Yi + wi ≤ W, ∀i ∈ I, (51) 

Zi + hi ≤ H, ∀i ∈ I, (52) 

Xi + li ≤ Xj + M big (1 − aij ), ∀i, j ∈ I, i < j, (53) 

Xj + lj ≤ Xi + M big (1 − bij ), ∀i, j ∈ I, i < j, (54) 

Yi + wi ≤ Yj + M big (1 − cij ), ∀i, j ∈ I, i < j, (55) 

Yj + wj ≤ Yi + M big (1 − dij ), ∀i, j ∈ I, i < j, (56) 

Zi + hi ≤ Zj + M big (1 − eij ), ∀i, j ∈ I, i < j, (57) 

Zj + hj ≤ Zi + M big (1 − fij ), ∀i, j ∈ I, i < j, (58) 

aij + bij + cij + dij + eij + fij ≥ sik + sjk − 1, ∀i, j ∈ I, i < j, k ∈ K , (59) 

Xi, Y i, Z i ≥ 0, ∀i ∈ I, (60) 

uk ∈ B, ∀k ∈ K , (61) 

sik ∈ B, ∀i ∈ I, k ∈ K , (62) 

aij , b ij , c ij , d ij , e ij , f ij ∈ B, ∀i, j ∈ I, i < j. (63) 

Evaluator convention. Let 

B := X

> k∈K

uk (64) denote the number of used bins. Because the symmetry-breaking constraints enforce consecutive bin usage, the last used bin is well-defined as 

k⋆ := max {k ∈ K | uk = 1 }. (65) Its volume-utilization ratio is 

ρlast =

P 

> i∈I

visik ⋆

LW H . (66) The executable evaluator minimizes 

F3D -BPP = B + λ (1 − ρlast ) , λ = 0 .1. (67) The first term is the canonical 3D-BPP objective, whereas the second term distinguishes solutions using the same number of bins by preferring a more highly utilized last bin. Since 0 ≤ λ(1 − ρlast ) ≤ 0.1 < 1, reducing the number of used bins by one always dominates any difference in the secondary term. Therefore, the evaluator is lexicographically consistent with first minimizing the number of bins and then maximizing the utilization of the last bin. 

Definition 10 (3D-CLP) . Given a set of rectangular items I = {1, 2, . . . , n } and a single rectangular container with length L,width W , and height H, each item i ∈ I has length li, width wi, height hi, and volume vi = liwihi. The Three-Dimensional Container Loading Problem (3D-CLP) aims to pack a feasible subset of the items into the container so as to maximize the total packed volume. Let si ∈ B equal 1 if item i is packed into the container, and 0 otherwise. Let (Xi, Y i, Z i) denote the coordinates of the lower-left-back corner of item i. For every pair of items i, j ∈ I with i < j , let aij , b ij , c ij , d ij , e ij , f ij ∈ B represent the six possible relative positions of the two items along the three coordinate axes. The 3D-CLP is formulated as 

max X

> i∈I

visi (68) s.t. Xi + li ≤ L + M big (1 − si), ∀i ∈ I, (69) 

Yi + wi ≤ W + M big (1 − si), ∀i ∈ I, (70) 

Zi + hi ≤ H + M big (1 − si), ∀i ∈ I, (71) 

Xi + li ≤ Xj + M big (1 − aij ), ∀i, j ∈ I, i < j, (72) 

Xj + lj ≤ Xi + M big (1 − bij ), ∀i, j ∈ I, i < j, (73) 

Yi + wi ≤ Yj + M big (1 − cij ), ∀i, j ∈ I, i < j, (74) 

Yj + wj ≤ Yi + M big (1 − dij ), ∀i, j ∈ I, i < j, (75) 

Zi + hi ≤ Zj + M big (1 − eij ), ∀i, j ∈ I, i < j, (76) 

Zj + hj ≤ Zi + M big (1 − fij ), ∀i, j ∈ I, i < j, (77) 

aij + bij + cij + dij + eij + fij ≥ si + sj − 1, ∀i, j ∈ I, i < j, (78) 

Xi, Y i, Z i ≥ 0, ∀i ∈ I, (79) 

si ∈ B, ∀i ∈ I, (80) 

aij , b ij , c ij , d ij , e ij , f ij ∈ B, ∀i, j ∈ I, i < j. (81) 

Evaluator convention. The packed-volume utilization is defined as 

ρfill =

P 

> i∈I

visi

LW H . (82) Since the executable evaluation engine follows a minimization convention, it evaluates each solution using 

F3D -CLP = 100 (1 − ρfill ) = 100 − 100 

P 

> i∈I

visi

LW H . (83) Thus, F3D -CLP = 0 corresponds to a completely filled container, and lower values indicate better performance. Since the container volume LW H is fixed, minimizing F3D -CLP is equivalent to maximizing the total packed volume. For compactness, the formulation above assumes fixed item orientations. The executable evaluator additionally considers the item-specific axis-aligned orientations permitted by the orientation_allowed flags; this extension changes the feasibility set but not the packed-volume objective. 

A.2.4 Graph Optimization 

Definition 11 (MIS) . Given an undirected graph G = ( V, E ), the Maximum Independent Set (MIS) problem aims to find a vertex subset S ⊆ V with maximum cardinality such that no two vertices in S are adjacent. The mathematical model is: 

max f (S) = |S|

s.t. {u, v } /∈ E, ∀u, v ∈ S, u ̸ = v. 

Definition 12 (Max-Cut) . Given an undirected graph G = ( V, E ) with edge weights W = {wuv }{u,v }∈ E , the Maximum Cut (Max-Cut) problem aims to find a vertex subset S ⊆ V that maximizes the total weight of the edges crossing the partition between S and V \ S. The mathematical model is: 

max  

> S⊆V

f (S) = X 

> {u,v }∈ Eu∈S, v ∈V\S

wuv .

## B Implementation Details of DGA 2D

This appendix provides the implementation-level details of DGA 2D. Algorithm 1 describes the overall training and candidate-evaluation procedure, while Algorithm 2 details the credit-guided evolution of the operator graph and implementation pools. Algorithm 1 summarizes the complete training procedure of DGA 2D. Before training, we randomly sample a fixed batch I of k

evolution instances from Xevo . The same batch is reused across all generations, ensuring that candidate scores remain directly comparable when maintaining the best discovered program A⋆. The pipeline policy samples a length-bounded directed walk Algorithm 1: DGA 2D Training 

Require: Domain evaluator f ; evolution instances Xevo ; operator repository Olib 

Require: Generations tmax ; algorithm candidate per generation N ; shared instances k; temperature τ ; maximum implementation-pool size 

M ; credit rate α

Ensure: Best discovered program A⋆, Evolved graph Dtmax +1 and implementation pools Ctmax +1 

1: (O, D1, C(1) ) ← LLMInitialization (f, Olib )

2: Initialize pipeline policy πpipe  

> θ

, transition credits Q1 ← 0 and transition counts n1(e) ← 0

3: A⋆ ← ∅, S⋆ ← −∞ 

4: I ← RandomSample (Xevo , k )

5: for t = 1 , . . . , t max do 

6: for j = 1 , . . . , N do 

7: Pt,j = ( Oin , O t,j, 1, . . . , O t,j,L t,j , O out ) ∼ πpipe  

> θ

(· | D t) ▷ bounded directed walk 8: ct,j, 0 ← cstart 

9: for i = 1 , . . . , L t,j do 

10: ct,j,i ∼ Softmax c∈C (t)(Ot,j,i )

 Qt(e(ct,j,i −1,c )) 

> τ



11: et,j,i ← e(ct,j,i −1, c t,j,i )

12: end for 

13: At,j ← (ct,j, 1, . . . , c t,j,L t,j )

14: end for 

15: {yt,j,x } ← EvaluateCandidates ({A t,j }Nj=1 , I)

16: for j = 1 , . . . , N do 

17: St,j ← − 1

k

P 

> x∈I

g(yt,j,x ; x)

18: if St,j > S ⋆ then 

19: S⋆ ← St,j 

20: A⋆ ← A t,j 

21: end if 

22: end for 

23: eRt,j ← Standardize ({St,j }Nj=1 ), j = 1 , . . . , N 

24: Lpipe ← − 1

N

PNj=1 eRt,j log πpipe  

> θ

(Pt,j | D t)

25: Update θ by minimizing Lpipe 

26: Qt+1 ← Qt, nt+1 ← nt

27: for j = 1 , . . . , N do 

28: for i = 1 , . . . , L t,j do 

29: Qt+1 (et,j,i ) ← (1 − α)Qt+1 (et,j,i ) + α eRt,j 

30: nt+1 (et,j,i ) ← nt+1 (et,j,i ) + 1 

31: end for 

32: end for 

33: (Dt+1 , C(t+1) ) ← DualLevelEvolution (Dt, C(t), Q t+1 , n t+1 , M )

34: end for 

35: return Dtmax +1 , C(tmax +1) , A⋆Pt,j from Oin to Oout in the current graph Dt. For every operator in the sampled pipeline, a concrete implementation is selected according to the transition credits: 

ct,j,i ∼ Softmax c∈C (t)(Ot,j,i )

 Qt(e(ct,j,i −1, c )) 

τ



,

where e(c, c ′) denotes the implementation-level transition from c to c′. The temperature τ controls the balance between exploiting high-credit transitions and exploring alternative implementations. Each instantiated candidate At,j is evaluated on the shared instance batch, and its score is computed as 

St,j = − 1

k

X

> x∈I

g(yt,j,x ; x).

The best-performing candidate observed throughout training is retained as A⋆. The candidate scores are standardized into eRt,j 

and used to optimize the pipeline policy through 

Lpipe = − 1

N

> N

X

> j=1

eRt,j log πpipe  

> θ

(Pt,j | D t).

Here, πpipe  

> θ

(Pt,j | Dt) denotes the probability of sampling pipeline Pt,j under the current pipeline policy. Its autoregressive factorization and the corresponding masked operator-selection distributions are detailed in Appendix D.1. Thus, pipelines with positive standardized rewards become more likely to be sampled, whereas poorly performing pipelines are suppressed. The same reward is also assigned to every implementation transition activated by the candidate: 

Qt+1 (et,j,i ) = (1 − α)Qt+1 (et,j,i ) + α eRt,j ,

while nt+1 (et,j,i ) records its cumulative activation count. The updated credits are subsequently used by Algorithm 2 to evolve the implementation pools and operator graph. Algorithm 2: Credit-Guided Dual-Level Evolution 

Require: Current graph Dt = ( Ot, Et); implementation pools C(t); transition credits Qt(e); transition counts nt(e); maximum pool size M

Ensure: Updated graph Dt+1 and implementation pools C(t+1) 

1: ¯Qt(c), ¯Qt(Oi) ← AggregateImplementationCredits (Qt(e), n t(e)) 

2: O⋆ ← arg min Oi∈O ¯Qt(Oi)

3: c− ← arg min c∈C (t)

> O⋆

¯Qt(c)

4: C(t+1) ← C (t)

5: if ¯Qt(O⋆) ≥ 0 and |C (t) 

> O⋆

| > 1 then 

6: C(t+1) (O⋆) ← C (t)(O⋆) \ { c−}

7: else if ¯Qt(O⋆) < 0 then 

8: c+ ← LLMGenerateImplementation (O⋆, C(t)(O⋆)) 

9: if |C (t)(O⋆)| < M then 

10: C(t+1) (O⋆) ← C (t)(O⋆) ∪ { c+}

11: else 

12: C(t+1) (O⋆) ←  C(t)(O⋆) \ { c−} ∪ { c+}

13: end if 

14: end if 

15: ¯Qt(Oi, O j ) ← AggregateEdgeCredits (Qt(e), n t(e)) 

16: (O− 

> i

, O − 

> j

) ← arg min (Oi,O j )∈E t ¯Qt(Oi, O j )

17: Et+1 ← E t

18: if ¯Qt(O− 

> i

, O − 

> j

) < 0 then 

19: (O+ 

> i

, O + 

> j

) ← LLMGenerateCompatibleEdge (Dt)

20: if the new edge is valid and preserves an input–output route then 

21: Et+1 ← E t \ { (O− 

> i

, O − 

> j

)} ∪ { (O+ 

> i

, O + 

> j

)}

22: end if 

23: end if 

24: Dt+1 ← (Ot, Et+1 )

25: return Dt+1 , C(t+1) Implementation- and Operator-Level Credit Aggregation. The procedure AggregateImplementationCredits first ag-gregates transition-level credits into an empirical credit for each implementation c:

¯Qt(c) = 

P 

> e∈T t(c)

nt(e)Qt(e)

P 

> e∈T t(c)

nt(e) + ϵ , (84) where Tt(c) contains all activated transitions involving c, and ϵ > 0 prevents division by zero. The corresponding operator credit is the average credit of its current implementations: 

¯Qt(Oi) = 

P 

> c∈C (t)(Oi)

¯Qt(c)

|C (t)(Oi)| . (85) The lowest-credit operator O⋆ and its lowest-credit implementation c− are then selected for pruning or local repair. 

Operator-Edge Credit Aggregation. The procedure AggregateEdgeCredits aggregates all implementation level transitions associated with an operator edge (Oi, O j ):

¯Qedge  

> t

(Oi, O j ) = 

P  

> e∈T t(Oi,O j)

nt(e)Qt(e)

P  

> e∈T t(Oi,O j)

nt(e) + ϵ , (86) where Tt(Oi, O j ) contains transitions from implementations of Oi to implementations of Oj . The edge with the lowest aggregated credit is considered for replacement. A proposed replacement is accepted only if it is valid and the resulting graph preserves at least one route from Oin to Oout .

Credit-Guided Evolution. If the lowest operator credit is non-negative, DGA 2D removes its lowest-credit implementation when more than one implementation remains, thereby controlling pool complexity. If the operator credit is negative, a new implementation is generated and either added to the pool or used to replace c− when the maximum pool size M has been reached. At the graph level, only negatively credited edges are considered for replacement. LLMGenerateImplementation 

returns a candidate only after it passes AST, signature, isolated-runtime, and composed smoke validation. If validation fails, the implementation pool remains unchanged 

## C Prompt Engineering and Templates 

The implementation begins from a fixed, versioned operator library. The external proposer handles constrained graph initial-ization, scheduled edge edits, and code generation after a rule-based version decision. The pipeline policy itself is a locally loaded Qwen model. In the templates below, blue specifies the role, purple monospace marks injected fields, red marks hard constraints, and green gives the required output. 

C.1 Directed-Graph Initialization 

Template I: Constrained DG Initialization 

Initialize a directed operator graph for an optimization RL system. 

Domain contract: {domain_prompt} Hard YAML controls: {bounded_H_controls} Candidate operator IDs: {operator_ids} Safe baseline: {initial_pipeline} 

Return JSON with exactly the keys H, operators , entry_nodes , exit_nodes , and edges .1. Select only listed operator IDs and obey every enabled YAML bound. 2. Entry nodes must be initialization operators and cannot be revisited. 3. Exit nodes must terminate a valid solution; at least one START -to-END route must exist. 4. Directed cycles are allowed only among non-initialization nodes. Return JSON only. START and END are implicit. C.2 Local Pipeline-Policy Context 

Template II: Constrained Pipeline Sampling 

Select a bounded walk through the current directed operator graph. 

Domain summary: {domain_summary} Instance summary: {scalar_sizes_and_array_shapes} Action aliases: {fixed_width_ASCII_mapping} 

At each step, generation is restricted by a token trie to aliases of outgoing edges that can still reach END within the remaining length. The initialization node cannot be revisited. Log-probability includes only generated operator and END tokens; prompt and padding tokens are excluded. Generate one legal alias sequence ending in END .

C.3 Transactional Graph Evolution 

Template III: GraphDelta Proposal 

Propose a small edge-only edit to the current directed graph. 

Current nodes and edges: {graph_json} Recent generation summary: {performance_summary} Domain contract: {domain_prompt} 

Return JSON with add_edges and delete_edges , containing at most two edges each. Do not add nodes. Every endpoint must already exist. The edit is applied to a graph copy and committed only if interface checks, DG constraints, reachability, and smoke execution all pass. Return JSON only; an empty delta is allowed. 

C.4 Rule-Guided Operator Evolution 

Template IV: ADD/REPLACE Code Generation 

Generate one implementation for a selected operator. 

Domain contract: {domain_prompt} Target operator: {category}|{name} Parent implementation: {parent_code} Failure context, if any: {diagnostic} 

The trainer has already selected ADD or REPLACE using fault, dominance, trial count, and version-cap rules. Preserve exactly def run(env_data, state, calc_makespan_fn): , use only documented environment fields, and update the returned state consistently. Generated code is activated only after AST, signature, isolated-runtime, and composed smoke validation. DELETE never calls the proposer. Return one Python code block and no prose. 

C.5 Condensed Domain Contracts 

The following problem cards are shared by graph initialization, graph evolution, and operator-code generation. They match the executable evaluator contracts. C.5.1 Travelling Salesman Problem (TSP) 

Objective: 

Minimize the total length of a Hamiltonian cycle that visits every one of the N cities exactly once and returns to its starting city. 

Encoding: 

state.sequence is an integer permutation of all city indices [0 , . . . , N − 1] . The permutation is interpreted cyclically, so its rotations describe the same tour; city 0 may be fixed as the conventional start. 

Decoder: 

Visit cities in sequence order, close the tour with the edge from the last city back to the first, and sum the corresponding entries of 

distance_matrix . Distances use TSPLIB EUC_2D : dij = ⌊p(xi − xj )2 + ( yi − yj )2 + 0 .5⌋.

Environment keys: 

num_nodes, coords, distance_matrix, upper_bound .

Constraints: 

The sequence must have length num_nodes , contain every index in [0 , num_nodes − 1] exactly once, and remain a valid permutation after every operator. 

C.5.2 3D Bin Packing Problem (3D-BPP) 

Objective: 

Pack all N fixed-orientation 3D items into the fewest fixed-size bins. Minimize bins + 0 .1(1 − last-bin utilization ), where the fractional term prefers greater utilization of the last bin among equal-bin solutions. 

Encoding: 

state.sequence is an int32 priority permutation of item indices [0 , . . . , N − 1] ; items are presented to the packing decoder in this order. 

Decoder: 

Apply Extreme-Points First-Fit. For each item, scan existing bins and candidate points consisting of the origin and the positive x, y,and z faces of placed items; sort points by z, then y, then x (Bottom–Left–Front), place at the first feasible point, or open a new bin at the origin. 

Environment keys: 

num_activities, bin_size, item_sizes, item_volumes, upper_bound .

Constraints: 

The sequence must contain every item exactly once. Items cannot rotate or overlap and each item must lie entirely within one bin; all indices and array accesses must remain in range. 

C.5.3 3D Container Loading Problem (3D-CLP) 

Objective: 

Maximize the volume utilization of a single fixed-size container by packing a feasible subset of the N items. The minimization engine evaluates 100 − fill-rate percentage , so 0 denotes a completely filled container and lower values are better. 

Encoding: 

state.sequence is an int32 priority permutation of all item indices [0 , . . . , N −1] ; items are presented to the packing decoder in this order, while items that cannot be placed are skipped. 

Decoder: 

Apply Extreme-Points First-Fit in the single container. For each item, collect the origin and the positive x, y, and z faces of placed items, sort these points by z, then y, then x (Bottom–Left–Front), and try only the axis-aligned orientations permitted by that item’s 

orientation_allowed flags. Place the item at the first feasible position or skip it if none exists. 

Environment keys: 

num_activities, bin_size, item_sizes, item_volumes, orientation_allowed, fill_upper_bound, upper_bound, lower_bound, name .

Constraints: 

The sequence must contain every item index exactly once and all accesses must remain in range. Only one container is available; every packed orientation must be permitted, lie fully within the container, and not overlap another packed item. C.5.4 Capacitated Vehicle Routing Problem (CVRP) 

Objective: 

Minimize the total distance of all depot-to-depot vehicle routes while serving every customer demand with vehicles of fixed capacity. 

Encoding: 

state.sequence is a flat list of 1-indexed customer IDs, separated by the depot marker 0; for example, [1,2,0,3,4] 

represents routes (1 , 2) and (3 , 4) . The first and last depot visits are implicit. 

Decoder: 

Split the sequence at depot markers. For each nonempty route, sum the edge from depot_idx to its first customer, all consecutive customer edges, and the final edge back to the depot; add these route costs to obtain total distance. 

Environment keys: 

dist_matrix, demands, capacity, num_nodes, depot_idx .

Constraints: 

Every customer in [1 , num_nodes −1] must appear exactly once; every route demand must not exceed capacity ; and consecutive zeros, empty routes, duplicates, omissions, and out-of-range IDs are invalid. With r routes, the sequence length is the number of customers plus r − 1.

C.5.5 Flexible Job Shop Scheduling Problem (FJSP) 

Objective: 

Minimize makespan while scheduling every operation of every job on one of its eligible alternative machines, preserving each job’s operation order. 

Encoding: 

A dual chromosome is used. The OS chromosome state.sequence has length total_ops and contains job j exactly 

op_counts[j] times. The MS chromosome stored in state.metadata["machine_alloc"] has shape (num_jobs, max_ops) ; entry (j, o ) stores the actual machine ID selected for operation o of job j.

Decoder: 

Scan OS left to right; the occurrence count of a job identifies its next operation. Read the actual machine ID from MS and its duration from processing_matrix , then start it at the maximum of that job’s ready time and the machine’s available time. The largest completion time is the makespan. 

Environment keys: 

num_jobs, num_machines, op_counts, eligible_counts, machines_matrix, times_matrix, processing_matrix, max_ops, total_ops .

Constraints: 

OS length and job multiplicities must be exact. Every stored machine ID must occur among the operation’s nonnegative eligible IDs, and its processing time must be finite. Job precedence and single-machine capacity must hold. An ineligible assignment receives the invalid makespan 999999999. 

C.5.6 Flow Shop Scheduling Problem (FSSP) 

Objective: 

Minimize makespan while processing every job once on each machine in the common fixed machine order. 

Encoding: 

state.sequence is an operation-based integer sequence of length total_ops . Each job ID appears exactly num_machines 

times, and its kth occurrence denotes that job’s operation on machine k.

Decoder: 

Scan the sequence left to right, infer the current machine from the job’s prior occurrence count, and schedule the operation at the maximum of that job’s ready time and the machine’s available time using processing_times[machine,job] . The maximum completion time is Cmax .

Environment keys: 

num_jobs, num_machines, processing_times, total_ops .

Constraints: 

The sequence length must equal num_jobs * num_machines ; each job in [0 , num_jobs − 1] must occur exactly 

num_machines times. Extra occurrences, invalid IDs, job-order violations, or machine conflicts are invalid. C.5.7 Job Shop Scheduling Problem (JSP) 

Objective: 

Minimize makespan while scheduling all job operations in their fixed technological order on their prescribed machines. 

Encoding: 

state.sequence is an operation-based sequence of length total_ops . Each job ID appears exactly num_machines times; its kth occurrence represents operation k of that job. 

Decoder: 

Scan the sequence from left to right. Use each job’s occurrence count as its operation index, read the prescribed machine and duration from machines_matrix and times_matrix , and start at the maximum of job and machine availability. Return the largest machine completion time. 

Environment keys: 

num_jobs, num_machines, machines_matrix, times_matrix, total_ops .

Constraints: 

The sequence must have length num_jobs * num_machines , contain every job exactly num_machines times, and use only IDs in [0 , num_jobs −1] . Job precedence and single-machine capacity must be preserved; violations receive makespan 999999999. 

C.5.8 Maximum Independent Set (MIS) 

Objective: 

Find a maximum-cardinality set of mutually non-adjacent vertices. The minimization engine uses score −| S|, so a larger feasible independent set has a smaller, better score. 

Encoding: 

state.sequence is a binary int32 vector of length num_vertices ; entry i = 1 selects vertex i, and i = 0 excludes it. 

Decoder: 

Collect all vertices whose bit is 1, verify that no selected vertex has a selected neighbor in adj , and return the negative selected-vertex count. An infeasible or malformed vector returns the domain’s large positive INVALID_SCORE .

Environment keys: 

num_vertices (also available as total_vertices), adj, name .

Constraints: 

The vector must have the declared length and contain only binary values with valid vertex indices. For every edge (u, v ), at most one of sequence[u] and sequence[v] may equal 1. 

C.5.9 Maximum Cut (Max-Cut) 

Objective: 

Partition the vertices of a weighted undirected graph into two disjoint sets so that the total weight of edges crossing between them is maximized. The minimization engine returns the negative cut weight, so a lower value represents a heavier cut. 

Encoding: 

state.sequence is a binary partition-assignment vector of length num_nodes ; sequence[i] = 0 or 1 identifies the partition containing vertex i.

Decoder: 

Traverse the graph stored in CSR form through offsets , neighbors , and edge_weights ; accumulate the weight of every edge whose endpoints have different assignments, and return the negative total cut weight. 

Environment keys: 

num_nodes, num_edges, neighbors, edge_weights, offsets .

Constraints: 

The assignment vector must have length num_nodes , contain only 0 or 1, and assign every vertex to exactly one partition. Vertex IDs, CSR offsets, neighbor indices, and edge-weight accesses must remain within their valid ranges. C.5.10 Open Shop Scheduling Problem (OSSP) 

Objective: 

Minimize makespan while processing every job exactly once on every machine; machine order is free, but operations sharing a job or machine cannot overlap. 

Encoding: 

state.sequence is a permutation of all operation IDs [0 , . . . , N M − 1] , where job_id = op // num_machines and 

mach_id = op % num_machines .

Decoder: 

Use a greedy list scheduler in permutation order. Start each operation at the maximum of its job’s and machine’s availability, add 

ossp_times[job,machine] , update both availability values, and return the latest completion time. 

Environment keys: 

num_jobs, num_machines, total_ops, ossp_times, upper_bound, lower_bound .

Constraints: 

The sequence must contain each operation ID in [0 , total_ops − 1] exactly once. Operations are non-preemptive, and neither two operations of one job nor two operations on one machine may overlap. 

C.5.11 Resource-Constrained Project Scheduling Problem (RCPSP) 

Objective: 

Minimize project makespan while scheduling all activities subject to finish–start precedence and per-period renewable-resource capacities. Activities 0 and N − 1 are zero-duration, zero-demand dummy start and end nodes. 

Encoding: 

state.sequence is an int32 topological permutation of all activity IDs [0 , . . . , N − 1] ; for every precedence edge (u, v ), u

appears before v.

Decoder: 

Apply the Serial Schedule Generation Scheme. In sequence order, compute the latest predecessor finish, then find the earliest contiguous interval of the activity’s duration during which every resource has sufficient residual capacity; allocate its requests there. The latest finish time is the makespan. 

Environment keys: 

num_activities, num_resources, durations, requests, capacities, adj_matrix, edges, upper_bound .

Constraints: 

The sequence must be a complete topological permutation. Every predecessor must finish before its successor starts, and at every time unit the summed request for each renewable resource must not exceed its capacity. 

C.5.12 Simple Assembly Line Balancing Problem Type 1 (SALBP-1) 

Objective: 

For fixed cycle time C, assign every precedence-constrained task to the minimum number of sequential workstations. The number of utilized workstations is the objective to be minimized, while upper_bound stores the theoretical lower bound ⌈total time /C ⌉.

Encoding: 

state.sequence is an int32 topological permutation of all task IDs [0 , . . . , N − 1] ; every predecessor appears before its successor. 

Decoder: 

Apply greedy First-Fit Station Assignment in sequence order. For each task, set its earliest allowed station to the latest station of its predecessors, place it in the first station at or after that point whose remaining cycle time is sufficient, or open a new station. 

Environment keys: 

num_tasks, cycle_time, times, adj_matrix, edges, upper_bound, total_time .

Constraints: 

The sequence must be a complete topological permutation, each task must be assigned exactly once, every station load must not exceed cycle_time , and each predecessor’s station index must be no greater than its successor’s. D Reinforcement-Learning Training and Credit Assignment 

D.1 Autoregressive Factorization and Local Credit-Induced Optimization 

This section derives the autoregressive factorization of the pipeline policy and analyzes the optimization behavior of the credit-weighted pipeline loss in the main text. We first show that the probability of a complete pipeline factorizes into the conditional probabilities of its operator-selection decisions. We then derive the corresponding gradient update and establish its local effect on both the complete pipeline and its individual actions. 

Pipeline representation and masked action distribution. Recall that the j-th pipeline sampled at generation t is 

Pt,j =  Oin , O t,j, 1, . . . , O t,j,L t,j , O out 

 . (87) For notational convenience, define 

Ot,j, 0 := Oin , Ot,j,L t,j +1 := Oout , (88) and let 

ht,j,i := ( Ot,j, 0, O t,j, 1, . . . , O t,j,i −1) (89) denote the complete decoding history before the i-th operator-selection decision. Let Mt,j,i denote the feasible action set at position i. It contains the outgoing operators that preserve at least one continuation to 

Oout within the remaining pipeline-length budget. Given the pre-softmax policy logit zθ (O | ht,j,i , D t), the masked conditional distribution is: 

πθ (O | ht,j,i , D t) = 



exp ( zθ (O | ht,j,i , D t)) 

X

> O′∈M t,j,i

exp ( zθ (O′ | ht,j,i , D t)) , O ∈ M t,j,i ,

0, O / ∈ M t,j,i .

(90) 

Autoregressive factorization of the pipeline policy. Because the pipeline is generated sequentially, the probability of the complete pipeline follows from the probability chain rule: 

πpipe  

> θ

(Pt,j | Dt) = πθ (Ot,j, 1 | ht,j, 1, D t)

× πθ (Ot,j, 2 | ht,j, 2, D t) × · · · × πθ

 Ot,j,L t,j +1 | ht,j,L t,j +1 , D t

 . (91) Here, πθ (O | ht,j,i , D t) denotes the probability induced by the constrained token-level decoder of generating the complete alias associated with operator O. The quantity zθ (O | ht,j,i , D t) is the corresponding operator-level log-score. Equivalently, 

πpipe  

> θ

(Pt,j | Dt) =  

> Lt,j +1

Y

> i=1

πθ (Ot,j,i | ht,j,i , D t) . (92) The product starts at i = 1 because Oin is a fixed sentinel rather than a sampled action. It ends at Lt,j + 1 because selecting 

Oout is also part of the generated pipeline. Taking the logarithm of Eq. (92) gives 

log πpipe  

> θ

(Pt,j | Dt) =  

> Lt,j +1

X

> i=1

log πθ (Ot,j,i | ht,j,i , D t) . (93) Thus, the log-probability of a complete pipeline is the sum of the log-probabilities of all operator-selection decisions appearing in that pipeline. 

Credit-weighted surrogate optimization. During policy optimization, the evaluated pipeline credit is treated as constant with respect to θ. Define 

Ct,j := sg 

 eRt,j 



, (94) where sg( ·) denotes the stop-gradient operator. The credit-weighted pipeline loss is 

Lpipe (θ) = − 1

N

> N

X

> j=1

Ct,j log πpipe  

> θ

(Pt,j | Dt) , (95) where N denotes the number of candidate pipelines sampled and evaluated at generation t, θ denotes the trainable parameters of the pipeline policy. Substituting Eq. (93) into Eq. (95) yields 

Lpipe (θ) = − 1

N

> N

X 

> j=1
> Lt,j +1

X

> i=1

Ct,j log πθ (Ot,j,i | ht,j,i , D t) . (96) Therefore, the terminal credit of a complete candidate algorithm is propagated to every operator-selection decision in its sampled pipeline. Since Ct,j is detached from the computation graph, differentiating Eq. (95) gives 

∇θ Lpipe (θ) = − 1

N

> N

X

> j=1

Ct,j ∇θ log πpipe  

> θ

(Pt,j | Dt) . (97) A gradient-descent update with learning rate η > 0 is consequently 

θ+ = θ − η∇θ Lpipe (θ)= θ + ηN

> N

X

> j=1

Ct,j ∇θ log πpipe  

> θ

(Pt,j | Dt) . (98) The following theorem formalizes the local contribution of an individual sampled pipeline to this update. 

Theorem 1 (Local reinforcement effect of pipeline credit) . Fix a sampled pipeline Pt,j and define its log-probability as 

ft,j (θ) := log πpipe  

> θ

(Pt,j | Dt) . (99) Ignoring the positive batch-scaling factor 1/N , the gradient-descent direction contributed by this pipeline is 

dt,j := Ct,j ∇θ ft,j (θ). (100) Then the directional derivative of the pipeline log-probability satisfies 

ddη ft,j (θ + ηd t,j )

> η=0

= Ct,j ∥∇ θ ft,j (θ)∥22 . (101) Consequently, whenever ∇θ ft,j (θ)̸ = 0 , a positive credit Ct,j > 0 gives a locally increasing direction for the sampled pipeline’s log-probability, whereas a negative credit Ct,j < 0 gives a locally decreasing direction. 

Proof. By the chain rule and the definition of dt,j ,

ddη ft,j (θ + ηd t,j )

> η=0

= ∇θ ft,j (θ)⊤dt,j (102) 

= Ct,j ∇θ ft,j (θ)⊤∇θ ft,j (θ) (103) 

= Ct,j ∥∇ θ ft,j (θ)∥22 . (104) Since the squared gradient norm is nonnegative, and is strictly positive whenever the gradient is nonzero, the sign of the directional derivative is determined by the sign of Ct,j .

Action-level effect of pipeline credit. Theorem 1 characterizes the first-order effect of an individual pipeline credit on the log-probability of the complete sampled pipeline. The following corollary further describes how the same credit affects the operator selected at each decoding position. 

Corollary 1 (Action-level effect of pipeline credit) . Fix a sampled pipeline Pt,j and a decoding position i ∈ { 1, . . . , L t,j + 1 }.For notational convenience, define 

πt,j,i (O) := πθ (O | ht,j,i , D t) , zt,j,i (O) := zθ (O | ht,j,i , D t) , (105) for every feasible operator O ∈ M t,j,i . Let 

O⋆t,j,i := Ot,j,i (106) denote the operator selected at position i. The corresponding action-level loss term is 

ℓt,j,i = −Ct,j log πt,j,i 

 O⋆t,j,i 

 . (107) Then, for every feasible operator O ∈ M t,j,i ,

∂ℓ t,j,i 

∂z t,j,i (O) = −Ct,j 

1 O = O⋆t,j,i − πt,j,i (O) . (108) Consequently, under a local gradient-descent step of size η > 0 in logit space, the selected operator satisfies 

z+

> t,j,i

 O⋆t,j,i 

 = zt,j,i 

 O⋆t,j,i 

 + ηC t,j 

1 − πt,j,i 

 O⋆t,j,i 

 , (109) whereas every unselected feasible operator O̸ = O⋆t,j,i satisfies 

z+

> t,j,i

(O) = zt,j,i (O) − ηC t,j πt,j,i (O). (110) Therefore, when Ct,j > 0, the update increases the logit of the selected operator and decreases the logits of all competing feasible operators. When Ct,j < 0, both update directions are reversed. Proof. Because the policy is a masked softmax over the feasible action set Mt,j,i , we have 

πt,j,i (O) = exp ( zt,j,i (O)) 

X

> O′∈M t,j,i

exp ( zt,j,i (O′)) , O ∈ M t,j,i . (111) Hence, the log-probability of the selected operator can be written as 

log πt,j,i 

 O⋆t,j,i 

 = zt,j,i 

 O⋆t,j,i 

 − log X

> O′∈M t,j,i

exp ( zt,j,i (O′)) . (112) Differentiating Eq. (112) with respect to the logit of an arbitrary feasible operator O gives 

∂ log πt,j,i 

 O⋆t,j,i 



∂z t,j,i (O) = 1 O = O⋆t,j,i − πt,j,i (O). (113) Multiplying Eq. (113) by −Ct,j yields Eq. (108). For the selected operator O = O⋆t,j,i , Eq. (108) becomes 

∂ℓ t,j,i 

∂z t,j,i 

 O⋆t,j,i 

 = −Ct,j 

1 − πt,j,i 

 O⋆t,j,i 

 . (114) Applying the gradient-descent update 

z+

> t,j,i

(O) = zt,j,i (O) − η ∂ℓ t,j,i 

∂z t,j,i (O) (115) to Eq. (114) gives Eq. (109). For an unselected feasible operator O̸ = O⋆t,j,i , Eq. (108) reduces to 

∂ℓ t,j,i 

∂z t,j,i (O) = Ct,j πt,j,i (O). (116) Substituting Eq. (116) into Eq. (115) gives Eq. (110). Finally, since 

1 − πt,j,i 

 O⋆t,j,i 

 ≥ 0, πt,j,i (O) ≥ 0, (117) the directions of both updates are determined by the sign of Ct,j . A positive credit increases the selected logit and decreases every competing feasible logit, while a negative credit reverses these directions. 

Remark 1 (Scope of the action-level result) . Corollary 1 describes a local gradient-descent update in the logits associated with one fixed decoding history. In the implemented policy, the logits are produced by shared parameters θ, and gradients from multiple decoding positions and sampled pipelines are aggregated in a single update. Consequently, the corollary does not imply that the probability of every selected operator changes monotonically after an arbitrary finite parameter update. It instead characterizes the intended first-order contribution of an individual action-level loss term. 

D.2 Statistical Analysis of Contextual Credit Estimation 

This section analyzes how the context order used for implementation-level credit assignment affects finite-sample conditional-mean estimation. We compare zero-order, first-order, higher-order, and full-path contextual estimators under a fixed finite sample of activated implementation positions. 

Context hierarchy. Consider n activated implementation positions indexed by s ∈ { 1, . . . , n }. Let H denote the space of possible implementation histories, and let Hs ∈ H denote the complete implementation history up to and including position s.Let Ys denote the standardized terminal credit assigned to that position. For each context order r ∈ { 0, 1, . . . , R }, let Xr denote the ambient space of possible order-r contexts, and let 

ϕr : H → X r (118) be a deterministic context-extraction map. Define 

X(r) 

> s

:= ϕr (Hs) ∈ X r . (119) In particular, 

X(0)  

> s

= Cs, X(1)  

> s

= ( C− 

> s

, C s), (120) where C− 

> s

and Cs denote the predecessor and current implementations, respectively. For r ≥ 2, X(r) 

> s

contains a longer implementation suffix, while X(R) 

> s

may represent the complete implementation history. We assume that the context representations form nested refinements. For every r ∈ { 0, . . . , R −1}, there exists a deterministic projection 

ρr+1 →r : Xr+1 → X r (121) such that 

X(r) 

> s

= ρr+1 →r



X(r+1) 

> s



for every s. (122) For r ≥ 1, let ρr→1 : Xr → X 1 denote the corresponding composition of adjacent projections, with ρ1→1 equal to the identity. Hence, 

X(1)  

> s

= ρr→1



X(r)

> s



, r ≥ 1. (123) The set of order-r contexts observed in the realized sample is 

X (n) 

> r

:= 

n

X(r) 

> s

: s = 1 , . . . , n 

o

⊆ X r . (124) For each x ∈ X (n) 

> r

, define 

Ir (x) := 

n

s : X(r) 

> s

= x

o

, nr (x) := |I r (x)|, (125) and let 

dr := |X (n) 

> r

| (126) denote the number of active order-r context cells. By construction, nr (x) ≥ 1 for every x ∈ X (n) 

> r

.

First-order sufficiency and fixed-design observation model. We assume that the first-order context is sufficient for the conditional expected terminal credit: 

E[Ys | Hs] = μ



X(1) 

> s



, (127) where 

μ(x) := E[Ys | X(1)  

> s

= x], x ∈ X 1, (128) is assumed not to depend on the position index s.For the exact finite-sample calculation below, we additionally condition on the realized context design 

X := 

n

X(r) 

> s

: s = 1 , . . . , n, r = 0 , . . . , R 

o

and assume the homoskedastic, conditionally uncorrelated noise model 

Ys = μ



X(1) 

> s



+ ϵs, (129) with 

E[ϵs | X] = 0 , Cov( ϵs, ϵ t | X) = σ21{s = t}. (130) All expectations and variances in the remainder of this subsection are conditional on X; this conditioning is suppressed for notational simplicity. For each context order r, define the empirical cell-mean estimator 

bqr (x) := 1

nr (x)

X 

> s∈I r(x)

Ys, x ∈ X (n) 

> r

. (131) 

Design-weighted conditional-mean estimation risk. We evaluate each estimator at the observed design points: 

Rr := 1

n

> n

X

> s=1

E

bqr



X(r)

> s



− μ



X(1) 

> s

 2

. (132) For a zero-order context u ∈ X (n)0 , define its design-weighted first-order mean as 

ν(u) := 1

n0(u)

X

> s∈I 0(u)

μ



X(1) 

> s



. (133) The representation-induced squared approximation bias of the zero-order estimator is 

B0 := 1

n

> n

X

> s=1

h

μ



X(1) 

> s



− ν



X(0) 

> s

i 2

. (134) Theorem 2 (Finite-sample bias–variance comparison under first-order sufficiency) . Under Eqs. (122), (127), (129), and (130), the risks of the empirical cell-mean estimators satisfy 

R0 = B0 + σ2d0

n , (135) and, for every r ≥ 1,

Rr = σ2dr

n . (136) The nested refinement property implies 

d0 ≤ d1 ≤ · · · ≤ dR. (137) Consequently, 

R1 ≤ R r , r ≥ 1. (138) Moreover, 

R1 ≤ R 0 ⇐⇒ B0 ≥ σ2(d1 − d0)

n . (139) If the inequality in Eq. (139) is strict and 

dr > d 1, r ≥ 2, (140) then the first-order estimator is the unique risk minimizer among the empirical cell-mean estimators considered here: 

R1 < Rr , r̸ = 1 . (141) 

Proof. Fix r ≥ 1 and an active context x ∈ X (n) 

> r

. By Eq. (123), every s ∈ I r (x) has the same first-order context ρr→1(x).Therefore, 

bqr (x) = 1

nr (x)

X 

> s∈I r(x)

h

μ



X(1) 

> s



+ ϵs

i

= μ(ρr→1(x)) + 1

nr (x)

X 

> s∈I r(x)

ϵs. (142) Hence, 

E[bqr (x)] = μ(ρr→1(x)) , (143) and Eq. (130) gives 

Var[ bqr (x)] = σ2

nr (x) . (144) Grouping the risk in Eq. (132) by active context cells yields 

Rr = 1

n

X 

> x∈X (n)
> r

X 

> s∈I r(x)

E

bqr (x) − μ



X(1) 

> s

 2

= 1

n

X 

> x∈X (n)
> r

nr (x) Var[ bqr (x)] = 1

n

X 

> x∈X (n)
> r

nr (x) σ2

nr (x) = σ2dr

n . (145) This proves Eq. (136). We next consider the zero-order estimator. For any u ∈ X (n)0 ,

bq0(u) = 1

n0(u)

X

> s∈I 0(u)

h

μ



X(1) 

> s



+ ϵs

i

= ν(u) + ϵu, (146) where 

ϵu := 1

n0(u)

X

> s∈I 0(u)

ϵs. (147) By Eq. (130), 

E[ϵu] = 0 , Var[ ϵu] = σ2

n0(u) . (148) For every s ∈ I 0(u),

bq0(u) − μ



X(1) 

> s



= ν(u) − μ



X(1) 

> s



+ ϵu. (149) Squaring, taking expectations, and using E[ϵu] = 0 give 

R0 = 1

n

X 

> u∈X (n)0

X

> s∈I 0(u)

h

ν(u) − μ



X(1) 

> s

i 2

+ 1

n

X 

> u∈X (n)0

n0(u) σ2

n0(u)= B0 + σ2d0

n , (150) which proves Eq. (135). It remains to compare the active cell counts. For every r ∈ { 0, . . . , R − 1}, the restriction of ρr+1 →r to X (n) 

> r+1

is surjective onto X (n) 

> r

: every active order-r context is realized by at least one sample position, whose order-(r + 1) context projects to it. Therefore, 

dr ≤ dr+1 , (151) which proves Eq. (137). Combining this monotonicity with Eq. (136) proves Eq. (138). Finally, using Eqs. (135) and (136), 

R1 ≤ R 0 ⇐⇒ σ2d1

n ≤ B0 + σ2d0

n

⇐⇒ B0 ≥ σ2(d1 − d0)

n . (152) This proves Eq. (139). If the inequality is strict, then R1 < R0. If additionally Eq. (140) holds, then Eq. (136) gives R1 < Rr

for every r ≥ 2, proving Eq. (141). 

Interpretation. Theorem 2 separates the effect of context order into representation-induced approximation bias squared and estimation variance. Zero-order credit pools positions that share the current implementation but have different predecessors, which produces the bias term B0. Under first-order sufficiency, every context order r ≥ 1 has zero approximation bias. Higher-order and full-path contexts induce partitions that are no coarser than the first-order partition and therefore cannot reduce the number of active credit cells. Under the homoskedastic, conditionally uncorrelated model, their finite-sample variance is consequently no smaller than that of the first-order estimator, and is strictly larger whenever the refinement increases the number of active cells. 

Relation to the online credit update. Suppose that the k-th activation of a context x yields credit Yk(x). The recursion 

Qk(x) = 



1 − 1

k



Qk−1(x) + 1

k Yk(x) (153) satisfies 

Qk(x) = 1

k

> k

X

> m=1

Ym(x), k ≥ 1, (154) regardless of the initialization Q0(x). Hence, the estimator in Eq. (131) is obtained exactly when the context-specific learning rate is the reciprocal activation count. With a constant learning rate, the update becomes an exponentially weighted estimator; in that case, Theorem 2 characterizes the context-partition bias–variance tradeoff but not the exact finite-sample risk of the implemented constant-step estimator. 

Scope of the noise model. The conditionally uncorrelated position-level noise model in Eq. (130) is an idealized assumption used to isolate the statistical effect of context-partition granularity. In the implemented algorithm, multiple transitions, including repeated occurrences of the same transition, may receive the same standardized terminal credit from one candidate. Generation-level standardization may also induce dependence across observations. Under a general correlated-noise model, 

Var[ bqr (x)] = 1

nr (x)2

X 

> s,t ∈I r(x)

Cov( ϵs, ϵ t | X), (155) so additional covariance terms enter the finite-sample risk and the simplified forms in Eqs. (135)–(136) need not hold. Scope of the first-order assumption. The result does not claim that first-order contextual credit is universally optimal. Its comparison relies on Eq. (127). If longer histories contain additional information about the conditional expected terminal credit, then higher-order estimators may reduce approximation bias, and that benefit must be compared against their larger finite-sample estimation variance. 

D.3 Spectral Expressivity of Directed-Graph Algorithm Spaces 

This section characterizes the structural expressivity of the directed-graph representation through the number and asymptotic growth of graph-valid operator pipelines. Throughout this section, a pipeline refers only to an operator-level directed walk in the current graph. Different code implementations of the same operator and execution-level validity are not distinguished. Thus, the analysis concerns the structural pipeline space rather than the number of fully instantiated executable algorithms. We first express the number of fixed-length pipelines exactly using powers of the graph adjacency matrix. We then contrast the finite-depth behavior of directed acyclic graphs with the unbounded iterative behavior enabled by directed cycles. Finally, we show that the exponential limsup growth rate of the pipeline space is determined by the spectral radius of the dominant input–output-relevant strongly connected components. 

Input–output-relevant graph. Fix a generation t and write the current directed operator graph as 

D = ( V, E ). (156) Let 

s := Oin , z := Oout (157) denote the input and output sentinel nodes. We assume that s̸ = z and that D contains at least one directed path from s to z, as guaranteed by the graph-validity checks. A vertex is called input–output relevant if it is reachable from s and can reach z. Define 

Vio := {v ∈ V : s ⇝ v and v ⇝ z} , (158) and let 

Dio := D[Vio ] (159) be the subgraph induced by these vertices. Vertices outside Vio cannot occur in any directed walk from s to z and are therefore irrelevant to the following analysis. Let 

A ∈ { 0, 1}|Vio |×| Vio | (160) be the adjacency matrix of Dio , with entries 

Auv := 

1, (u, v ) ∈ E, 

0, (u, v ) /∈ E. (161) For v ∈ Vio , let ev denote the corresponding standard basis vector. For every integer ℓ ≥ 0, let Wℓ denote the number of directed walks from s to z containing exactly ℓ edges. A pipeline with 

L functional operators contains L + 1 graph edges and is therefore counted by WL+1 .

Lemma 1 (Exact counting of fixed-length pipelines) . For every integer ℓ ≥ 0,

Wℓ = e⊤ 

> s

Aℓez . (162) Consequently, when candidate pipelines are required to contain at least one functional operator, the number of pipelines containing at most Lmax functional operators is 

W≤Lmax =

> Lmax +1

X

> ℓ=2

e⊤ 

> s

Aℓez . (163) 

Proof. We use the standard identity 

(Aℓ)uv = the number of length-ℓ directed walks from u to v. (164) For ℓ = 0 , A0 = I, which counts the unique zero-edge walk from each vertex to itself. For ℓ = 1 , the identity follows directly from the definition of the adjacency matrix. Assume that the identity holds for some ℓ ≥ 1. Then 

(Aℓ+1 )uv = X

> w∈Vio

(Aℓ)uw Awv . (165) By the induction hypothesis, (Aℓ)uw counts all length-ℓ walks from u to w. Multiplication by Awv retains exactly those walks that can be extended by the edge (w, v ). Summing over w counts all length-(ℓ + 1) walks from u to v.Setting u = s and v = z proves Eq. (162). Summing the exact counts over all permitted lengths proves Eq. (163). Proposition 1 (Finite depth of DAG representations) . Suppose that Dio is a directed acyclic graph and let 

nio := |Vio |. (166) Then 

Anio = 0 , (167) and hence 

Wℓ = 0 , ℓ ≥ nio . (168) Equivalently, a graph-valid pipeline in a fixed-size DAG contains at most nio − 2 functional operators. By contrast, if Dio contains a directed cycle, then it admits input-to-output walks of unbounded length. 

Proof. A directed walk in a DAG cannot visit the same vertex twice, because a repeated vertex would induce a directed cycle. Therefore, every directed walk in an nio -vertex DAG contains at most nio − 1 edges. It follows that every entry of Anio is zero, proving Eq. (167). Lemma 1 then gives Eq. (168). Since a pipeline with L functional operators contains L + 1 edges, its number of functional operators is at most nio − 2.Now suppose that Dio contains a directed cycle. Every vertex on that cycle is reachable from s and can reach z. Concatenating a fixed entrance path, an arbitrary number of traversals of the cycle, and a fixed exit path produces input-to-output walks of unbounded length. 

Spectral growth rate. Let 

Sio (169) denote the collection of strongly connected components of Dio . For each S ∈ Sio , let 

AS (170) be the adjacency matrix of the subgraph induced by S. Define the input–output-relevant spectral radius as 

ρ⋆ := max 

> S∈Sio

ρ(AS ), (171) where ρ(·) denotes the matrix spectral radius. 

Theorem 3 (Spectral growth of the structural pipeline space) . Suppose that Dio admits input-to-output walks of unbounded length. Then 

lim sup 

> ℓ→∞

W 1/ℓ ℓ = ρ⋆. (172) Thus, ρ⋆ is the exponential limsup growth rate of the number of fixed-length structural pipelines. 

Proof. We first establish the upper bound. Arrange the strongly connected components of Dio in a topological order of the condensation graph. The adjacency matrix A then has Frobenius block upper-triangular form, with diagonal blocks AS .Therefore, 

ρ(A) = max 

> S∈Sio

ρ(AS ) = ρ⋆. (173) Using the induced matrix 1-norm and the nonnegativity of Aℓ,

Wℓ = ( Aℓ)sz ≤ ∥ Aℓ∥1. (174) By Gelfand’s formula, 

lim  

> ℓ→∞

∥Aℓ∥1/ℓ  

> 1

= ρ(A) = ρ⋆. (175) Combining Eqs. (174) and (175) yields 

lim sup 

> ℓ→∞

W 1/ℓ ℓ ≤ ρ⋆. (176) For the lower bound, choose a strongly connected component S⋆ ∈ Sio satisfying 

ρ(AS⋆ ) = ρ⋆, (177) and write 

B := AS⋆ . (178) Choose arbitrary vertices u, v ∈ S⋆. Since S⋆ is input–output relevant, there exists a path from s to u of some fixed length a,and a path from v to z of some fixed length b.The matrix B is irreducible because S⋆ is strongly connected. A standard consequence of the Perron–Frobenius theorem for irreducible nonnegative matrices is 

lim sup 

> m→∞

[( Bm)uv ]1/m = ρ(B) = ρ⋆. (179) Every length-m walk from u to v inside S⋆ can be concatenated with the fixed entrance and exit paths. Consequently, 

Wa+m+b ≥ (Bm)uv . (180) Taking (a + m + b)-th roots and then the limit superior gives 

lim sup 

> ℓ→∞

W 1/ℓ ℓ ≥ lim sup 

> m→∞

[( Bm)uv ]1/(a+m+b)

= ρ⋆. (181) Together with Eq. (176), this proves Eq. (172). 

Primitive dominant-core case. The limit superior in Theorem 3 is necessary in general because a strongly connected component may be periodic. Under a primitive dominant-core assumption, the pipeline count admits a sharper asymptotic form. 

Corollary 2 (Primitive-core asymptotic pipeline count) . Suppose that Dio contains a unique cyclic strongly connected component 

S⋆, and that 

B := AS⋆ (182) is primitive. Let 

ρ := ρ(B). (183) Then there exists a constant κ > 0 such that 

Wℓ = κρ ℓ 1 + o(1)  as ℓ → ∞ . (184) Equivalently, 

lim 

> ℓ→∞

Wℓ

ρℓ = κ. (185) 

Proof. Because S⋆ is the unique cyclic strongly connected component, the subgraph induced by Vio \S⋆ is acyclic. Consequently, there are only finitely many entrance paths from s to S⋆ and finitely many exit paths from S⋆ to z.Let Pin be the set of directed paths 

p = ( p0, . . . , p a)

such that p0 = s, pa ∈ S⋆, and pi /∈ S⋆ for every i < a . Thus, p first enters S⋆ at its final vertex. Let 

u(p) := pa ∈ S⋆, (186) and let |p| := a denote its number of edges. Similarly, let Pout be the set of directed paths 

q = ( q0, . . . , q b)

such that q0 ∈ S⋆, qb = z, and qi /∈ S⋆ for every i > 0. Thus, after its initial vertex, q lies entirely outside S⋆. Let 

v(q) := q0 ∈ S⋆, (187) and let |q| := b denote its number of edges. Since all walks that avoid S⋆ lie in an acyclic subgraph, their lengths are uniformly bounded. Hence, for all sufficiently large 

ℓ, every length-ℓ input-to-output walk passes through S⋆. Moreover, because the condensation graph is acyclic, once such a walk leaves S⋆, it cannot return. Therefore, every sufficiently long walk has a unique decomposition into an entrance path, an internal walk in S⋆, and an exit path. It follows that 

Wℓ = X

> p∈P in

X

> q∈P out



B ℓ−| p|−| q|

> u(p),v (q)

(188) for all sufficiently large ℓ, with terms having negative exponents omitted. Since B is primitive, the Perron–Frobenius theorem gives strictly positive right and left eigenvectors r and h satisfying 

Br = ρr, h⊤B = ρh⊤, h⊤r = 1 , (189) and 

Bm = ρmrh ⊤ + o(ρm). (190) Hence, for every fixed pair u, v ∈ S⋆,

(Bm)uv = ρmruhv + o(ρm). (191) Substituting Eq. (191) into Eq. (188), and using the finiteness of Pin and Pout , gives 

Wℓ = κρ ℓ + o(ρℓ), (192) where 

κ := X

> p∈P in

X

> q∈P out

ρ−(|p|+|q|)ru(p)hv(q). (193) The entrance and exit path sets are nonempty, ρ > 0, and the Perron vectors are strictly positive. Therefore, κ > 0, proving Eq. (184). Interpretation. Lemma 1 gives an exact matrix characterization of the bounded structural pipeline space. In a fixed-size DAG, the adjacency matrix is nilpotent, so the maximum representable pipeline depth is intrinsically finite. As the permitted length bound Lmax increases, a directed cycle removes this intrinsic finite-depth restriction and permits repeated execution of an operator motif without duplicating its nodes. For every fixed Lmax , however, the sampled pipeline space remains finite. Theorem 3 further distinguishes two forms of cyclic expressivity. When 

ρ⋆ = 1 , (194) the graph can represent unbounded iterative behavior, but the number of length-ℓ pipelines need not grow exponentially. When 

ρ⋆ > 1, (195) the fixed-length structural pipeline count has exponential limsup growth rate ρ⋆. Under the primitive-core assumptions of Corollary 2, this strengthens to the ordinary asymptotic relation 

Wℓ ∼ κρ ℓ.

Thus, the spectral radius quantifies the structural branching capacity of the directed-graph algorithm space. These results concern representational expressivity rather than optimization performance. A larger pipeline space does not by itself guarantee that a finite-budget search procedure will discover a better algorithm; the empirical benefit of the directed-graph representation is evaluated separately through the structural ablation study. 

D.4 Transition-Credit Stability and Tracking 

This section analyzes the boundedness, expected-value convergence, and tracking properties of the transition-credit update. Since a transition is updated only when it is activated, the analysis is conducted on the transition-specific activation clock rather than on the global generation index. 

Transition-specific activation clock. Fix an implementation transition e. Let 

 tn(e), j n(e), i n(e)

denote, respectively, the generation index, candidate index, and pipeline-position index of the n-th activation of e. Activations are ordered according to the nested update order over (t, j, i ) in Algorithm 1. The terminal credit observed at the n-th activation is 

Yn(e) := eRtn(e),j n(e), (196) and Qn(e) denotes the credit of transition e immediately after this activation has been processed. If the same transition is activated multiple times within one sampled candidate, these occurrences have different position indices in(e) and are treated as separate activation events, although they receive the same candidate-level terminal credit 

eRtn(e),j n(e).Let {F en}n≥0 denote the corresponding activation-clock filtration, where Fen contains all random variables revealed up to and including the n-th activation of e.The asymptotic statements below are understood for transitions that are activated infinitely often. If a transition is activated only finitely many times, the finite-n identities and bounds apply up to its final activation, after which its stored credit remains unchanged unless the transition is removed. We consider the constant-rate update used in the implementation, 

αn(e) ≡ α, 0 < α < 1, (197) for which 

Qn(e) = (1 − α)Qn−1(e) + αY n(e). (198) For notational convenience, define 

β := 1 − α. (199) 

Proposition 2 (Exponential representation and boundedness) . For every integer n ≥ 1,

Qn(e) = βnQ0(e) + α

> n

X

> k=1

βn−kYk(e). (200) The coefficients in Eq. (200) are nonnegative and sum to one: 

βn + α

> n

X

> k=1

βn−k = 1 . (201) Consequently, if there exists Rmax > 0 such that 

|Q0(e)| ≤ Rmax , |Yn(e)| ≤ Rmax for all n, (202) then 

|Qn(e)| ≤ Rmax for all n. (203) Proof. We prove Eq. (200) by induction. For n = 1 ,

Q1(e) = βQ 0(e) + αY 1(e), (204) which agrees with Eq. (200). Suppose that the expansion holds for some n ≥ 1. Then 

Qn+1 (e) = βQ n(e) + αY n+1 (e)= β

"

βnQ0(e) + α

> n

X

> k=1

βn−kYk(e)

#

+ αY n+1 (e)= βn+1 Q0(e) + α

> n+1

X

> k=1

βn+1 −kYk(e). (205) Thus, the expansion holds for every n ≥ 1.Moreover, 

βn + α

> n

X

> k=1

βn−k = βn + α

> n−1

X

> j=0

βj

= βn + α 1 − βn

1 − β

= βn + 1 − βn = 1 , (206) where 1 − β = α. Therefore, Qn(e) is a convex combination of Q0(e) and Y1(e), . . . , Y n(e). Under Eq. (202), this convex combination lies in [−Rmax , R max ], proving Eq. (203). 

Stationary transition value. We first analyze a locally stationary phase in which the conditional expected terminal credit associated with transition e is constant. Assume 

Yn(e) = μe + ξn(e), (207) where μe is a deterministic constant and {ξn(e)}n≥1 is adapted to {F en}n≥0 and satisfies the martingale-difference condition 

Eξn(e) | F en−1

 = 0 . (208) For the exact mean-squared-error formula below, additionally assume 

Eξn(e)2 | F en−1

 = σ2 

> e

. (209) 

Theorem 4 (Expected-value convergence and stationary estimation error) . Suppose that Q0(e) is deterministic and Eqs. (207)– (209) hold. Then 

E[Qn(e)] − μe = βn Q0(e) − μe

. (210) Consequently, 

lim  

> n→∞

E[Qn(e)] = μe, (211) and the expected bias decreases geometrically at rate β = 1 − α.Moreover, 

E

h Qn(e) − μe

2i

= β2n Q0(e) − μe

2 + ασ 2

> e

2 − α

 1 − β2n. (212) Therefore, 

lim  

> n→∞

E

h Qn(e) − μe

2i

= ασ 2

> e

2 − α . (213) 

Proof. Substituting Eq. (207) into Eq. (200) yields 

Qn(e) = βnQ0(e) + α

> n

X

> k=1

βn−kμe + ξk(e)

= μe + βn Q0(e) − μe

 + α

> n

X

> k=1

βn−kξk(e), (214) where Eq. (201) was used. Taking expectations and using Eq. (208) proves Eq. (210). The martingale-difference property implies that distinct noise terms are orthogonal. Indeed, for k < ℓ ,

E[ξk(e)ξℓ(e)] = Eξk(e)Eξℓ(e) | F eℓ−1

 = 0 , (215) because ξk(e) is Feℓ−1-measurable. Squaring Eq. (214), taking expectations, and applying Eqs. (209) and (215) give 

E

h Qn(e) − μe

2i

= β2n Q0(e) − μe

2 + α2σ2

> en

X

> k=1

β2( n−k). (216) The geometric sum satisfies 

α2

> n

X

> k=1

β2( n−k) = α2 1 − β2n

1 − β2

= α

2 − α

 1 − β2n, (217) because 

1 − β2 = 1 − (1 − α)2 = α(2 − α). (218) This proves Eqs. (212) and (213). 

Tracking a time-varying transition value. The graph, pipeline policy, and implementation pools evolve during training, so the conditional expected credit associated with a transition may vary along its activation clock. We therefore consider 

Yn(e) = μn(e) + ξn(e), (219) where the deterministic target sequence {μn(e)}n≥0 satisfies 

|μn(e) − μn−1(e)| ≤ ∆e for every n ≥ 1. (220) 

Theorem 5 (Tracking bound for a drifting transition value) . Suppose that Q0(e) is deterministic and the deterministic target sequence {μn(e)}n≥0 satisfies Eq. (220). Assume that {ξn(e)}n≥1 is adapted to {F en}n≥0 and satisfies 

Eξn(e) | F en−1

 = 0 , Eξn(e)2 | F en−1

 ≤ σ2 

> e

. (221) Then 

|E[Qn(e)] − μn(e)| ≤ βn|Q0(e) − μ0(e)| + β∆e

α

 1 − βn. (222) Consequently, 

lim sup 

> n→∞

|E[Qn(e)] − μn(e)| ≤ (1 − α)∆ e

α . (223) The mean-squared tracking error further satisfies 

E

h Qn(e) − μn(e)2i

≤



βn|Q0(e) − μ0(e)| + β∆e

α

 1 − βn2

+ ασ 2

> e

2 − α

 1 − β2n. (224) Therefore, 

lim sup 

> n→∞

E

h Qn(e) − μn(e)2i

≤

 (1 − α)∆ e

α

2

+ ασ 2

> e

2 − α . (225) 

Proof. Define 

En(e) := Qn(e) − μn(e). (226) Using Eqs. (198) and (219), 

En(e) = βQ n−1(e) + αμn(e) + ξn(e) − μn(e)= βE n−1(e) + βμn−1(e) − μn(e) + αξ n(e). (227) Iterating the recursion gives 

En(e) = βnE0(e) + β

> n

X

> k=1

βn−kμk−1(e) − μk(e)

+ α

> n

X

> k=1

βn−kξk(e). (228) Taking expectations, applying the triangle inequality, and using Eq. (220) yield 

|E[En(e)] | ≤ βn|E0(e)| + β∆enX

> k=1

βn−k

= βn|E0(e)| + β∆e

α

 1 − βn, (229) which proves Eqs. (222) and (223). In Eq. (228), the first two terms are deterministic under the assumed target sequence, while the final term has zero mean. The martingale-difference property implies orthogonality of the noise terms. Therefore, 

E[En(e)2] ≤

"

βn|E0(e)| + β∆enX

> k=1

βn−k

#2

+ α2σ2

> en

X

> k=1

β2( n−k). (230) Evaluating the two geometric sums gives Eq. (224). Taking the limit superior proves Eq. (225). 

Remark 2 (Scope of the stochastic-noise model) . The martingale-difference assumptions in Theorems 4 and 5 are idealized activation-clock conditions. In the implemented algorithm, multiple transitions, and potentially repeated activations of the same transition, may receive the same standardized terminal credit from one candidate. Generation-level standardization may also induce dependence across observed credits. Proposition 2 and Corollary 3 are deterministic and do not require the martingale-difference model. The expected-value and mean-squared-error results in Theorems 4 and 5 apply under their stated stochastic assumptions; under correlated noise, additional covariance terms enter the mean-squared-error expressions. 

Boundedness of aggregated credits. Transition credits are subsequently aggregated into implementation-, operator-, and operator-edge-level statistics. The following result shows that these aggregations preserve uniform boundedness. 

Corollary 3 (Boundedness of aggregated contextual credits) . Suppose that 

|Qt(e)| ≤ Rmax (231) for every activated transition e. Recall the implementation-level aggregation used in Algorithm 2: 

Qt(c) = 

P 

> e∈T t(c)

nt(e)Qt(e)

P 

> e∈T t(c)

nt(e) + ϵ , nt(e) ≥ 0, ϵ > 0. (232) Then 

|Qt(c)| ≤ Rmax . (233) The same bound holds for the operator-level average and for the operator-edge aggregation used in Algorithm 2. 

Proof. Let 

St(c) := X

> e∈T t(c)

nt(e), (234) and define 

we := nt(e)

St(c) + ϵ , w0 := ϵSt(c) + ϵ . (235) These weights are nonnegative and satisfy 

w0 + X

> e∈T t(c)

we = 1 . (236) Equation (232) can therefore be written as 

Qt(c) = w0 · 0 + X

> e∈T t(c)

weQt(e). (237) Thus, Qt(c) is a convex combination of zero and uniformly bounded transition credits, proving Eq. (233). The operator-level credit is an arithmetic average of bounded implementation credits, while the operator-edge credit has the same nonnegative weighted-average form. Therefore, both inherit the same uniform bound. Interpretation. Proposition 2 shows that the transition-credit update is an exponential moving average of observed terminal credits and cannot diverge under bounded observations. Theorem 4 shows that, under a stationary transition value and the stated martingale-difference model, the expected estimate converges geometrically to the conditional expected credit. With a constant learning rate, however, the estimator retains the nonzero limiting mean-squared error in Eq. (213); the theorem therefore does not imply mean-square or almost-sure convergence of the sample path. A smaller α reduces the stationary noise term but slows the decay of the initial bias. For a time-varying transition value, Theorem 5 makes the adaptation–variance tradeoff explicit. Increasing α reduces the drift-induced asymptotic bias term (1 − α)∆ e/α , but increases the stationary noise term ασ 2 

> e

/(2 − α). Finally, Corollary 3 shows that the implementation-, operator-, and operator-edge-level credits used for dual-level evolution inherit the same uniform boundedness as their constituent transition-credit estimates. 

## E Experimental Setup 

E.1 Implementation and Reproducibility Details 

The pipeline policy is implemented using Qwen2.5-0.5B-Instruct. An external LLM backbone is used for directed-graph initialization, operator-implementation code generation, and graph-topology evolution proposals. Unless otherwise stated, the external backbone is either DeepSeek-V4-Flash or GPT-5.6 Sol, as specified in the corresponding experimental group. For each problem domain, the algorithm-generation process is limited to at most 500 external LLM calls. This budget includes calls for directed-graph initialization, scheduled graph-edit proposals, and code generation for Add and Replace actions. Local pipeline-policy inference, rule-based Delete actions, candidate execution, and deterministic validation are not counted toward this budget. Training terminates when either the external-call budget is exhausted or the configured maximum number of training generations is reached. Every Ival = 5 generations, deterministic constrained beam search with beam width 4 is used to decode candidate pipelines from the current policy. Each decoded pipeline is instantiated by selecting the highest-credit implementation for every operator and is then evaluated on the complete validation split. The checkpoint achieving the highest mean raw validation reward is retained. Validation does not update the pipeline policy, transition-credit table, implementation pools, or graph topology. After training, the best validation checkpoint is restored and evaluated once on the held-out test split for each independent training run. Unless otherwise stated, each candidate algorithm is executed under a wall-clock budget of 90 seconds per instance. Compila-tion failures, runtime errors, infeasible solutions, and timeouts receive the failure reward. The main in-distribution combinatorial-optimization results are aggregated over 10 independent training runs with different random seeds. Experiments using a different number of runs or evaluation budgets are explicitly identified in their corresponding sections. The anonymized repository submitted with the supplementary material includes the method implementation, operator repos-itory, prompt templates, experiment configurations, offline smoke tests, and single-run entry points.                               

> Hyperparameter Value
> Base policy model Qwen2.5-0.5B-Instruct Pipelines per generation N4Shared instances per generation k3Maximum pipeline length 10 Credit update rate α0.1 Maximum implementation-pool size M10 Sampling temperature 0.7 Optimizer AdamW Learning rate 1×10 −4
> Maximum gradient norm 1.0 Reward normalization constant ϵ1×10 −8
> Failure reward Rfail -5.0 Number of training generations 50 Dataset split seed 0External LLM call budget 500 Validation interval Ival 5 generations Validation beam width 4Per-instance wall-clock budget 90 s

Table 6: Training and evolution hyperparameters used in DGA2D. E.2 Experimental Datasets 

We draw instances from the available benchmark groups in 12 domains, subject to a fixed size filter determined before training. RCPSP and SALBP-1 retain their original single-group evaluation scope. The retained collection contains 2,873 unique instances. Table 7 reports the included benchmark groups, split sizes aggregated by domain, and problem-size ranges. Auxiliary index, metadata, solution, and best-known-solution (BKS) files are not counted as instances. Alternative JSON representations of the same source instance are likewise excluded. The 34 instances in DIMACS_subset also occur in DIMACS_all and are therefore counted only once. To keep the common per-instance evaluation budget feasible, we exclude the CVRP group AGS , the FSSP group tai500_20 ,and the SALBP-1 dataset very large data set_n=1000 . We also exclude the Max-Cut instances G50 , G55 , G60 , G65 ,

G70 , G72 , and G81 , together with the MIS instances C4000.5 , MANN_a81 , and keller6 . For RCPSP, we retain only PSPLIB 

j30 ; for SALBP-1, we retain only the Scholl small set with 20 tasks. Every instance belonging to the listed retained groups is included. Dataset splitting is performed independently within each benchmark group. Within a group, instances are ordered by a stable identifier, shuffled using split seed 0, and deterministically divided into disjoint training, validation, and test subsets according to a 60/20/20 ratio. The resulting subsets are then aggregated by domain for the reported domain-level experiments. Consequently, the split sizes in Table 7 are obtained by applying the integer-rounding rule within each benchmark group before aggregation; they are not computed by applying the split ratio directly to the total number of instances in a domain. Before each training run, k instances are sampled without replacement from the active training split and reused across all generations in that run. All candidate pipelines are therefore evaluated on the same shared instance batch, which makes their scores directly comparable across generations. Validation is used only for checkpoint selection and does not update the pipeline policy, transition-credit table, graph topology, or implementation pools. The test split remains held out until the best validation checkpoint has been restored. For reward normalization and gap reporting, each evaluator provides an associated known optimum, BKS, or designated reference value. Because no universally accepted BKS is available for the 3D-CLP suite, we use the reference values produced by the Extreme-Point–First-Fit packing baseline. These values serve as comparison references rather than proven bounds; therefore, a negative reported gap is possible when a method outperforms the reference baseline.                                                                                        

> Problem Included benchmark groups Train Validation Test Problem size
> 3D-BPP MPV n10 ,n20 ,n30 ,n40 ,n50 ,n60 ,n100 ,n150 ,
> n200
> 432 144 144 10–200 items 3D-CLP Bischoff–Ratcliff BR1 –BR15 90 30 30 95–476 items CVRP A, B, CMT, E, F, Golden, M, P, tai 81 25 36 12–483 customers FJSP Barnes, Behnke, Brandimarte, Dauzere, Fattahi, Hurink, Kacem 199 66 71 2–100 jobs, 2–60 machines FSSP Taillard 20-, 50-, and 100-job groups; 200x10 ,
> 200x20
> 66 22 22 20x5 –200x20
> JSP abz, ft, la, orb, swv, ta, yn 96 33 33 6x5 –100x20
> Max-Cut G-set instances with at most 2,000 vertices 24 89800–2,000 vertices MIS BHOSLIB and DIMACS, deduplicated, with at most 2,000 vertices 67 22 23 28–2,000 vertices OSSP Taillard 4x4 ,5x5 ,7x7 ,10x10 ,15x15 ,20x20 36 12 12 4x4 –20x20
> RCPSP PSPLIB j30 288 96 96 30 real activities SALBP-1 Scholl small set 315 105 105 20 tasks TSP TSPLIB collection 21 7751–299 cities

Table 7: Benchmark groups retained for the experiments. The training, validation, and test splits contain 1,715, 570, and 588 instances, respectively, for a total of 2,873 unique instances across all 12 domains. 

## F Extended Experimental Results 

F.1 Full Results Across All Benchmark Domains 

To complement the four representative benchmarks reported in the main text, we further evaluate DGA 2D on eight additional combinatorial optimization problems: CVRP, JSP, OSSP, SALBP-1, 3D-BPP, FSSP, RCPSP, and Max-Cut. Together with FJSP, 3D-CLP, MIS, and TSP, the complete evaluation covers twelve domains spanning routing, scheduling, packing, project planning, graph optimization, and assembly-line balancing. All LLM-based AHD methods are evaluated using the same prompt template and evaluation budget under both DeepSeek-V4-Flash and GPT-5.6 Sol. Across the eight additional benchmarks, DGA 2D consistently achieves the best performance under both LLM backbones. With DeepSeek-V4-Flash, it obtains an average normalized gap of 2.98% , compared with 10 .66% for the strongest baseline Table 8: Performance of DGA 2D compared with baselines across combinatorial optimization benchmarks, grouped by LLM backbones. Obj.: Objective value ( ↓ lower is better, ↑ higher is better). Gap (%): normalized gap to the corresponding reference solution. Results are reported as mean ± standard deviation (10 trials). Bold signifies the best overall result and the best LLM-based result under each backbone. Gray indicates the best performance under the same LLM API.                                                                                                                                                                                                                                           

> Method CVRP JSP OSSP SALBP-1
> Obj. ( 10 3)↓Gap (%) ±std ↓Obj. ( 10 4)↓Gap (%) ±std ↓Obj. ( 10 3)↓Gap (%) ±std ↓Obj. ( 10 2)↓Gap (%) ±std ↓
> Reference 3.388 –3.451 –1.210 –6.180 –
> LLM: DeepSeek-V4-Flash
> EoH 4.262 27.21 ±1.36 4.350 27.02 ±4.12 1.216 0.49 ±0.08 6.980 9.10 ±0.31 MCTS-AHD 4.252 25.18 ±5.84 4.511 30.71 ±5.02 1.217 0.54 ±0.11 7.060 10.23 ±0.64 MEoH 4.340 28.43 ±1.27 4.318 25.13 ±2.21 1.213 0.24 ±0.04 6.695 8.34 ±0.18 ReEvo 4.203 24.54 ±4.30 3.797 10.01 ±2.76 1.230 1.69 ±0.43 7.040 9.67 ±0.52
> DGA 2D(Ours) 3.640 7.42 ±1.93 3.555 3.02 ±0.88 1.213 0.21 ±0.09 6.343 2.64 ±0.46
> LLM: GPT-5.6 Sol
> EoH 3.600 6.75 ±1.12 4.292 24.36 ±1.48 1.213 0.25 ±0.04 7.010 9.07 ±0.23 MCTS-AHD 4.410 30.84 ±5.21 4.596 33.18 ±3.74 1.213 0.27 ±0.07 7.030 9.67 ±0.58 MEoH 4.233 24.92 ±1.06 4.225 22.41 ±1.15 1.212 0.19 ±0.03 6.682 8.12 ±0.16 ReEvo 4.432 31.47 ±5.73 3.749 8.64 ±3.12 1.216 0.50 ±0.14 6.757 9.33 ±0.49
> DGA 2D(Ours) 3.587 5.86 ±1.41 3.532 2.34 ±0.71 1.211 0.16 ±0.09 6.317 2.21 ±0.38
> Method 3D-BPP FSSP RCPSP Max-Cut
> Obj. ( 10 3)↓Gap (%) ±std ↓Obj. ( 10 5)↓Gap (%) ±std ↓Obj. ( 10 3)↓Gap (%) ±std ↓Obj. ( 10 5)↑Gap (%) ±std ↓
> Reference 1.917 –6.353 –6.167 –2.901 –
> LLM: DeepSeek-V4-Flash
> EoH 2.007 7.90 ±1.40 7.298 19.20 ±3.05 6.424 5.20 ±3.70 2.745 8.67 ±0.48 MCTS-AHD 2.106 12.27 ±5.78 7.529 17.96 ±5.72 6.539 6.82 ±4.84 2.804 4.84 ±0.93 MEoH 2.020 8.73 ±1.37 7.172 14.22 ±5.75 6.478 6.79 ±4.24 2.766 7.27 ±1.50 ReEvo 1.998 6.89 ±0.34 7.542 18.16 ±5.94 6.334 3.38 ±1.72 2.720 10.92 ±3.86
> DGA 2D(Ours) 1.922 0.66 ±0.14 6.841 7.96 ±1.77 6.169 0.03 ±0.02 2.859 1.90 ±0.41
> LLM: GPT-5.6 Sol
> EoH 2.048 9.68 ±3.05 7.306 16.53 ±1.00 6.500 6.59 ±3.45 2.781 6.01 ±0.49 MCTS-AHD 2.080 11.31 ±3.90 6.897 6.80 ±6.48 6.890 12.98 ±7.26 2.778 6.26 ±0.76 MEoH 2.005 7.15 ±0.75 7.307 24.31 ±13.84 6.429 5.16 ±2.11 2.782 6.02 ±0.92 ReEvo 2.016 7.75 ±0.75 6.742 21.40 ±21.11 6.486 6.86 ±1.52 2.758 7.70 ±1.13
> DGA 2D(Ours) 1.931 0.60 ±0.21 6.624 4.25 ±2.67 6.180 0.22 ±0.31 2.872 1.47 ±0.23

on average. With GPT-5.6 Sol, the average normalized gap further decreases to 2.14% , compared with 9.77% for the strongest baseline, corresponding to an absolute improvement of 7.63 percentage points. The improvements are consistent across problems with substantially different representations, constraints, and objective structures. In particular, DGA 2D remains effective on both relatively challenging domains, where competing methods exhibit large optimality gaps, and domains where most methods already approach the reference solutions. The comparatively small variations across repeated runs further indicate that the observed improvements are stable rather than being driven by isolated favorable outcomes. Overall, DGA 2D ranks first in all 16 combinations of the eight additional benchmarks and two LLM backbones. Combined with the results on the four benchmarks in the main text, these findings demonstrate that the effectiveness of DGA 2D generalizes across a broad range of combinatorial optimization domains. Its consistent performance under both DeepSeek-V4-Flash and GPT-5.6 Sol also suggests that the proposed framework is not tied to a specific backbone model or problem representation, but provides a robust and generalizable approach to LLM-based automated heuristic design. 

F.2 Sensitivity Analysis of LLM 

To assess the sensitivity of automated heuristic design to the choice of language-model backbone, Table 9 compares four methods on FJSP across eight models from three LLM families. The reported metric is the optimality gap, for which lower values indicate better solutions. DGA 2D achieves the lowest gap for every backbone. Relative to MCTS-AHD, the strongest baseline in each column, it reduces the gap by 55 .2% –60 .1% . Averaged over all eight backbones, its gap is 7.65% , compared with 18 .34% for MCTS-AHD. Table 9: FJSP optimality gap (%) under different LLM backbones. Lower values indicate better performance, and the best result in each column is shown in bold.                                             

> Method DeepSeek Claude GPT V4-F V4-P 4.6 Opus 4.7 Opus 4.8 Opus 5.4-Pro 5.5-Pro 5.6-Sol
> EoH 28.76 26.80 25.90 24.80 23.70 25.40 24.10 22.90 MEoH 26.37 26.63 24.60 23.50 22.40 24.10 22.60 21.50 MCTS-AHD 22.20 20.10 19.30 17.90 16.50 18.60 16.80 15.30
> DGA 2D(Ours) 9.94 8.54 8.00 7.30 6.70 7.70 6.90 6.10
> Note: V4-F and V4-P denote DeepSeek-V4-Flash and DeepSeek-V4-Pro, respectively. 5.4-Pro, 5.5-Pro, and 5.6-Sol denote GPT-5.4-Pro, GPT-5.5-Pro, and GPT-5.6 Sol, respectively.

Comparing DeepSeek-V4-Flash with GPT-5.6 Sol, all four methods obtain lower gaps; for DGA 2D, the gap decreases from 

9.94% to 6.10% . These results suggest that more capable backbones generally improve solution quality, while the advantage of DGA 2D remains consistent across LLM families rather than depending on a single model. 

F.3 Sensitivity to Evaluation Wall-Clock Budget 

Experimental Setup. We evaluate the sensitivity of the generated heuristics to wall-clock budgets of 0.5, 1, 5, 10 , 30 , 60 , 90 ,

180 , and 300 seconds on 3D-CLP and FJSP. Each budget setting is evaluated through an independent run from scratch using the same generated heuristic and benchmark instances; a longer-budget run does not continue from a shorter-budget run. All evaluated methods terminate within 60 seconds. Therefore, budgets greater than 60 seconds are non-binding and do not provide additional computation after the corresponding heuristic has terminated. DGA 2D employs stochastic solution construction, and its results may therefore vary across independent runs even when the nominal wall-clock budget exceeds its actual termination time. In contrast, the heuristics generated by EoH, MCTS-AHD, MEoH, and ReEvo are deterministic in this evaluation. Given the same generated code and problem instance, they return the same solution, and their results consequently remain unchanged once execution is completed. Table 10: Sensitivity of the normalized gap (%) to the evaluation wall-clock budget on 3D-CLP and FJSP. Lower values are better, and the best result under each budget is shown in bold. 

Model Evaluation Wall-Clock Budget (s) 0.5 1 5 10 30 60 90 180 300 

Benchmark: 3D-CLP 

EoH 2.611 1.336 0.699 0.699 0.699 0.699 0.699 0.699 0.699 MCTS-AHD 2.611 1.336 0.699 0.699 0.699 0.699 0.699 0.699 0.699 MEoH 2.639 1.365 0.727 0.727 0.727 0.727 0.727 0.727 0.727 ReEvo 3.051 1.740 1.103 1.103 1.103 1.103 1.103 1.103 1.103 

DGA 2D (Ours) 0.393 0.228 0.228 0.166 0.109 -0.080 -0.273 -0.114 -0.368 

Benchmark: FJSP 

EoH – 42.781 28.409 28.409 28.409 28.409 28.409 28.409 28.409 MCTS-AHD – 38.245 26.144 26.144 26.144 26.144 26.144 26.144 26.144 MEoH – 40.113 27.313 27.313 27.313 27.313 27.313 27.313 27.313 ReEvo – 48.719 28.750 28.750 28.750 28.750 28.750 28.750 28.750 

DGA 2D (Ours) 18.177 15.974 13.312 12.178 11.425 7.791 7.029 7.320 6.369    

> Note: The symbol −indicates that no valid solution is generated within the specified wall-clock budget.

Sensitivity to the Evaluation Wall-Clock Budget. Table 10 examines the effect of the evaluation wall-clock budget on the normalized gap achieved by different methods on 3D-CLP and FJSP. DGA 2D consistently achieves the best performance across all evaluated budget settings. Under restrictive budgets, it produces valid and high-quality solutions substantially faster than the comparison methods. On FJSP, DGA 2D generates valid solutions within both 0.5 and 1 seconds, whereas none of the baselines produces a valid solution under these budgets. As the available evaluation budget increases, the performance of DGA 2D generally improves up to 60 seconds, by which point all evaluated methods have completed their computation. Therefore, the results reported at 90, 180, and 300 seconds do not represent additional search beyond the 60-second setting. The variations in the results of DGA 2D after 60 seconds arise from stochastic differences across independent runs, whereas the deterministic constructive baselines return identical results once their executions have terminated. Overall, these results demonstrate that DGA 2D maintains a clear advantage under both restrictive and sufficiently large evaluation budgets. It can rapidly generate valid and competitive solutions, while completing its effective computation within 60 seconds on both benchmarks. 

F.4 Out-of-Distribution Generalization 

We evaluate the out-of-distribution generalization ability of DGA 2D on the Hurink FJSP benchmark using three subsets, namely 

edata , rdata , and vdata . Each subset contains 66 instances and is characterized by a different flexibility ratio. DGA 2D is trained independently on each subset and subsequently evaluated on all three subsets, resulting in nine train–test configurations. The diagonal entries represent in-distribution performance, whereas the off-diagonal entries measure cross-domain generalization under changes in instance flexibility. Each configuration is independently repeated 10 times, and the results are reported as the mean ± standard deviation. Table 11: In-distribution and cross-domain performance of DGA 2D on the Hurink benchmark. Results are reported as the mean 

± standard deviation over 10 independent runs, with lower values indicating better performance. 

Training Domain Test-E Test-R Test-V 

Train-E 18.97 ±0.19 25.88 ±0.15 25.12 ±0.19 Train-R 15.92 ±0.40 11.17 ±0.20 7.45 ±0.35 Train-V 18.59 ±0.36 20.00 ±0.69 19.04 ±0.26 The reported metric is the normalized optimality gap (%), for which lower values indicate better performance. Training on 

rdata yields the best overall cross-domain performance, achieving the lowest average gap across the three test domains. The solver trained on edata exhibits a pronounced performance degradation on Test-R and Test-V, whereas the vdata -trained solver shows relatively stable but less competitive performance across the three domains. These results suggest that cross-domain generalization is sensitive to the flexibility distribution of the source domain, with rdata providing the most transferable training distribution among the three subsets. The strong transfer performance further suggests that DGA 2D can discover scheduling strategies that remain effective under changes in instance flexibility, rather than merely fitting distribution-specific statistics. 

F.5 Performance at Different Convergence Stages 

To further examine the search efficiency and long-horizon evolutionary behavior of DGA 2D, we analyze its convergence from two complementary perspectives. Figure 4 reports the evolution of the best-so-far optimality gap on CVRP across the early, middle, and late stages, while Figure 5 compares DGA 2D with representative AHD baselines on FJSP under two computational budgets: output tokens and the number of LLM calls. 1 10 20 30 40 50   

> Epoch
> 0
> 1
> 2
> 3
> 4
> 5
> Best-so-far GAP (%)
> Early Middle Late

CVRP Convergence Across Evolutionary Stages  

> E1: 5.20%
> E2: 3.50% E17: 2.40%
> E18: 1.93%
> E23: 1.85%
> E27: 1.42%
> E37: 0.80%
> E38: 0.42%
> Best-so-far GAP

Figure 4: Stage-wise convergence of DGA 2D on CVRP, showing the best-so-far optimality gap across the early, middle, and late evolutionary stages. As shown in Figure 4, DGA 2D achieves a rapid reduction in the optimality gap during the early stage, decreasing from 

5.20% at epoch 1 to 1.93% at epoch 18. This sharp initial improvement suggests that the search can quickly identify promising algorithmic structures. During the middle stage, the gap is progressively reduced from 1.93% to 0.42% , with intermediate improvements to 1.85% , 1.42% , and 0.80% at epochs 23, 27, and 37, respectively, followed by a final decrease to 0.42% 

at epoch 38. The gap then remains unchanged throughout the late stage, indicating that the search has converged. Overall, the trajectory reflects rapid early-stage exploration, sustained middle-stage refinement, and stable late-stage convergence. To 

Figure 5: Convergence of the best-so-far optimality gap on FJSP with respect to output tokens (left) and the number of LLM calls (right). Lower values indicate better solution quality. DGA 2D exhibits faster convergence and achieves a lower final gap than EoH, MCTS-AHD, MEoH, and ReEvo under both budget measures. complement this stage-wise analysis, Figure 5 presents the full convergence trajectories of different methods with respect to output tokens and LLM calls. DGA 2D exhibits a sharp initial decrease under both budget measures and reaches a competitive gap substantially earlier than the baselines. Although ReEvo continues to improve with additional evaluations, it requires a considerably larger budget to approach a similar performance level and still converges to a higher final gap. In contrast, EoH, MCTS-AHD, and MEoH enter relatively high plateaus at earlier stages. These results indicate that DGA 2D combines efficient early-stage discovery with sustained long-horizon refinement, yielding both faster convergence and better final solution quality. 

F.6 Resource Consumption Analysis 

Table 12 summarizes the wall-clock time and total token usage required to design heuristics for TSP, FJSP, MIS, and 3D-CLP. Among the evaluated baselines, MCTS-AHD is the only method that employs an explicit structured search mechanism broadly comparable to that of DGA 2D, making it the most relevant architectural reference for resource comparison. Table 12: Analysis of computational resources. Wall-clock time in hours and total token usage in millions for heuristic design across four optimization problems.                                                   

> Method Metric TSP FJSP MIS 3D-CLP
> EoH Time (h) 0.35 0.65 0.63 0.32 Tokens (M) 1.04 2.76 1.16 0.82 MCTS-AHD Time (h) 2.08 3.88 4.65 3.24 Tokens (M) 1.26 1.13 1.10 1.20 ReEvo Time (h) 0.92 0.97 1.01 0.7 Tokens (M) 2.10 3.33 1.35 1.36 MEoH Time (h) 0.27 0.67 0.39 0.49 Tokens (M) 1.03 2.14 1.25 1.03
> DGA2D (Ours) Time (h) 1.43 2.90 3.38 2.35 Tokens (M) 0.56 1.74 0.81 0.45

Compared with MCTS-AHD, DGA 2D reduces the total wall-clock time from 13 .85 to 10 .06 hours and the total token consumption from 4.69 M to 3.56 M, corresponding to reductions of approximately 27 .4% and 24 .1% , respectively. DGA 2Drequires less time on all four problems and consumes fewer tokens on TSP, MIS, and 3D-CLP, although MCTS-AHD uses fewer tokens on FJSP. The relatively low token consumption of DGA 2D can be attributed to its localized optimization strategy. Rather than repeatedly regenerating or revising an entire heuristic program, DGA 2D selectively updates individual operator implementations and local structural transitions while preserving the remaining components. In contrast, the other evolutionary AHD frameworks generally perform search at the whole-heuristic or population level, which requires broader program synthesis and revision and may consequently incur more extensive LLM interactions. Localized refinement therefore allows DGA 2D to reuse validated components and avoid unnecessary full-program regeneration. Compared with EoH, ReEvo, and MEoH, DGA 2D incurs a higher wall-clock cost because of the additional computation required for structured policy search, credit assignment, and local program maintenance. Nevertheless, it achieves the lowest aggregate token consumption among all evaluated methods. Overall, DGA 2D trades additional local computation for reduced LLM usage, providing a favorable balance between structured search effectiveness and computational resource consumption. 

## G Extension to Continuous Numerical Tasks 

To examine whether the proposed framework is applicable beyond combinatorial optimization, we further extend DGA 2D

to three classes of continuous numerical tasks: ordinary differential equation integration, feedback control, and numerical root-finding. These tasks differ substantially from the combinatorial optimization benchmarks in their solution representations, operator interfaces, objectives, and numerical stability requirements. For each benchmark problem within a task class, DGA 2D is instantiated with the corresponding task interface and operator repository, and a separate algorithm is evolved specifically for that problem. Therefore, these experiments are intended to evaluate the cross-domain applicability of the proposed automated design framework, rather than zero-shot transfer of a single evolved algorithm across different domains. Unless otherwise specified, higher scores indicate better performance, while large negative values denote unsuccessful or invalid solutions. 

G.1 Discovery of Numerical ODE Solvers 

To evaluate the capability of DGA 2D in automatically discovering numerical algorithms for continuous dynamical systems, we construct a comprehensive benchmark consisting of seven differential-equation systems with diverse numerical properties, including non-stiff, stiff, chaotic, and high-dimensional dynamics. 

Experimental Setup. We evaluate the methods on seven differential-equation systems with diverse numerical properties: Lotka–Volterra, Van der Pol, Robertson, Lorenz, a stiff linear system, a two-dimensional heat equation, and a convection– diffusion equation. The benchmark covers non-stiff, nonlinear, stiff, and chaotic dynamics, as well as high-dimensional systems obtained from the spatial discretization of partial differential equations. For each system, one instance is used for algorithm design and four instances are used for testing. The nominal step size is h = 0 .05 , with integration intervals [0 , 10] for the standard ODE systems and [0 , 2] for the PDE-derived systems. The stiff linear system has dimension 15 , while Heat2D and ConvDiff use 10 × 10 and 100 -point spatial discretizations, respectively. High-accuracy terminal states are used as reference solutions. Following the same evaluation protocol, the performance score is defined as S = − log 10 (Error) − λ log 10 (Cost) ,where Error measures the numerical integration error and Cost is the number of right-hand-side function evaluations. We set 

λ = 0 .1 for the standard ODE systems and λ = 0 .05 for the two high-dimensional PDE-derived systems. Higher scores indicate better performance, and a score of −20 is assigned when a solver diverges or fails to produce a valid solution. The average score is computed over all seven systems, including failure penalties. 

Baselines. We compare DGA 2D and EoH against five standard numerical integrators covering different solver families. Euler and fourth-order Runge–Kutta (RK4) represent classical explicit fixed-step methods. DOP853 is a high-order explicit adaptive solver, while LSODA automatically switches between non-stiff and stiff integration schemes. Radau is an implicit adaptive solver designed for stiff differential equations. Together, these baselines provide comparisons against both computationally inexpensive explicit methods and robust adaptive solvers commonly used for challenging stiff systems. 

Results Analysis. Table 13 shows that DGA 2D achieves the strongest overall performance across the seven-system benchmark. It obtains the highest score on five systems and ties with RK4 on Lotka–Volterra. On the remaining Heat2D problem, DGA 2Dachieves a score of 14 .81 , only slightly below the best score of 14 .89 obtained by EoH. Its average score reaches 13 .79 ,substantially exceeding Radau’s 7.56 , the best result among the conventional numerical solvers. In contrast, the explicit fixed-step methods frequently receive failure penalties on stiff, chaotic, and spatially discretized systems, whereas DOP853, LSODA, and Radau exhibit greater robustness. EoH displays more task-dependent performance: it performs particularly well on Heat2D and remains competitive on the stiff linear and convection–diffusion systems, but its performance decreases considerably on Van der Pol, Robertson, and Lorenz. These results demonstrate DGA 2D’s ability to discover effective integration strategies across substantially different numerical regimes. 

G.2 Discovering Feedback Control Policies 

To evaluate DGA 2D’s capability in discovering feedback-control policies, we constructed a benchmark covering dynamical systems with distinct control characteristics and stability requirements. Table 13: Performance on ordinary differential equation solving tasks. Higher values indicate better performance and −20 

indicates divergence. Failure penalties are included when computing the average. 

Method Lotka–Volterra Van der Pol Robertson Lorenz Linear Stiff Heat2D ConvDiff Average 

Euler 0.76 -10.00 -20.00 -20.00 -20.00 -20.00 -20.00 -15.61 RK4 6.98 3.21 -20.00 -20.00 -20.00 -20.00 -20.00 -12.83 DOP853 5.31 4.85 7.03 2.33 8.48 9.48 6.72 6.31 LSODA 3.95 3.40 5.69 1.33 7.78 10.33 6.01 5.50 Radau 6.26 6.59 7.25 3.38 9.91 12.18 7.36 7.56 DGA 2D 6.98 14.74 14.79 14.72 15.65 14.81 14.84 13.79 

EoH 5.49 3.21 1.38 0.75 8.30 14.89 8.64 6.09 

Experimental Setup. We considered three representative feedback-control tasks: a double integrator, a pendulum swing-up system, and a cart–pole system. These environments cover linear motion control, nonlinear stabilization, and coupled underactuated dynamics, respectively. For each system, one instance is used for algorithm design and 30 instances are used for testing. Trajectories are simulated with ∆t = 0 .02 over a horizon of T = 100 . Control inputs are bounded by [−5, 5] , [−8, 8] ,and [−20 , 20] for the double integrator, pendulum, and cart–pole systems, respectively. The test instances vary the initial states, with additional physical-parameter perturbations of up to 20% for the pendulum and cart–pole systems. To jointly evaluate tracking performance and control effort, we use the cumulative control objective 

J = X

> t

∥xt − x∗∥2 

> Q

+ 0 .01 ∥ut∥22

 , S = − log 10 (1 + J),

where xt and ut denote the system state and control input at time step t, respectively, x∗ is the target state, Q is the identity weighting matrix, and the coefficient 0.01 balances tracking accuracy against control effort. Higher scores indicate better performance, with values closer to zero corresponding to lower cumulative control cost. Invalid or divergent trajectories receive a fixed failure penalty. 

Baselines. We compared DGA 2D and EoH against a diverse collection of conventional feedback-control strategies. PID represents a widely used error-feedback controller that does not require an explicit dynamics model. Linear quadratic regulation (LQR) provides a model-based optimal controller for approximately linear systems, while sliding mode control (SMC) is designed to offer robustness against nonlinearities and disturbances. Bang–bang control represents a simple switching strategy based on maximum-magnitude control actions. We additionally included energy shaping, which stabilizes nonlinear mechanical systems by modifying their effective energy landscape. Together, these baselines cover classical linear, nonlinear, switching, and energy-based control paradigms. Table 14: Performance of feedback control policies. Higher values, i.e., values closer to zero, indicate better performance. The best result in each column is shown in bold. 

Method Double Integrator Pendulum Cart-Pole Average 

PID -2.37 -3.99 -11.59 -5.98 LQR -2.15 -2.83 -11.66 -5.55 SMC -3.16 -3.46 -4.73 -3.78 Bang–Bang -3.21 -3.58 -10.27 -5.69 Energy Shaping -3.77 -3.21 -4.71 -3.90 DGA 2D -2.13 -2.74 -4.69 -3.19 

EoH -2.25 -2.83 -4.88 -3.32 

Results Analysis. The comparative results in Table 14 demonstrate that DGA 2D achieves consistently strong performance across all three control systems. It obtains the best score on the double-integrator, pendulum, and cart–pole tasks, with scores of −2.13 , −2.74 , and −4.69 , respectively. Its average score is −3.19 , outperforming EoH at −3.32 and SMC at −3.78 , the strongest conventional baseline on average. The performance margin is relatively small on the double integrator, where DGA 2Dslightly exceeds LQR, and on cart–pole, where it remains close to energy shaping and SMC. Nevertheless, DGA 2D is the only method that ranks first on every individual task. Conventional controllers exhibit more pronounced task-specific behavior: LQR performs well on the double integrator and pendulum but degrades substantially on cart–pole, whereas SMC and energy shaping are more competitive on cart–pole but weaker on the other systems. EoH provides comparatively stable performance and ranks second overall, although it does not achieve the best result on any single task. These findings demonstrate that DGA 2D can discover competitive feedback-control policies for systems with substantially different dynamical characteristics. 

G.3 Discovery of Numerical Root-Finding Algorithms 

To evaluate DGA 2D’s capability in discovering numerical algorithms for continuous nonlinear equations, we constructed a comprehensive benchmark covering root-finding problems with diverse conditioning properties, function geometries, and input dimensions. 

Experimental Setup. The benchmark consists of four representative problem families: a Smooth Polynomial problem with a well-behaved differentiable landscape, an Ill-Conditioned problem characterized by weak or vanishing derivatives, an Oscillatory 

problem containing multiple local variations and candidate roots, and a Two-Dimensional Nonlinear System requiring a vector-valued solution. The four families are defined as 

fsmooth (x) = ( x − r)( a0 + a1x + a2x2),fill (x) = ( x − r)m(a0 + a1x + a2x2),fosc (x) = sin  ω(x − r) + α(x − r),F2D (x) = 

x21 + x2 − r1

x1 + x22 − r2



, x = ( x1, x 2)⊤.

For the smooth and ill-conditioned families, r ∈ [−5, 5] and aj ∈ [−2, 2] , with m ∈ { 2, 3, 4} for the latter. For the oscillatory family, r ∈ [−3, 3] , ω ∈ [2 , 10] , and α ∈ [0 .01 , 0.5] ; for the two-dimensional family, r1, r 2 ∈ [−1.5, 1.5] . Initial guesses are generated by applying random offsets to the known reference roots. For each family, one instance is used for algorithm design and four instances are used for testing. All methods use a residual tolerance of 10 −8. These tasks jointly evaluate convergence reliability, numerical efficiency, robustness to poor conditioning, and generalization from scalar equations to coupled nonlinear systems. We define the following efficiency-aware score: 

S = 1 − 0.05 ln(1 + Nfev ) − 0.02 ln(1 + Niter ),

where Nfev and Niter denote the numbers of function evaluations and solver iterations, respectively. Thus, the score rewards methods that require fewer function evaluations and solver iterations. Higher values indicate better performance. Unsuccessful or inapplicable solutions are reported as “–” in Table 15. 

Baselines. We compare DGA 2D and EoH against a broad collection of classical numerical root-finding methods. For scalar equations, we include robust bracketing methods such as Bisection and Brent’s method; derivative-based methods such as Newton’s method and Halley’s method; and derivative-free or accelerated iterations including the Secant and Steffensen methods. For the two-dimensional nonlinear system, we consider Damped Newton, Broyden’s method, Anderson acceleration, and SciPy’s hybrid solver. These baselines cover bracketing, derivative-based, quasi-Newton, fixed-point acceleration, and hybrid solution strategies, thereby providing a comprehensive comparison across both scalar and multivariate root-finding settings. Table 15: Results on numerical root-finding tasks. Higher is better. “–” indicates an unsuccessful or inapplicable solution. The best result in each column is highlighted in bold. 

Method Smooth Polynomial Ill-Conditioned Oscillatory Nonlinear System 2D 

Bisection 0.73 0.04 0.73 –Brent 0.80 0.10 0.80 –Newton 0.82 0.77 0.83 –Secant 0.83 0.78 0.86 –Steffensen 0.81 0.78 0.78 –Halley 0.83 0.78 0.83 –Newton (Damped) – – – 0.83 Broyden – – – 0.84 Anderson – – – 0.83 SciPy (hybr) – – – 0.82 DGA 2D 0.86 0.85 0.84 0.88 

EoH – -0.58 -1.29 0.88 Results Analysis. Table 15 demonstrates that DGA 2D achieves the most consistent performance across all four root-finding settings. It obtains scores of 0.86 , 0.85 , 0.84 , and 0.88 on the Smooth Polynomial, Ill-Conditioned, Oscillatory, and Two-Dimensional Nonlinear System tasks, respectively. DGA 2D ranks first on the Smooth Polynomial and Ill-Conditioned problems, ranks second only to the Secant method on the Oscillatory problem, and ties with EoH for the best result on the two-dimensional system. The conventional solvers exhibit a clear separation between scalar and multivariate applicability. Scalar methods perform strongly on the first three problems, with derivative-based approaches generally offering high efficiency, but they are inapplicable to the coupled two-dimensional system under the unified evaluation setting. Conversely, Damped Newton, Broyden, Anderson acceleration, and the hybrid solver successfully address the multivariate problem but do not transfer to the scalar benchmark tasks. EoH also displays strong task dependence: it achieves the best score on the two-dimensional system but fails on the Smooth Polynomial problem and obtains negative scores on the Ill-Conditioned and Oscillatory tasks. In contrast, DGA 2D consistently discovers effective root-finding algorithms across variations in conditioning, function geometry, and dimensionality. Overall, these experiments demonstrate the cross-domain applicability of DGA 2D as an automated algorithm-design frame-work. Although a separate algorithm is evolved for each benchmark problem within each task class, DGA 2D consistently discovers competitive task-specific methods across ODE integration, feedback control, and numerical root finding. These results therefore support framework-level transferability across continuous numerical domains, rather than zero-shot transfer of a single evolved algorithm.