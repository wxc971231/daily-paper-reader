---
title: Bellman-Taylor Score Decoding for Markov Decision Processes with State-Dependent Feasible Action Sets
title_zh: Bellman-Taylor分数解码：基于状态依赖可行动作集的马尔可夫决策过程
authors: "Yi Chen, Rushuai Yang, Qiang Chen, Dongyan, Huo"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10979v1"
tags: ["query:llm-opt-code"]
score: 9.0
evidence: 面向状态依赖可行动作集的马尔可夫决策过程的Bellman-Taylor得分解码，直接处理运筹优化问题
tldr: 针对状态依赖可行动作集的马尔可夫决策过程，标准深度强化学习难以处理隐式约束。本文提出Bellman-Taylor分数解码框架，通过泰勒展开将策略学习映射到欧几里得分数空间，并利用动作解码器保证可行性。理论证明最优性差距可分解为结构近似误差与算法学习误差。在排队网络控制实验中，该方法学习到状态依赖索引规则，小规模实例接近最优，大规模系统显著优于基准。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10979v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1664, \"height\": 383, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1702, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 536, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10979v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1281, \"height\": 351, \"label\": \"Table\"}]"
motivation: 标准DRL算法无法处理状态依赖且隐式定义的可行动作集，需新方法在保持可行性的同时利用深度强化学习。
method: 基于最优动作值函数泰勒展开，构建分数编码器将状态映射到欧几里得空间，动作解码器将分数映射为可行动作，形成潜在分数MDP，可用标准DRL优化。
result: 理论分解最优性差距；排队网络控制实验中小实例接近最优，大规模系统性能大幅超越基准。
conclusion: 提出的框架有效处理隐式约束MDP，为操作研究中的复杂控制问题提供了可扩展的强化学习方案。
---

## 摘要
许多运筹学中的马尔可夫决策过程（MDP）具有依赖于状态且由各种运营约束隐式定义的可行动作。这些特性使得难以使用标准的深度强化学习（DRL）算法，因为它们的动作接口通常假设固定的有限动作目录或简单的欧几里得空间。受最优动作值函数泰勒展开的启发，我们提出了Bellman-Taylor分数解码框架，该框架通过动作解码器强制可行性，将策略学习转移到欧几里得分数空间。由此产生的潜在分数MDP可以通过标准DRL算法进行优化，而无需通过解码器进行微分。我们提供了性能保证，表明该方法的优化差距分解为结构近似误差和算法学习误差。最后，我们将该框架应用于排队网络控制问题，其中策略本质上学习了一种基于状态依赖索引的调度规则。数值实验显示，在小规模实例中性能接近最优，在较大系统中相比基准有显著改进。

## Abstract
Many Markov decision processes (MDPs) in operations research have feasible actions that are state dependent and defined implicitly by various operational constraints. These features make it difficult to use standard deep reinforcement learning (DRL) algorithms, whose action interfaces typically assume either a fixed finite action catalog or a simple Euclidean space. Motivated by a Taylor expansion of the optimal action-value function, we propose Bellman--Taylor score decoding, a framework that moves policy learning to a Euclidean score space while enforcing feasibility through an action decoder. The induced latent-score MDP then can be optimized by standard DRL algorithms without differentiating through the decoder. We provide a performance guarantee showing that the optimality gap of this approach decomposes into a structural approximation error and an algorithmic learning error. Lastly, we apply this framework to a queueing network control problem, where the policy essentially learns a state-dependent index-based dispatching rule. Numerical experiments show near-optimal performance in small instances and considerable improvements over benchmarks in larger systems.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：运筹学中的许多马尔可夫决策过程（MDP）具有**状态依赖且隐式定义**的可行动作集，这些动作集通常由容量、匹配、整数等运营约束构成，无法枚举或标准化。标准深度强化学习（DRL）算法（如DQN、PPO）通常假设固定的有限动作目录或简单的欧几里得动作空间，因此难以直接应用于这类问题。
- **核心问题**：如何在不修改标准DRL算法接口的前提下，高效处理具有复杂、状态依赖约束的MDP，同时保证动作可行性。
- **整体含义**：提出一种新的动作接口范式——**Bellman-Taylor分数解码（BTSD）**，将策略学习从原始的不规则动作空间转移到欧几里得分数空间，利用动作解码器确保可行性，从而允许直接使用标准连续动作DRL算法（如PPO）进行优化。

## 2. 论文提出的方法论

- **核心思想**：将动作选择分解为**分数生成**和**解码**两个阶段。基于最优动作值函数\(Q^*(s,a)\)的泰勒展开，将动作的后继值（continuation value）近似为关于后动作特征（post-action configuration）的线性函数，从而将最优动作选择转化为一个线性优化问题。
- **关键技术细节**：
  1. 定义后动作配置\(\phi_s(a)\)，状态转移可写为\(s' = \Xi_s(\phi_s(a), \xi_s)\)，其中\(\xi_s\)为外生随机扰动。
  2. 定义后动作续值函数\(G^*_s(x) = \mathbb{E}[V^*(\Xi_s(x,\xi_s))]\)。
  3. 对\(G^*_s(\phi_s(a))\)在参考点\(x_{\text{ref}}(s)\)处作一阶泰勒展开，代入\(Q^*(s,a) = \psi_s(a) + \gamma G^*_s(\phi_s(a))\)，得到近似：
     \[
     Q^*(s,a) \approx \text{const} + \psi_s(a) + \langle z^*(s), \phi_s(a) \rangle
     \]
     其中\(z^*(s)\)为最优分数向量（梯度信息）。
  4. **动作解码器**：给定分数\(z\)，选择动作\(a\)使得\(\psi_s(a) + \langle z, \phi_s(a)\rangle\)最大。该优化问题在原动作集上求解，精确保证可行性。
  5. **潜在分数MDP**：原始MDP诱导出一个新的MDP，其中状态不变，动作为分数向量\(z \in \mathbb{R}^{d_s}\)，奖励和转移通过解码器定义。该MDP可用标准连续动作DRL（如PPO）求解，无需对解码器求导。
  6. 推广至更高阶：对于非线性较强的系统，可引入二阶或更高阶特征映射\(F_{s,K}(a)\)，构建高阶解码器。
- **性能分析**：将最优性差距分解为**结构近似误差**（由泰勒余项引起的动作排序误差）和**算法学习误差**（PPO求解潜在MDP的误差），并给出了用余项振荡\(\Omega_s(z)\)刻画的结构误差上界。

## 3. 实验设计

- **场景与数据集**：
  - **小规模场景**：2类顾客 × 2个服务池的排队网络，每个池容量5，服务率同质，仅交叉溢出成本不同（0.1, 0.3, 0.6），考虑平衡负载（ρ=0.9,0.9）和不平衡负载（ρ=0.95,0.85）两种情况。
  - **中等规模场景**：5类顾客 × 5个服务池的排队网络，每个池容量30，服务率池依赖但类无关（随机采样），设定三个负载模式（对称重型、中等不平衡、含过载类），每个模式生成9个实例（不同成本系数）。
  - **消融实验**：在同一中等规模实例上，将BTSD与PPO、SAC、DQN分别结合，对比无BTSD的vanilla版本（使用masking/projection保证可行性）。
- **基准对比方法**：
  - **传统启发式调度规则**：cμ规则、修正cμ规则、最大权重规则、修正最大权重规则（来自Chen, Dong, Shi, 2020）。
  - **DRL基线**：Atom-PPO（将组合动作分解为顺序原子动作的PPO，Sun et al., 2024）。
  - **优化型基线**：MIP-based 值向前看方法（Harsha et al., 2025）。
  - **消融对比**：无BTSD的vanilla PPO、SAC、DQN。
- **评价指标**：平均累积折扣成本（越低越好），以及小规模场景下的最优性差距、动作一致率、Bellman遗憾。

## 4. 资源与算力

**论文未明确说明所使用的GPU型号、数量及训练时长**。仅提到使用标准PPO实现，在CPU上进行小规模精确求解，中等规模实验依赖模拟器。未提供具体的硬件配置或训练时间。因此无法评估算力消耗。

## 5. 实验数量与充分性

- **实验数量**：
  - 小规模场景：2种负载设置 × 3种成本 = 6个实例，每个实例报告均值与标准差。
  - 中等规模场景：3种负载模式 × 每个模式3个实例 = 9个实例，每个实例对比5种基准 + BTSD-PPO。
  - 消融实验：1个实例，对比3种DRL骨干的vanilla和BTSD版本，共6种配置。
- **充分性与客观性**：
  - 小规模实验验证了接近最优的性能（差距<2.5%），证明方法在可精确求解情况下有效。
  - 中等规模实验覆盖了对称、不对称、过载等多种负载模式，基准包括传统启发式和前沿DRL方法，结果具有说服力。
  - 消融实验分离了BTSD框架与具体DRL优化器的影响，表明BTSD带来的提升是结构性的，而非优化器选择所致。
  - 但**局限性**：仅测试了排队网络控制一个应用领域，缺乏对其他类型约束MDP（如库存、资源分配）的广泛验证。实验次数有限，未进行大规模统计检验（如多次重复训练），也未讨论超参数敏感性。总体而言，实验设计合理、对比充分，但推广性需更多证据。

## 6. 论文的主要结论与发现

1. **BTSD框架有效**：能够将具有状态依赖约束的MDP转化为潜在分数MDP，允许直接使用标准DRL算法，无需设计复杂的概率分布或可行性修复。
2. **接近最优性能**：在小规模排队网络中，BTSD-PPO的最优性差距在1.1%–2.5%之间。
3. **显著优于基准**：在中等规模场景中，BTSD-PPO在所有9个实例上均获得最低成本，相对于最佳基准改善4.0%–23.7%。
4. **结构优势**：消融实验表明，BTSD对PPO、SAC、DQN等不同骨干均带来40%以上的成本降低，说明提升源于动作接口而非特定优化器。
5. **理论分解**：提供了最优性差距的理论分解，阐明了结构近似误差与学习误差的权衡。

## 7. 优点

- **方法创新**：将泰勒展开与动作解码结合，创造性地将约束MDP转化为标准连续动作MDP，同时严格保证可行性，避免了复杂的微分可导优化层。
- **实用性**：易于与现有DRL库（如PPO）集成，无需大量工程定制。
- **理论支撑**：给出了最优性差距的结构化分解，为理解方法误差提供了理论依据。
- **实验全面**：从小规模精确验证到中等规模大规模对比，再到消融研究，层级合理，结论可信。
- **揭示机理**：在排队网络例子中，BTSD自然学习到一种类max-weight索引规则，与传统启发式建立了联系，增强了可解释性。

## 8. 不足与局限

- **模型假设强**：方法要求MDP具有已知的结构化转移表示（即后动作配置\(\phi_s(a)\)和过渡函数\(\Xi_s\)），这在黑箱或数据驱动设定下可能不成立。
- **应用范围有限**：仅在排队网络控制上验证，未推广到其他运筹学问题（如库存、调度、车辆路径），通用性有待检验。
- **计算成本**：每个动作选择都需要求解一次整数线性规划解码器，在大规模或实时场景下可能成为瓶颈。论文未分析解码器的求解时间。
- **理论界实用性有限**：性能分析中的余项振荡\(\Omega_s(z)\)依赖未知的最优值函数，实际难以计算或先验给定。
- **未讨论平均成本准则**：论文聚焦折扣奖励，许多排队模型更关心平均成本，扩展并非平凡。
- **统计严谨性不足**：未报告多次独立训练的平均结果或置信区间，也未进行显着的超参数扫描，可能低估了方差影响。
- **潜在偏差**：实验中的基准实现可能未针对该特定问题调优，对比公平性存在细微偏差（如Atom-PPO原为时变泊松到达，本实验使用固定速率）。

（完）
