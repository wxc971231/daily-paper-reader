---
title: "EvoOptiGraph: Weakness-Driven Coevolution via Graph-Based Structural Generation for Optimization Modeling"
title_zh: EvoOptiGraph：基于图的结构生成中弱点驱动的协同进化用于优化建模
authors: "Qingcan Kang, Mingyang Liu, Xiaojin Fu, Shixiong Kai, Tao Zhong, Mingxuan Yuan"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26578v1"
tags: ["query:llm-opt-code"]
score: 9.0
evidence: 使用LLM从自然语言自动生成优化模型和求解代码
tldr: 自动化优化建模面临训练数据缺乏结构多样性和生成流水线静态的问题。EvoOptiGraph将混合整数线性规划表示为属性二分图，通过保留有效性的进化算子生成结构多样的实例，并转化为代码和自然语言。采用监督微调和基于模型弱点的强化学习，形成数据-模型协同进化的闭环。在六个数据集上显著优于大模型、代理方法和专用基线，验证了针对性协同进化策略的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26578v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1424, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 668, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1452, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 671, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1417, \"height\": 941, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26578v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1458, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1290, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1499, \"height\": 1510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1496, \"height\": 2326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1490, \"height\": 2342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1497, \"height\": 605, \"label\": \"Table\"}]"
motivation: 现有LLM优化建模数据缺乏结构多样性，且生成流水线静态，无法针对模型弱点改进。
method: 将MILP表示为属性二分图，用进化算子生成多样实例，通过确定性编译和验证回译转化为代码和语言，再结合SFT和弱信号驱动的RLVR训练。
result: 在六个数据集上，EvoOptiGraph在准确性、可执行性和泛化性上显著超越更大通用模型、代理方法和专门基线。
conclusion: 证明针对性数据-模型协同进化是提升LLM优化建模能力的有效策略。
---

## 摘要
利用大语言模型（LLM）从自然语言自动生成优化建模面临两大挑战。首先，训练语料库缺乏结构多样性。其次，数据生成流程仍保持静态且与模型学习脱节。为应对这些挑战，我们提出EvoOptiGraph——一种新颖的框架，其中数据和模型在模型弱点的驱动下协同进化。EvoOptiGraph将每个混合整数线性规划（MILP）表示为带属性的二分图，并应用保持有效性的进化算子生成结构多样化的实例。通过确定性编译和经过验证的回译，将进化后的图转换为求解器代码和自然语言。训练分为两个阶段：在初始数据集上进行监督微调（SFT），随后进行基于可验证奖励的强化学习（RLVR），其中由图导出的弱点信号指导针对模型失败点生成新实例。这形成了持续更新训练分布的闭环。在六个公开数据集上的实验结果表明，EvoOptiGraph在准确性、可执行性和泛化能力上显著优于更大的通用模型、智能体方法和专门的基线与基线方法。这些结果表明，有针对性的数据-模型协同进化是提升LLM在优化建模任务上表现的有效策略。

## Abstract
Automating optimization modeling from natural language with large language models (LLMs) faces two key challenges. First, training corpora lack structural diversity. Second, data generation pipelines remain static and decoupled from model learning. To address these challenges, we propose EvoOptiGraph, a novel framework where data and model co-evolve, driven by model weaknesses. EvoOptiGraph represents each mixed-integer linear program (MILP) as an attributed bipartite graph and applies validity-preserving evolutionary operators to generate structurally diverse instances. The evolved graphs are converted into solver code and natural language via deterministic compilation and verified back-translation. Training proceeds in two stages: supervised fine-tuning (SFT) on an initial dataset, followed by reinforcement learning with verifiable rewards (RLVR), where graph-derived weakness signals guide the generation of new instances targeting the model's failures. This forms a closed loop that continuously updates the training distribution. Empirical results on six public datasets show that EvoOptiGraph significantly outperforms larger generalist models, agentic methods, and specialized baselines in accuracy, executability, and generalization. These results demonstrate that targeted data-model coevolution is an effective strategy for improving LLMs on optimization modeling tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：使用大语言模型（LLM）从自然语言描述自动生成优化模型（例如混合整数线性规划 MILP）的求解代码，面临两大挑战：  
  - **训练数据缺乏结构多样性**：现有基准（如 NL4Opt、MAMO）仅覆盖少量结构模板，合成 pipelines 主要改变系数或文本措辞，而不是底层的优化结构。  
  - **数据生成流水线静态化**：数据集一次性构建后冻结，模型无法基于自身错误模式自适应地获取针对性的训练实例。

- **整体含义**：作者旨在构建一个**数据与模型协同进化**的闭环系统，通过显式的图结构表示来生成多样化、结构新颖的实例，并利用模型弱点信号驱动数据生成，从而持续提升 LLM 的优化建模能力。

### 2. 论文提出的方法论

- **核心思想**：将 MILP 表示为**属性二分图**，在图上执行遗传操作（交叉和变异）生成新实例；将进化后的图确定性编译为求解器代码，并通过验证回译得到自然语言描述；采用两阶段训练（SFT + 基于可验证奖励的 RL），其中弱点信号（从图特征推导）引导数据进化。

- **关键技术细节**：
  1. **图表示**：每个 MILP 建模为二分图 \(G=(V,E,A)\)，变量节点和约束节点通过边（带系数）连接，节点带有属性（如类型、界限）。该图可逆地恢复为求解器代码。
  2. **图进化**：
     - **交叉**：在相同或不同问题类之间交换兼容子图，保持每个后代恰好一个目标节点。
     - **变异**：参数变异（数值扰动）、结构变异（增删变量/约束）、规模变异（扩大问题尺寸）。所有后代经过有效性过滤。
     - **适应度函数**：\(f(G) = \beta \tilde{f}_{\text{diff}} + \gamma \tilde{f}_{\text{div}} + \delta \tilde{f}_{\text{weak}}\)，分别对应难度、多样性（按图编辑距离）、与模型弱点向量的余弦相似度。
  3. **弱点分析**：在验证集上收集当前模型的错误实例，计算图特征向量 \(\phi(G)\)，训练 XGBoost 分类器预测错误，再利用 SHAP 值获得特征重要性权重 \(w\)，作为弱点向量。
  4. **训练流程（Algorithm 1）**：
     - 从 53 个种子生成器进化得到初始数据集 \(D_0\)，进行 SFT 热身。
     - 每轮迭代：① 在验证集上提取弱点向量 \(w_t\)；② 用包含弱点对齐项的适应度函数进化新实例生成 \(D_{\text{RL}}\)；③ 对每个 prompt 采样 N 个候选代码，用 Gurobi 执行并计算多阶段奖励（过程、可解、接近度、正确性）；④ 使用 GRPO 进行策略优化。
     - 循环 T 轮，实现数据分布与模型的协同适应。

### 3. 实验设计

- **数据集**：六个公开基准，包括 NL4Opt、MAMO（EasyLP / ComplexLP）、NLP4LP、ComplexOR、IndustryOR。使用清理后的版本（源自 Xiao et al., 2025）。
- **对比方法**：
  - **零样本通用 LLM**：GPT-4o、DeepSeek-V3、Qwen3-32B、Qwen2.5-72B-Instruct。
  - **智能体方法**：OptiMUS-v0.3、CoT、Chain-of-Experts (CoE)。
  - **专门微调模型**：ORLM (8B)、LLMOPT (14B)、OptMATH (32B)。
- **评价指标**：Pass@1 准确率（生成的代码求解出的最优目标值与参考值匹配）。
- **消融实验**：比较 SFT-only 和 RL-only 两种变体，验证两阶段训练的必要性。
- **数据多样性对比**：使用 GenBench-MILP 的结构相似性指标，对比 OptMATH 与 EvoOptiGraph 生成数据的多样性。
- **共进化循环分析**：追踪从基础模型到 SFT 再到三轮 RL 的性能变化曲线。

### 4. 资源与算力

- 论文**未明确说明**训练 EvoOptiGraph 所消耗的具体算力（GPU 型号、数量、训练时长等）。仅在实现细节中提到：
  - 使用 Qwen3-8B 作为基础模型。
  - SFT 采用 LoRA（rank=32），学习率 \(1\times10^{-4}\)，3–5 个 epoch。
  - RL 使用 GRPO，采样 4 个候选/ prompt，温度 0.7，学习率 \(5\times10^{-6}\)，3 个 epoch。
  - 数据生成时使用 Gurobi 求解器，时间限制 300 s。
  - 回译阶段调用 DeepSeek-V3 生成自然语言描述。
- 因此，算力资源的具体细节缺失，但可以推断需要一个中等规模的 GPU 集群（例如 8×A100）进行数小时至数天的训练，但无法确定。

### 5. 实验数量与充分性

- **实验数量**：
  - 主表（Table 1）报告了 6 个数据集上的 Pass@1 结果，对比了 10+ 种基线方法。
  - 消融实验（Table 2）包括两个变体（SFT-only、RL-only）。
  - 数据多样性比较：50 次随机抽样取平均。
  - 共进化循环分析：绘制了从 Base → SFT → RL1 → RL2 → RL3 的准确率变化曲线。
  - 额外的图结构特征可视化（t-SNE）和 XGBoost AUC 值（附录 C.3）。
- **充分性与客观公平性**：
  - 对比基线覆盖全面（零样本、智能体、微调模型），且使用了清理后的数据集版本（避免噪声差异）。但注明了 OptMATH 和 LLMOPT 的结果来自原始论文（未清理版本），可能不完全可比，这是一个公开的诚实说明。
  - 消融实验设计合理，验证了两阶段协同进化的必要性。
  - 数据多样性对比采用通用指标，客观展示本方法生成了更多样化的数据。
  - 共进化循环分析展示了非单调性（MAMO-complex 上 RL2 下降然后 RL3 恢复），体现了诚实报道。
  - 总体实验设计较为充分，结论可信。

### 6. 论文的主要结论与发现

- **性能提升**：EvoOptiGraph 在六个基准上均取得最佳或并列最佳 Pass@1 准确率（宏平均 78.1%），超越更大规模的通用模型（如 GPT-4o、DeepSeek-V3）和专门模型（如 ORLM、LLMOPT）。
- **数据多样性**：生成数据的结构相似性（66.93%）远低于 OptMATH（93.04%），证明图进化显著提升了结构多样性。
- **两阶段有效性**：SFT 提供基础改进，RL 进一步带来额外增益；单独使用 SFT 或 RL 均低于完整框架。
- **弱点驱动的必要性**：弱点对齐项（\(\tilde{f}_{\text{weak}}\)）使进化生成的数据针对模型当前失败模式，是性能持续提升的关键。
- **规模不是唯一因素**：8B 参数模型通过针对性数据进化胜过 72B、32B 甚至 671B 的模型，说明结构化的数据进化比单纯扩大模型参数更有效。

### 7. 优点

1. **方法论创新**：首次提出数据与模型协同进化的闭环框架，将图结构表示、遗传编程、弱点分析和强化学习有机结合。
2. **结构化数据生成**：直接在二分图上进行交叉/变异，突破了传统参数扰动的局限，能产生全新的优化结构（例如跨类混合问题）。
3. **可解释性**：通过 SHAP 从图特征中提取弱点向量，提供了模型失败原因的直观视窗。
4. **实验结果扎实**：在多个基准、多种基线下进行系统比较，消融和轨迹分析支撑了结论。
5. **资源效率**：基于 8B 模型即可获得顶尖结果，表明方法对算力要求相对较低。

### 8. 不足与局限

1. **问题覆盖局限**：当前框架仅适用于混合整数线性规划（MILP），未扩展到非线性规划、组合优化等其他类型。
2. **数据生成成本**：验证每个进化实例需要调用 Gurobi 求解器，对于大规模实例可能非常耗时（文中提到时间限制 300s，MIP gap 0.01）。
3. **弱点向量作为优先级信号**：论文明确说明弱点向量是“practical prioritization signal rather than a causal account”，即可能无法揭示真正的因果失败模式。
4. **种子依赖**：初始种群依赖 53 个专家设计的种子生成器，若种子覆盖不全，则可能限制多样性。
5. **回译质量**：使用 LLM（DeepSeek-V3）生成自然语言描述，虽然有三阶段验证，但仍可能引入语义偏差或错误。
6. **实验公平性**：部分基线结果来自原始论文（未清理数据集），可能轻微不利于本方法（因为本方法使用清理版），但作者已指出，总体影响较小。
7. **算力未公开**：缺乏训练资源的具体报告，不利于复现和资源规划。

（完）
