---
title: Multi-Source and Cross-Scenario Strategy-Guided Code Optimization
title_zh: 多源跨场景策略指导的代码优化
authors: "Yuwei Zhao, Qianyu Xiao, Ye Cui, Yijun Yu, Yingfei Xiong"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20353v1"
tags: ["query:llm-opt-code"]
score: 8.0
evidence: 基于LLM的策略引导代码优化
tldr: "现有策略引导的代码优化受限于单一知识源和场景，无法利用教科书等多源知识且策略难以跨语言迁移。MoST框架将多源知识统一表示为证据对象，通过自平衡加权聚类跨源识别策略，并借助示例迁移生成目标场景规则。在C/C++、Python、Rust任务上，MoST生成与开发者完全一致补丁比SemOpt高24%-180%，语义等价补丁高21.88%-37.50%。该方法首次实现多源跨场景策略引导，显著提升LLM优化效果。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20353v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20353v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1861, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20353v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 925, \"height\": 405, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20353v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 914, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20353v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 793, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20353v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 670, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20353v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 783, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20353v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1850, \"height\": 588, \"label\": \"Table\"}]"
motivation: 现有策略引导方法仅利用提交历史，无法整合教科书等多源知识，且策略不能跨场景复用。
method: MoST统一表示多源知识为证据对象，通过自平衡加权聚类跨源识别策略，并用示例迁移生成目标场景的静态分析规则。
result: "在C/C++、Python和Rust任务上，MoST生成的语义等价补丁比SemOpt多21.88%-37.50%，性能测试提升最高717.42%。"
conclusion: 提出多源跨场景策略引导框架，显著提升LLM代码优化效果，可广泛应用于不同语言和场景。
---

## 摘要
自动化代码优化通过重构源代码来提升程序性能，近期研究使用大语言模型生成优化补丁。最新方法是策略引导的：它们从历史优化提交中总结策略作为静态分析规则，并利用这些规则匹配代码位置供大语言模型优化。然而，这些方法存在两个局限性：(1) 策略可能来自其他知识源（如教科书和网页），但现有方法无法利用它们；(2) 一条策略可能适用于不同场景（例如不同编程语言），但现有方法只能形式化来源提交所属场景的策略。为解决这些局限性，我们提出MoST，一种基于大语言模型的代码优化框架，它跨场景整合多个知识源。MoST将不同知识源中的条目统一表示为证据对象，以跨源跨场景的方式聚类它们以识别策略，并在必要时将其迁移到目标场景以生成静态分析规则。为实现这一过程，MoST采用了一种新颖的自平衡加权聚类算法来平衡来自不同知识源的证据对象，以及一种新颖的示例迁移流程，以确保跨场景迁移时生成规则的质量。在一个包含151个C/C++、150个Python和50个Rust历史优化任务的基准测试中，与SemOpt相比，MoST生成的与开发者补丁完全相同的补丁数量分别增加了24.44%-180.00%，语义等价的补丁数量增加了21.88%-37.50%。在优化15个真实项目时，MoST在项目性能测试中实现了19.72%-717.42%的最大改进和4.44%-258.17%的平均改进，显著优于SemOpt和Codex。

## Abstract
Automated code optimization improves program performance by refactoring source code, and recent studies use LLMs to generate optimization patches. The newest approaches are strategy-guided: they summarize strategies from historical optimization commits as static analysis rules, and use these rules to match code locations for LLMs to optimize. However, these approaches have two limitations: (1) the strategies may come from other knowledge sources, such as textbooks and web pages, but the existing approaches cannot utilize them; (2) a strategy may be applicable to different scenarios, e.g., different programming languages, but existing approaches can only formalize strategies for the scenario to which the source commit belongs. To address these limitations, we propose MoST, an LLM-based code optimization framework that integrates multiple knowledge sources across scenarios. MoST uniformly represents items in different knowledge sources as evidence objects, clusters them in a cross-source and cross-scenario manner to identify strategies, and transfers them to the target scenario when necessary for generating static analysis rules. To implement this process, MoST employs a novel self-balanced weighted clustering algorithm to balance evidence objects from different knowledge sources, and a novel example transfer procedure to ensure the quality of the generated rules when transferring across scenarios. On a benchmark containing 151 C/C++, 150 Python, and 50 Rust historical optimization tasks, compared with SemOpt, MoST yields 24.44%-180.00% and 21.88%-37.50% more patches that are exactly the same as or semantically equivalent to developer patches, respectively. When optimizing 15 real-world projects, MoST achieves 19.72%-717.42% maximum improvements and 4.44%-258.17% average improvements for the performance tests in the projects, significantly outperforming SemOpt and Codex.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：自动化代码优化对于提升程序性能至关重要。现有策略引导的方法（如 SemOpt）从历史优化提交中提取策略并生成静态分析规则来指导 LLM 优化代码，取得了比直接提示更好的效果。
- **核心问题**：但这类方法存在两个严重局限：
  1. **知识源单一**：仅能利用历史提交，无法整合教科书、手册、网页等多源知识，而许多优化策略存在于这些来源中（如 Intel 优化手册中的循环分块）。
  2. **策略无法跨场景迁移**：每个策略被形式化为其来源场景（如语言、架构）下的规则，无法在不同场景（例如从 C 迁移到 Rust）下复用。
- **整体含义**：作者提出 MoST 框架，首次实现多源知识融合与跨场景策略迁移，显著扩展了策略覆盖范围，提升了 LLM 代码优化效果。

## 2. 方法论（核心思想、关键技术细节、算法流程）
### 2.1 核心思想
- 将不同知识源中的数据项统一表示为**证据对象**（Evidence Object），包含自然语言描述、代码修改示例、适用场景标签、来源类型。
- 通过**自平衡加权聚类**跨源识别策略，解决不同来源证据频率/质量不平衡问题。
- 通过**示例迁移**+**规则验证**生成目标场景下的静态分析规则，确保规则质量。

### 2.2 关键技术细节与流程
#### Phase 1：证据对象构造
- 对于**提交**：提取代码变更，利用 LLM 生成自然语言描述。
- 对于**文档**（当前仅 Intel 优化手册）：分割为块，LLM 提取优化描述，必要时生成完整的前/后代码示例。
- 每个证据对象包含：`<P, E, T, y>`（描述、示例、场景标签、源类型）。

#### Phase 2：自平衡加权聚类
- 使用**加权密度聚类（DBSCAN）**，提交证据权重固定为 1，文档证据权重为可调超参数。
- 三个超参数：文档权重 w_d、相似度阈值 τ、最小簇大小 s_min。
- **自平衡选择准则**：衡量聚类后各场景策略簇数量相对于单场景聚类结果的偏差（使用对数比率 + 惩罚项），在候选参数集 Θ 中随机搜索，选择使损失最小的参数（自动选出 w_d=3, τ=0.76, s_min=5）。
- 聚类后，每个簇分配一个整数示例预算 W(C) = floor(平均加权证据强度 + 0.5)。

#### Phase 3：目标场景规则生成与验证
- 对于每个簇 C 和目标场景 T*：
  - 计算预算 n_C = W(C)。
  - 分三种情况选择示例：
    1. 已有足够目标场景示例（≥ n_C）：直接采样。
    2. 有部分目标场景示例（0 < m < n_C）：保留这些，从其他场景采样并请 LLM 迁移到目标场景。
    3. 没有目标场景示例：先评估策略是否适用于目标场景，若适用则从其他场景采样并迁移。
  - 为每个选择/迁移的示例，LLM 生成 b 条 Semgrep 规则（b=5）。
  - **功能验证**：规则必须通过解析和执行检查，且必须匹配前优化代码、不匹配后优化代码；否则返回给 LLM 重新生成。
- 最终生成 356 个策略簇，对应 8 个目标场景共约 34,170 条规则。

#### Phase 4：优化器
- 使用规则扫描目标函数，合并匹配位置，按规则数排序，保留 Top 25 位置-策略对。
- 为每个对提供目标函数、匹配位置、策略描述、目标场景示例、上下文（如架构信息），提示 LLM 生成优化补丁。

## 3. 实验设计
### 3.1 数据集 / 场景
- **历史优化任务再现（RQ1）**：
  - C/C++：151 个任务（来自 SemOpt 基准）
  - Python：150 个任务（来自 SemOpt 基准）
  - Rust：自行构建 50 个任务（从 GitHub Top100 仓库中筛选）
- **真实项目优化（RQ4）**：
  - C/C++：RocksDB、Redis、gRPC、LevelDB、spdlog（共 5 个）
  - Python：Click、Flask、Jinja2、Requests、Scrapy（共 5 个）
  - Rust：Rapier、image、regex、sqlparser、Tokio（共 5 个）
  - 合计 15 个项目，259 个性能数据点。

### 3.2 对比方法
- **Direct Prompt**：直接要求 LLM 优化当前函数。
- **RAG**：检索 BM25 最相似的 4 个优化示例，拼入提示。
- **SemOpt**：SOTA 策略引导方法（仅利用提交历史）。
- **Codex Agent**（在真实项目实验中）：Codex Spark、Codex 5.4 Mini、Codex 5.4（使用 30 分钟版本 + 高思考模式）。

### 3.3 评估指标
- **历史任务**：精确匹配（EM）、语义等价（SemEqv）。
- **真实项目**：最大性能改进（Max）、平均改进（Avg）、改进≥5%和≥10%的数据点数量。使用 Welch t-test 检验显著性（p<0.05），每个测试运行 6 次取后 5 次平均。

## 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。
- 所有 LLM 调用均为**推理**（无训练），使用 DeepSeek-V4-Pro 和 GPT-5.2 作为优化器模型，温度设为 0。
- 仅在真实项目实验中使用 Codex Agent 时提及 30 分钟版本，但未说明硬件细节。

## 5. 实验数量与充分性
### 5.1 实验数量
- **历史任务**：351 个任务（C/C++ 151 + Python 150 + Rust 50），每个方法重复 3 次。
- **消融实验**（RQ2）：在 C/C++ 上对比完整 MoST、移除加权聚类、移除可靠规则生成三种变体。
- **参数选择**：45 个任务的 pilot 集（每种语言 15 个）用于选择聚类参数，不与正式基准重叠。
- **真实项目**：15 个项目，每个项目有 2~45 个性能数据点，最终结果经过统计显著性检验。
- **来源分析**（RQ5）：统计 3831 个有效优化结果的来源构成。

### 5.2 充分性与公平性
- **充分性**：实验覆盖三种主流语言，既有历史再现有真实应用；消融实验验证了两个核心组件的贡献；参数选择通过独立 pilot 集，避免过拟合；与多种基线（直接提示、RAG、SemOpt、Codex）比较。
- **公平性**：所有方法使用相同的 LLM（DeepSeek-V4-Pro），在相同 benchmark 和评估协议下比较；排除数据泄露（去除与任务完全相同的 commit 或代码）；统计检验保证性能差异显著。整体设计较为客观公正。

## 6. 主要结论与发现
- **历史任务再现**：MoST 在 EM 上比 SemOpt 提升 24.44%~180.00%，在 SemEqv 上提升 21.88%~37.50%；比 Direct Prompt 提升 480%~1300%，比 RAG 提升 93.94%~250%。
- **真实项目优化**：MoST 在全部 15 个项目上取得最大性能改进（Max），在 14 个项目上取得平均改进（Avg）最高。例如，C/C++ 项目 Max 提升 19.72%~249.75%，Python 项目 Max 提升 82.62%~717.42%，Rust 项目 Max 提升 20.22%~384.42%。
- **消融实验**：去掉加权聚类导致性能下降 8.97%~10.71%，去掉可靠规则生成导致下降 43.59%~50.00%，说明两个组件均重要，且规则生成更为关键。
- **来源分析**：文档信息贡献了 21.93% 的有效优化结果（C/C++ 中高达 45.73%）；跨场景迁移生成的规则贡献了 55.47% 的有效结果（Python 和 Rust 中超过 75%），证明多源和跨场景策略的有效性。

## 7. 优点
- **创新性强**：首次提出多源知识融合和跨场景策略迁移，统一了异构知识源的表示，解决了现有方法的根本局限。
- **方法设计巧妙**：自平衡加权聚类自动调节不同源权重，避免高频低质数据淹没高频高质数据；示例迁移配合功能验证，确保跨场景规则质量。
- **实验全面**：覆盖 C/C++、Python、Rust 三种语言，包含历史再现实、真实项目、消融、来源追溯等多维度评估，统计显著性检验增强可信度。
- **结果显著**：在多个指标和场景下大幅超越现有最强方法，尤其在 Rust 等低资源语言上效果提升巨大。
- **实用性强**：在真实项目中可产生可衡量的性能提升，且优化建议接受率高（89.70% 被判断为有价值）。

## 8. 不足与局限
- **数据泄露风险**：虽然排除了精确匹配的 commit 和代码，但 LLM 训练数据中可能包含类似策略，无法完全审计。
- **知识源单一**：当前仅整合了 Intel 优化手册（C/C++ 文档），没有涵盖更多文档源（如 Python 性能指南、Rust 优化手册），可能导致对其他语言的支持不足。
- **实验覆盖有限**：Rust 基准仅 50 个任务，且来源于 GitHub 高星项目，可能不代表所有 Rust 优化类型；真实项目数量 15 个，每个项目实验次数较少（仅 1 次最终评估）。
- **依赖 LLM 能力**：策略描述生成、示例迁移、规则生成均依赖 LLM 的语义理解能力，LLM 的性能波动可能影响结果稳定性；仅使用 DeepSeek-V4-Pro 作为主力模型，未充分验证其他 LLM 下的泛化性。
- **计算成本**：虽然未细算，但大规模规则生成（~34,170 条）和多次 LLM 调用在推理成本上可能较高，实际应用中需权衡。
- **应用限制**：仅针对能表达为静态分析规则的模式，对于需要复杂上下文的优化（如算法替换、跨函数优化）可能不适用；目前仅支持 Semgrep 规则，扩展性受限于该工具。

（完）
