---
title: "Symbolic Discovery of Iterative Algorithms: A Continuous Latent Space Bayesian Optimization Framework"
title_zh: 迭代算法的符号发现：一种连续潜空间贝叶斯优化框架
authors: "Tongjia Liu, Ilias Mitrai"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01552v1"
tags: ["query:llm-opt-code"]
score: 8.0
evidence: 自动发现迭代优化算法
tldr: 算法发现任务面临离散搜索空间大、计算开销高的挑战。本文提出将迭代算法发现转化为离散优化问题，利用变分自编码器将离散更新函数空间映射到连续潜在空间，再采用贝叶斯优化高效搜索新算法。在两个案例中，该方法成功发现了符号形式的更新函数，无需先验函数假设，且计算时间显著低于传统数学规划方法。该框架为自动化发现迭代优化算法提供了一种高效、通用的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1782, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1891, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 544, \"label\": \"Figure\"}]"
motivation: 现有算法发现方法依赖数学规划，计算成本高且受限于函数形式假设，亟需更高效、通用的自动化发现方法。
method: 将离散更新函数空间通过变分自编码器映射至连续潜在空间，随后在该连续空间中执行贝叶斯优化搜索新算法。
result: 在两个优化案例中成功发现符号形式的更新函数，计算时间低于传统数学规划方法，且无需预设函数结构。
conclusion: 提出了一种无需函数形式假设、计算高效的符号算法发现框架，为自动设计迭代优化算法提供了新途径。
---

## 摘要
在本文中，我们考虑迭代优化算法的自动发现。我们将算法发现任务表述为一个离散优化问题，并使用潜空间贝叶斯优化搜索新的更新函数。所提出的框架首先使用变分自编码器学习更新函数的离散空间的连续表示，将算法发现任务从离散搜索问题转化为连续搜索问题。随后，连续表示被用于使用贝叶斯优化搜索新算法。应用于两个案例研究表明，所提出的方法能够在不对更新函数的功能形式做任何假设的情况下，以符号形式发现新的更新函数。此外，发现新更新函数所需的计算时间低于现有的基于数学规划的方法。

## Abstract
In this paper, we consider the automated discovery of iterative optimization algorithms. We formulate the algorithm discovery task as a discrete optimization problem and search for new update functions using latent space Bayesian Optimization. The proposed framework first learns a continuous representation of the discrete space of update functions using variational autoencoders, transforming the algorithm discovery task from a discrete to a continuous search problem. The continuous representation is subsequently used to search for new algorithms using Bayesian optimization. Application to two case studies shows that the proposed approach can discover new update functions in symbolic form without any assumptions on the functional form of the update function. Moreover, the computational time required to discover the new update functions is lower than existing mathematical programming-based approaches.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：如何自动发现迭代优化算法中的更新函数（update function），以实现无需人工设计、无需先验函数形式假设的算法生成。
- **研究动机**：传统迭代算法依赖领域专家手动设计更新规则，耗时长且难以自动化；现有自动发现方法（如神经网络参数化、遗传编程、数学规划）存在可解释性与可扩展性之间的权衡——黑箱模型难以分析，数学规划搜索成本高且评价指标需闭合表达式。
- **背景**：本文将算法发现建模为离散优化问题，利用变分自编码器（VAE）将离散的表达式树空间映射到连续潜空间，再通过贝叶斯优化（BO）进行高效搜索，旨在实现**可解释、样本高效**的符号算法发现。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将更新函数表示为二元表达式树，通过VAE学习树的离散表示的连续潜空间，将该空间作为贝叶斯优化的搜索域，从而将离散优化转化为连续优化问题。
- **关键技术细节**：
  - **表达式树表示**：更新函数 \( g \) 用深度为 \( d \) 的完全二叉树表示，节点可赋予二元运算符（+,-,*）、一元运算符（sin,cos,exp）或操作数（变量 \( x_k \)、梯度 \( \partial_k F \)、常数）。满足arity约束（式4-6）。
  - **VAE编码**：将树的整数编码 \( \hat{y} \) 输入编码器得到潜变量 \( z \) 的均值与方差，通过重参数化采样，解码器再将 \( z \) 映射回整数表示 \( \hat{y}_{\text{pred}} \)。
  - **训练损失**：交叉熵重构损失 + β 加权KL散度（\( \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{recon}} + \beta \mathcal{L}_{\text{KL}} \)）。
  - **更新函数评价**：对于给定的树结构 \( \hat{y} \)，固定结构后求解一个关于常数的非线性规划（式9），以最小化多个目标函数和起点下的梯度范数。若不可行则返回大常数惩罚。
  - **潜空间贝叶斯优化**：使用高斯过程（GP）代理模型和期望改进（EI）获取函数，在潜空间 \( Z \) 中迭代采样候选点，解码后评价性能，更新GP。
  - **有效性处理**：解码出的树若不符合arity规则则直接赋予大惩罚（文中实验未出现无效树）。

#### 3. 实验设计：数据集/场景、基准、对比方法
- **数据集/场景**：
  - **案例1**：两个非凸函数 \( F_1(x)=(x_1-2)^2+2x_2^2-x_1x_2 \)，\( F_2(x)=(x_1-2)^4+2(x_1-2x_2)^2 \)；各使用2个随机起点（固定种子1234）。
  - **案例2**：线性回归最小二乘问题 \( \min 0.5\|Ax-b\|^2 \)；两个随机生成的问题（A矩阵条件数分别为16.87和7.88），各2个起点。
- **基准**：带步长优化的最速梯度下降（GD），步长通过最小化达到收敛所需迭代次数确定。
- **对比方法**：确定性MINLP数学规划方法（来自作者此前工作[6]），使用BARON和SCIP求解器，求解时间上限3600秒。
- **评价指标**：\( m(\hat{y}) \) 为在 \( N_F=2 \) 个函数、\( N_p=2 \) 个起点上运行 \( N_{it}=5 \) 次迭代后梯度范数的均值。

#### 4. 资源与算力
- 文中**未明确提及GPU型号、数量**，仅说明训练VAE耗时约1.66秒（CPU），贝叶斯优化总计算时间案例1为121秒（其中112秒用于函数评价，9秒用于选择候选点），案例2为176秒（157秒评价，18秒选择）。所有优化求解（评价任务）使用SCIP v9.2.3求解器在Pyomo中实现，运行于CPU环境。未提供具体硬件细节。

#### 5. 实验数量与充分性
- **实验数量**：全篇共两个主要案例，每个案例仅有一套参数设置（2函数×2起点×5迭代）。此外包含VAE潜空间维度敏感性分析（从2到14维，图2），用于确定隐层维度为7。
- **充分性评估**：
  - **正面**：两个案例覆盖了非凸优化和凸二次优化，且与MINLP方法进行了计算时间比较，展示了优势。
  - **不足**：
    - 实验规模较小（仅4个优化问题实例），未验证不同函数、不同维度、不同迭代次数下的泛化性。
    - 未做消融实验（如去掉VAE直接使用离散BO、不同VAE架构、不同获取函数等）。
    - 评价任务中常数优化使用全局求解器SCIP，未尝试局部优化以减少时间；也未与其他自动发现方法（如遗传编程、强化学习）定量对比。
    - 仅测试了无约束优化，未涉及约束优化或等式系统等更广泛任务。
    - 未报告多次运行（种子或重启）的统计结果，结论可能受随机初始化影响。

#### 6. 论文的主要结论与发现
- **结论**：所提出的潜空间贝叶斯优化框架能够自动发现符号形式的更新函数，无需预设函数形式假设，且计算时间远低于现有数学规划方法。
- **具体发现**：
  - 案例1发现了一个状态相关步长更新规则 \( x_{i+1} = x_i - 0.035(2.18+x_i)\nabla F(x_i) \)，在5次迭代内的梯度范数显著低于GD（GD需12次达到同等水平）；GD在该框架中被“重新发现”。
  - 案例2发现规则 \( x_{i+1} = x_i - 0.11\nabla F(x_i) - 1.61\times10^{-3}x_i \)，对于条件数较大的问题效果优于GD；条件数较小的则相近。
  - VAE能够学习有意义的连续表示，BO能有效探索该空间并发现新算法。

#### 7. 优点（方法或实验设计上的亮点）
- **方法论创新**：将离散表达式树搜索转化为连续潜空间搜索，融合VAE与BO，兼顾可解释性与采样效率。
- **无需先验假设**：更新函数表达式完全由树结构生成，不限制运算组合。
- **计算效率高**：相较于MINLP（3600秒内未找到最优），本方法在百余秒内找到更好解。
- **通用性**：框架可应用于任何迭代计算任务（如求解方程组、PDE等）。
- **实验设计**：对比了最速下降法这一经典基线，且重新发现GD验证了表示学习的有效性。

#### 8. 不足与局限
- **实验覆盖不全**：仅两个简单案例（低维、小迭代次数），未验证在更高维、更多函数、更复杂任务上的表现。
- **评价任务计算开销**：仍需对每个候选树求解一个全局非线性规划，对于大规模问题可能仍昂贵；文中仅用5次迭代，实际应用需要更多迭代评估。
- **缺乏与主流方法的系统对比**：未与遗传编程（如Chen et al. 2024）、学习优化（L2O）等方法在相同基准上比较。
- **VAE生成有效性**：虽然文中未出现无效树，但可能因树深度小、算子集合有限所致；深度更大时可能频繁生成无效树，需额外处理（如grammar VAE）。
- **偏差风险**：随机种子固定（1234），未报告多次实验的方差，结论稳健性存疑。
- **应用限制**：假设所有问题变量数相同，且仅考虑无约束优化；未讨论如何适应不同维度的任务。

（完）
