---
title: Multi-turn RL with Structural and Performance Aware Rewards for CUDA Kernel Generation
title_zh: 面向CUDA内核生成的结构与性能感知奖励的多轮强化学习
authors: "Quazi Ishtiaque Mahmud, Nesreen K. Ahmed, Ali Jannesari"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20908v1"
tags: ["query:llm-opt-code"]
score: 8.0
evidence: LLM利用强化学习生成优化的CUDA内核代码
tldr: "现有强化学习框架仅基于执行结果奖励，忽略代码结构性能关键特征。提出CudaPerf，包含离线对比排序和在线强化学习两阶段，联合优化正确性、加速比和结构效率。在C到CUDA和PyTorch到CUDA任务上，分别比Qwen-3-32B和CUDA Agent获得最高5倍和3.32倍加速比提升，正确性提升17%和7%。通过引入结构感知奖励，显著提升LLM生成优化CUDA代码的性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20908v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1424, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20908v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20908v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 691, \"height\": 489, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20908v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1406, \"height\": 856, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20908v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 641, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20908v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20908v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1453, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20908v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1456, \"height\": 693, \"label\": \"Table\"}]"
motivation: 现有RLVR方法仅基于执行结果奖励，缺乏对并行化结构特征的优化。
method: 提出两阶段框架：离线对比排序学习代码结构优劣，在线RL联合优化正确性、加速比和结构效率。
result: "在C到CUDA和PyTorch到CUDA任务上，加速比提升最高5倍和3.32倍，正确性提升17%和7%。"
conclusion: CudaPerf通过结构性能联合奖励显著超越强基线，证明结构化奖励对CUDA内核生成的有效性。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已成为增强大语言模型推理能力以生成优化代码的强大技术。然而，现有的RLVR方法主要依赖于结果性信号（如正确性和加速比），忽略了对于生成优化代码至关重要的程序性能关键结构属性。在本工作中，我们提出了CudaPerf，一个反思式强化学习框架，它结合了可验证的执行奖励和从并行化特征（如内存合并、占用率、算术强度和同步模式）中推导出的结构化代码感知奖励。CudaPerf分两个阶段运行：（1）离线成对排序模块，通过对比学习区分强弱程序候选；（2）在线RL训练阶段，通过统一的奖励信号协同优化正确性、性能和结构效率。为了进一步增强学习，CudaPerf利用执行反馈进行迭代细化，使得生成的候选程序能够逐步改进。我们还引入了一个数据集，包含2.9k个C到CUDA程序以及1k个PyTorch到CUDA程序，每个程序配对多种输入配置和多种包含不同优化策略的CUDA实现。CudaPerf在多个基准测试上进行了评估，包括C到CUDA和PyTorch到CUDA的转换。实验结果表明，CudaPerf显著优于强基线模型，包括Qwen-3-32B（用于C到CUDA）和CUDA Agent（用于PyTorch到CUDA），在加速比上分别获得了高达5倍和3.32倍的提升，在正确性上分别获得了17%和7%的提升。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful technique to enhance the reasoning capacity of LLMs for optimized code generation. However, existing RLVR approaches primarily rely on outcome-based signals such as correctness and speedup, overlooking performance-critical structural properties of programs that are essential for generating optimized code. In this work, we propose CudaPerf, a reflective RL framework that incorporates both verifiable execution rewards and structural code-aware rewards derived from parallelization features (e.g., memory coalescing, occupancy, Arithmatic Intensity, and synchronization patterns). CudaPerf operates in two stages: (1) an offline pairwise ranking module that learns to distinguish strong and weak program candidates via contrastive comparisons, and (2) an online RL training phase that jointly optimizes for correctness, performance, and structural efficiency through a unified reward signal. To further enhance learning, CudaPerf utilizes iterative refinement using execution feedback enabling progressive improvement of generated candidates. We also introduce a dataset comprising 2.9k C to CUDA and 1k PyTorch to CUDA programs, each paired with diverse input configurations and multiple CUDA implementations encompassing diverse optimization strategies. CudaPerf is evaluated across multiple benchmarks comprising both C to CUDA and PyTorch to CUDA transformations. Empirical findings suggest that CudaPerf significantly outperforms strong baselines, including Qwen-3-32B (for C to CUDA) and CUDA Agent (for PyTorch to CUDA) by achieving up to 5X & 3.32X improvements in speedup, and 17% & 7% improvements in correctness, respectively.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与研究动机

- 现有基于可验证奖励的强化学习（RLVR）在生成优化CUDA内核时，仅依赖结果性信号（如正确性和加速比），忽略了影响GPU性能的关键代码结构属性（如内存合并、算术强度、占用率、控制分歧、同步模式等）。
- 专家GPU程序员不仅依靠执行反馈，还会利用代码结构特性指导优化，因此需要将结构感知信号融入奖励函数。
- 论文旨在弥合神经代码生成与专家级性能工程之间的差距，提出一个同时利用执行反馈与结构特征的学习框架。

## 2. 方法论

### 核心思想
- 提出两阶段框架 **CUDAPERF**：
  1. **离线成对排序模块**：学习从代码结构特征到性能分数的映射。
  2. **在线RL训练阶段**：通过统一奖励信号联合优化正确性、性能与结构效率。

### 关键技术细节
- **结构特征抽取**：从CUDA程序中提取11维特征向量：
  - `coal`（内存合并）、`ai`（算术强度）、`occ`（占用率）、`div`（控制分歧）、`xfer`（数据传输开销）、`atomics`、`sync`、`kernels`、`tpb`（每块线程数）、`gmem_access`、`ops`。
  - 基于NVIDIA CUDA指南和性能建模文献设计。
- **离线排序训练**：
  - 对每个训练样本（原始代码）生成多个CUDA候选，按实测加速比排序，取上四分位（强候选）和下四分位（弱候选）。
  - 训练MLP ranker（隐藏层64、32，dropout 0.1）通过成对排序损失：
    \[
    L_{rank} = \mathbb{E}_{(y^+, y^-)}[\log(1 + \exp(-(s_\psi(\hat{\phi}(y^+)) - s_\psi(\hat{\phi}(y^-)))))]
    \]
  - 使用AdamW优化，训练50 epoch。
- **在线RL训练**：
  - 采用GRPO（Group Relative Policy Optimization）目标函数，结合两种奖励：
    - **可验证执行奖励** \(R_{ver}\)：编译惩罚、正确性惩罚（测试通过率）、性能奖励（log缩放加速比）、稳定性惩罚（变异系数）。
    - **结构奖励** \(R_{str}\)：由离线排训器输出分数。
  - 复合奖励 \(r_i = R_{ver}(i) + R_{str}(i)\)，组归一化后计算优势。
  - 多轮迭代细化（最多5轮）：将执行反馈和结构特征值以结构化文本形式加入prompt，并利用另一LLM总结历史反馈避免上下文爆炸。
- **Prompt构建**：包含顺序代码、输入、5个自上而下选择的few-shot示例（按加速比排序）。

## 3. 实验设计

### 数据集与Benchmark
- **C→CUDA**：
  - 自建数据集：2903个C程序，每个至少5种有效CUDA变体（≥1.5×加速比），10%留作评估。
  - BabelTower评估集：233对C→CUDA。
- **PyTorch→CUDA**：
  - 自建数据集：1013个PyTorch程序。
  - KernelBench数据集：250个PyTorch程序（ML负载，三个难度级别）。
- **训练数据来源**：BabelTower、CUDA Agent、CodeNet（AIZU/AtCoder）等。

### 对比方法
- 通用基座：Qwen-3-32B、Qwen2.5-Coder-32B、CodeLlama-34b-Instruct-hf、OpenAI O4-mini。
- 任务专门模型：CodeRosetta、QiMeng-MuPa（C→CUDA）；Kevin、CUDA Agent（PyTorch→CUDA）。

### 评估指标
- **正确性**：所有测试用例通过的比例（Pass@1/3/5）。
- **性能**：几何平均加速比（顺序运行时间 / CUDA运行时间，5次中值）。

## 4. 资源与算力

- **硬件**：2× A100 40GB GPU。
- **训练时长**：
  - 离线排训：2小时16分钟。
  - C→CUDA RL训练：3天12小时12分钟。
  - PyTorch→CUDA RL训练：3天8小时44分钟。
- **QLoRA微调**：4-bit量化，基础模型Qwen-3-32B，生成16个候选，核采样（p=0.9，温度0.2~0.7），最大长度1536 tokens。

## 5. 实验数量与充分性

- **主要实验**：在三个基准（CUDAPerf数据集、BabelTower、KernelBench）上与多个基线对比（Table 1），覆盖pass@5。
- **模型大小消融**：Qwen-3-8B/14B/32B作为基座（Table 2）。
- **奖励消融**：仅结构奖励、仅可验证奖励、两者结合（Table 3）。
- **详细Pass@1/3/5结果**：Table 4展示了更细粒度的正确性和性能。
- **公平性**：所有基线在相同硬件上评估，超参数报告完整，训练/评估分离。
- **充分性评估**：实验覆盖了主要任务和主流基线，消融实验验证了各组成部分的贡献，结果页面提供众多数值。可以认为是充分的。

## 6. 主要结论与发现

- CUDAPERF在所有基准上显著优于强基线：
  - **C→CUDA**：正确性最高89%（比Qwen-3-32B高17%），加速比11.02×（比Qwen-3-32B高4.82×）。
  - **PyTorch→CUDA**：正确性93%（比CUDA Agent高7%），加速比6.41×（比CUDA Agent高3.32×）。
  - 对比QiMeng-MuPa（正确性接近但加速比高3.01×）。
- **结构奖励+执行奖励互补**：单独移除任一会导致正确性和加速比大幅下降（Table 3）。
- 模型规模越大性能越好（32B > 14B > 8B）。

## 7. 优点

- **创新奖励设计**：首次将代码结构特征（如内存合并、算术强度）显式引入RL奖励函数，使模型学习“为什么快”而不仅是“快”。
- **两阶段框架**：离线排序提供可部署的结构先验，在线RL通过执行反馈微调，两者结合高效。
- **多轮迭代细化**：利用编译错误、失败用例、结构特征等结构化反馈，提升模型调试和优化能力。
- **数据收集**：构建大规模多任务CUDA数据集，含多种优化策略。
- **实验严谨**：多重消融、多基线、Pass@1/3/5指标、模型规模影响分析。

## 8. 不足与局限

- **计算开销**：训练过程需大量编译和基准测试，整体耗时较长（~3.5天/A100×2）。
- **探索能力限制**：奖励设计基于现有结构特征，可能无法引导模型发现超出已知模式的创新优化策略。
- **环境依赖性**：执行结果依赖于特定GPU（A100），到其他架构的泛化性未验证。
- **数据集偏差**：自建数据集仅包含静态输入配置和有限优化策略，可能不覆盖所有真实应用场景。
- **迭代轮次上限**：仅使用最多5轮细化，更复杂任务可能需要更多轮次。

（完）
