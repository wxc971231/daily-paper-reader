---
title: "How to Guide LLM Generation: Dual-Surrogate Guided Search for Automated Heuristic Design"
title_zh: 如何引导大语言模型生成：双代理引导搜索实现自动启发式设计
authors: "Yuhan Wang, Chaoda Peng, Xingyu Wu, Sheng-Hao Wu, Zhi-Hui Zhan"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13911v1"
tags: ["query:llm-opt-code"]
score: 8.0
evidence: LLM从任务描述生成可执行的优化启发式代码
tldr: 大语言模型用于自动启发式设计时，搜索效率依赖于预生成动作选择。现有方法使用预定义规则间接建模动作效果。本文提出双代理指导搜索，通过转移代理预测子代表示分布，实例条件效用代理估计子代性能，并采用不确定性感知规则选择动作。在多样化启发式设计基准上超越强基线，消融实验表明其行为超越简单排序或固定偏好，为LLM引导的自动设计提供了有效框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13911v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1817, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13911v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1556, \"height\": 314, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13911v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 662, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13911v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 703, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13911v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 657, \"height\": 422, \"label\": \"Table\"}]"
motivation: 现有LLM自动启发式设计方法对预生成动作选择仅用预定义规则，未能直接建模每个动作的预期效果，导致搜索效率受限。
method: 提出双代理模块，包含转移代理（预测子代表示分布）和实例条件效用代理（估计子代性能），结合不确定性感知获取规则选择动作。
result: 在多种启发式设计任务中，该方法优于强LLM-AHD基线，消融和动作选择分析表明其行为超越简单存档排序或固定操作偏好。
conclusion: 双代理指导搜索有效提升了LLM自动启发式设计的效率，为预生成动作选择提供了可学习的建模方式。
---

## 摘要
大型语言模型（LLM）通过从任务描述和评估器反馈中生成可执行的启发式代码，使得自动启发式设计（AHD）越来越实用。然而，在有限的查询和评估预算下，搜索效率关键取决于预生成决策。在每次LLM查询和黑盒评估之前，系统必须选择哪些存档的启发式算法作为父代重用，以及哪个生成算子应转换它们。现有方法通常使用预定义规则选择此类动作，仅间接建模每个具体算子-父代动作的预期结果。因此，我们提出双代理引导搜索（DSGS），一种用于基于LLM的AHD中算子-父代选择的代理引导动作选择模块。DSGS通过两个互补代理对预生成动作进行评分来引导LLM代码生成过程。具体来说，提出了一个转换代理来预测由算子-父代动作引起的子代表征的潜在分布，同时提出了一个实例条件效用代理来估计采样子代潜在变量的预期性能。此外，我们提出了一个不确定性感知的获取规则，结合预测效用、效用不确定性和转换不确定性来选择下一个LLM生成动作。在多样化的启发式设计套件中，DSGS与强大的LLM-AHD基线相比具有竞争力，消融和动作选择分析表明其行为超越了简单的档案排名或固定算子偏好。

## Abstract
Large language models (LLMs) have made automated heuristic design (AHD) increasingly practical by generating executable heuristic code from task descriptions and evaluator feedback. Yet under a limited query and evaluation budget, search efficiency depends critically on a pre-generation decision. Before each LLM query and black-box evaluation, the system must choose which archived heuristics to reuse as parents and which generation operator should transform them. Existing methods typically choose such actions with predefined rules, leaving the expected outcome of each concrete operator-parent action only indirectly modeled. Therefore, we propose \emph{\fullmethod{}} (\method{}), a surrogate-guided action-selection module for operator-parent selection in LLM-based AHD. \method{} guides the LLM code-generation process by scoring pre-generation actions with two complementary surrogates. Specifically, a transition surrogate is proposed to predict the latent distribution of the child representation induced by an operator-parent action, while an instance-conditioned utility surrogate is proposed to estimate the expected performance of sampled child latents. Moreover, we propose an uncertainty-aware acquisition rule that combines predicted utility, utility uncertainty, and transition uncertainty to select the next LLM generation action. Across a diverse heuristic-design suite, \method{} is competitive with strong LLM-AHD baselines, and ablation and action-selection analyses suggest that its behavior goes beyond simple archive ranking or fixed operator preferences.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在大语言模型（LLM）驱动的自动启发式设计（AHD）中，搜索效率受限于**预生成动作选择**——即在调用 LLM 和黑盒评估之前，系统必须决定从存档中选取哪些已有启发式作为父代，以及使用哪种生成算子（如变异、交叉）来构造子代。
- **现有方法不足**：当前方法（如 FunSearch、EoH、ReEvo、MCTS-AHD）通常基于预设规则（如档案排名、随机选择、固定调度）来选择动作，这些规则**未能直接学习每个具体算子-父代动作的预期效果**，导致对昂贵 LLM 查询和评估的利用不够高效。
- **整体意义**：论文探索如何将过去的评估数据转化为对下一个生成动作的**可学习指导**，从而在有限预算下提升搜索效率。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**双代理引导搜索（Dual-Surrogate Guided Search, DGS）**，通过两个互补的代理模型对**预生成动作（operator-parent action）** 进行评分，并利用不确定性感知的获取规则选择最优动作。
- **关键技术细节**：
  - **共享潜在表示**：使用 ModernBERT 编码启发式代码，并通过轻量级任务自适应映射（skip projection + 残差 MLP）得到潜在向量 `z(a)`；每个训练实例有可学习的嵌入 `q_j`；算子有独立嵌入 `v_o`。
  - **实例条件效用代理（Instance-Conditioned Utility Surrogate）**：预测子代潜在向量在各训练实例上的效用。输入为启发式潜在 `z` 与实例嵌入 `q_j` 的交互特征（连接、逐元素乘积、绝对差、余弦相似度）；使用 H 个头部组成的集成，输出均值 `μ_u` 和不确定性 `σ_u`；损失函数包括 MSE 和**实例内排序损失**，并加入漂移惩罚稳定表示。
  - **转换代理（Transition Surrogate）**：预测从动作 `x=(o, S)` 到子代潜在分布的转换。输入为父代潜势分布（均值、方差）与算子嵌入，输出子代潜势的均值和对角协方差；损失为高斯负对数似然；转换不确定性 `s_τ(x)` 为预测标准差范数。
  - **不确定性感知获取规则**：综合考虑预测效用、效用不确定性、转换不确定性：
    ```
    α(x) = E[μ_u(˜z)] + β_u E[σ_u(˜z)] + β_u λ_τ |P_tr| s_τ(x)
    ```
    其中第一项为探索，后两项为探索（集成不确定性和转换不确定性）。
  - **算法流程**（Algorithm 1）：
    1. 用算子 i1 初始化 n0 个程序，收集初始数据。
    2. 热身阶段：采样 nw 个随机动作，生成并评估子代。
    3. 循环（直到预算 T）：更新代理和表示；从 top-K 档案池 B_t 枚举可行动作；计算每个动作的获取值；选择最大值对应的动作，调用 LLM 生成子代并评估；将结果加入档案。

### 3. 实验设计：数据集/场景、基准、对比方法

- **任务**：五个启发式设计任务，涵盖直接构造型启发式和嵌入求解器的组件：
  - Traveling Salesman Problem (TSP)
  - Knapsack Problem (KP)
  - Online Bin Packing (OBP)
  - Admissible Set Problem (ASP)
  - Capacitated Vehicle Routing Problem with Ant-Colony Optimization (CVRP-ACO)
- **搜索预算**：200 次生成+评估，每个方法运行 3 次。
- **LLM**：统一使用 GPT-4o-mini。
- **对比方法**：
  - FunSearch
  - EoH (Evolution of Heuristics)
  - ReEvo (Reflective Evolution)
  - MCTS-AHD (Monte Carlo Tree Search for AHD)
- **评价指标**：训练集上的最优得分，测试集上的最终表现；使用平均排名汇总结果。
- **额外实验**：
  - **控制实验（RQ2）**：在 TSP 上固定相同算子集和 top-K 池，对比不同动作选择规则（轮询、随机、算子bandit、仅算子代理、仅父代代理、联合算子-父代代理）。
  - **消融实验（RQ3）**：依次去除排序损失、集成不确定性、漂移正则、转换不确定性、效用集成、交互特征、实例条件、转换预测等组件。
  - **附加分析**：离线启发式选择测试、操作符使用序列分析、扩大算子集（加入 MCTS-AHD 的树路径合成算子）验证泛化性。

### 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。
- 提及 LLM 为 **GPT-4o-mini**，属于托管 API 调用，本地训练的代理模型（ModernBERT + MLP）为轻量级，计算开销相对较小。文中提及“控制在线训练成本”并采用交替更新策略，但无具体算力数字。

### 5. 实验数量与充分性

- **实验数量充足**：包含 5 个任务的主对比实验（每任务 3 次运行）、1 个 TSP 控制实验（6 种配置）、1 个双任务消融实验（8 种变体）、3 项附加分析。
- **充分性评价**：
  - **正面**：覆盖多种典型启发式设计任务（组合优化、在线问题、蚁群组件）；对比了四种代表性基线；消融实验覆盖了模型所有关键组件；控制实验分离了算子选择与父代选择的影响；附加分析验证了潜在表示的预测能力和算子集泛化性。
  - **不足**：
    - 所有实验仅使用一种 LLM（GPT-4o-mini），未测试不同 LLM 的泛化能力。
    - 任务数量依然有限（5个），且均为特定类型（组合优化），未涉及其他 AHD 领域（如连续优化、黑箱优化）。
    - 运行次数仅 3 次，统计显著性较弱（未报告置信区间或 p 值）。
    - 消融实验中某些变体在测试集上性能下降，但论文未详细分析原因。

### 6. 论文的主要结论与发现

- **RQ1（样本效率）**：DGS 在 TSP、KP、OBP 上取得最佳平均排名，在 ASP 与 ReEvo 并列，在 CVRP-ACO 排名第二。搜索轨迹显示 DGS 能更快地提高最优解质量。
- **RQ2（联合算子-父代选择必要性）**：控制实验表明，联合算子-父代代理显著优于仅算子或仅父代代理，也优于手写规则（bandit + 排名），说明**学习联合动作是关键**。
- **RQ3（内部成分贡献）**：消融实验证实去除任何关键组件（排序损失、集成不确定性、转换不确定性、交互特征等）均导致训练集或测试集性能下降，表明各组件共同发挥作用。
- **附加发现**：基于效用训练的潜在表示在离线选择中优于冻结编码；DGS 自适应调整算子偏好，而非固定策略；DGS 可兼容扩展的算子集（如树路径合成算子）。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次将 LLM 预生成动作选择形式化为**可学习的代理优化问题**，引入转换代理和实例条件效用代理，突破了传统规则或事后评分限制。
- **双代理互补**：转换代理处理动作到子代的随机映射，效用代理评估子代性能，两者结合弥补了“动作无直接候选解”的鸿沟。
- **不确定性量化**：同时利用集成不确定性和转换不确定性，在探索-利用平衡中更合理。
- **实验设计严谨**：
  - 控制实验保持了除动作选择外所有组件相同，准确归因效果。
  - 消融实验覆盖所有关键设计，验证了各成分必要性。
  - 附加分析进一步检验了表示学习、算子偏好、算子集泛化性，增强结论可信度。
- **实用性强**：方法可与现有 LLM-AHD 框架（EoH、MCTS-AHD）所使用的算子集兼容，易于集成。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：仅使用一种 LLM（GPT-4o-mini），未验证在不同能力或不同 API 的 LLM 上的表现；任务类型限于组合优化，未扩展到其他 AHD 场景（如调度、连续优化、自动机器学习）。
- **统计可靠性**：每个实验仅运行 3 次，缺乏统计检验；部分任务（如 CVRP-ACO）DGS 不是最优，未深入分析原因。
- **计算开销**：代理模型需要训练（包括表示更新和头部训练），虽然论文提到“控制在线训练成本”，但未给出实际时间开销；对于超大规模问题，ModernBERT 编码可能成为瓶颈。
- **偏差风险**：使用 EoH 的五个固定算子作为算子集，虽验证了可扩展性，但未探索完全自适应的算子生成。实例嵌入采用随机初始化，可能不如任务特定嵌入（如 Transformer 嵌入）有效。
- **应用限制**：该方法依赖于黑盒评估函数和足够多的历史数据（至少 n0+nw 个样本）才能开始有效引导，在极低预算场景（如<50次评估）可能不适用。
- **理论分析缺失**：未对获取规则的探索-利用权衡进行理论分析（如遗憾界）。

（完）
