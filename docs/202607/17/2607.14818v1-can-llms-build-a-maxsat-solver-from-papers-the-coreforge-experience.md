---
title: Can LLMs Build a MaxSAT Solver from Papers? The CoreForge Experience
title_zh: LLM能否根据论文构建MaxSAT求解器？CoreForge经验
authors: Ruben Martins
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14818v1"
tags: ["query:llm-opt-code"]
score: 9.0
evidence: 使用LLM从论文生成MaxSAT求解器代码；优化问题的代码生成
tldr: 大语言模型能否依据论文而非现有代码构建MaxSAT求解器？CoreForge项目探索了这一问题，采用迭代工作流结合ChatGPT讨论、Codex代码生成和LLM辅助审计，实现了基于不可满足性驱动的MaxSAT算法。测试中虽未发现错误答案，但求解性能显著低于顶级人工求解器。研究表明LLM可辅助求解器开发，但需人类验证与性能优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14818v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 949, \"height\": 300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14818v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1337, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14818v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1298, \"height\": 919, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14818v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1093, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14818v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 755, \"height\": 973, \"label\": \"Table\"}]"
motivation: 探索利用大语言模型从研究论文而非现有代码实现MaxSAT求解器的可行性。
method: 采用迭代工作流，结合论文讨论、Codex代码生成和LLM辅助审计，实现不可满足性驱动的MaxSAT算法。
result: 测试配置中未发现错误答案，但性能显著低于最优人工求解器。
conclusion: LLM可辅助求解器实现，但需外部验证和人类指导，未来需改进性能。
---

## 摘要
我们报告了CoreForge项目，这是一个使用大型语言模型（LLM）根据研究论文而非现有求解器代码库构建无权重MaxSAT求解器的经验。该项目专注于基于不可满足性的MaxSAT算法，并遵循一种迭代工作流程，该流程结合了与ChatGPT的论文讨论、通过Codex提示进行的实现，以及反复的LLM辅助代码审计和修订。尽管代码库实现了多种算法和求解器组件，我们的评估重点在于结合核心引导优化、轻量级预处理、核心最小化、与整数线性优化后端的集成，以及一种新的核心序列前瞻方法的配置。我们的经验表明，LLM可以支持根据论文实现求解器，同时需要外部验证、基准测试和人工指导。在我们的实验中，模糊测试和MaxSAT评估实例未在测试配置中发现错误答案，尽管性能仍低于最佳手工设计的MaxSAT求解器。我们总结了有效的方法、仍然存在的困难，以及未来LLM辅助求解器开发的经验教训。

## Abstract
We report on CoreForge, an experience in using large language models (LLMs) to build an unweighted MaxSAT solver from research papers rather than from an existing solver codebase. The project focuses on unsatisfiability-based MaxSAT algorithms and follows an iterative workflow that combines paper discussions with ChatGPT, implementation through Codex prompts, and repeated LLM-assisted code audits and revisions. Although the codebase implements several algorithms and solver components, our evaluation focuses on configurations that combine core-guided optimization, lightweight preprocessing, core minimization, integration with integer linear optimization backends, and a new core-sequence lookahead approach.   Our experience suggests that LLMs can support solver implementation from papers, while requiring external validation, benchmarking, and human guidance. In our experiments, fuzzing and MaxSAT Evaluation instances did not reveal wrong answers in the tested configurations, although performance remains below the best hand-engineered MaxSAT solvers. We summarize what worked, what remained difficult, and the lessons for future LLM-assisted solver development.

---

## 论文详细总结（自动生成）

基于提供的论文文本，以下是对论文《Can LLMs Build a MaxSAT Solver from Papers? The CoreForge Experience》的详细中文总结。

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）能否仅依据研究论文（而非现有求解器代码库）构建一个非平凡的MaxSAT求解器？具体而言，研究三个子问题：
  1. LLM能否从论文构建出有意义的MaxSAT求解器？
  2. 生成的代码中是否观察到错误答案？
  3. 所得求解器的性能与手工设计的顶级求解器差距多大？
- **研究背景**：已有工作探索了LLM用于求解器工程（如SAT求解器启发式优化、SMT求解器构建等），但大多建立在现有代码基础上或仅优化特定组件。CoreForge尝试更长期的“论文到代码”任务，从多篇MaxSAT论文出发，经过多次迭代构建求解器，并基于标准基准评估。这区别于从零构建或仅优化现有代码。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：采用迭代式“论文→代码”工作流，而非一次性生成。人类负责选择论文、定义实现目标、运行实验和决定采用哪些生成变更。LLM（ChatGPT用于高层规划讨论，Codex用于代码生成和审计）负责产生求解器架构、实现算法、辅助审计。
- **关键技术细节**：
  - **基础算法**：实现了多种基于不可满足性（unsatisfiability-based）的MaxSAT算法，包括PM2、MSU3、OLL。使用增量基数约束编码（incremental totalizer encodings）、轻量级预处理（intrinsic AtMost1推理、不相交核预处理）、核最小化。
  - **优化后端集成**：集成SCIP和CP-SAT作为整数线性优化后端，用于计算初始上界和求解。
  - **核心序列前瞻（core-sequence lookahead）**：受核轨迹重要性启发，在正式搜索前运行多个有界探测（probe），每个探测采用不同的假设排序和核处理策略，提取短核序列前缀，使用轻量统计（下界增益、核大小、涉及的不同软子句数）打分，选择最有前景的策略用于主搜索。图2展示了结构：输入公式 → Probe 1..k → 评分前缀 → 提交选定策略。
- **算法流程**：以OLL为核心，结合预处理、核最小化等；可选地先由后端求解器（SCIP/CP-SAT）提供上界；Lookahead配置则在主搜索前执行8个有界探测（限制SAT调用次数、每次调用的冲突数、收集的核数），选择最佳策略后重新开始主搜索。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/基准**：从MaxSAT Evaluation 2024基准中选取417个实例子集。选择标准：被至少一个求解器在MSE’2024中求解过，且最优值大于零。这保证了实例非平凡且有参考最优值。
- **对比方法**：
  - **自身三种配置**：Baseline（OLL+预处理+核最小化）、ILP（Baseline+初始上界+SCIP/CP-SAT后端）、Lookahead（ILP+核心序列前瞻）。
  - **外部强求解器**：maxcdcl-openwbo300、UWrMaxSat-SCIP-MaxPre、EvalMaxSAT-SBVA、wmaxcdcl等，结果部分来自本地重跑（900s超时，16GB内存限制，Intel Xeon Silver 4110 @2.10 GHz，32核），部分取自MSE’2024官方结果（1200s超时）。作者指出并非完全受控对比，但用于识别趋势。

## 4. 资源与算力

- **论文中明确提及的资源**：
  - 开发周期约4周，基本耗尽了$20计划的每周Codex使用额度。
  - 主要使用GPT-5.4，项目后期GPT-5.5可用。
  - 涉及70+个大提示，约120个Git提交，分析了约30篇MaxSAT论文及MSE会议论文集。
  - 本地实验机器：Intel Xeon Silver 4110 CPU @2.10 GHz，32核，64GB RAM，16GB内存限制，900秒超时。
- **未明确说明的内容**：未提及GPU型号、数量或LLM训练时长（因为本研究不是训练LLM，而是使用现有LLM进行推理和代码生成）。也没有提到具体能耗或计算成本。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验为图3的cactus图（在417实例上比较6条曲线）和表2（列出20个求解器/配置的求解数）。
  - 还包含消融分析：ILP配置相比Baseline新增14个实例但丢失20个；Lookahead相比ILP新增11个但丢失7个。分析了性能差距集中在少数基准族（frb和minimize-5gons）。
  - 此外，开发过程中使用模糊测试和LLM辅助审计发现并修复了问题。
- **充分性与公平性**：
  - 作者声明实验是“诊断性比较”而非完整竞赛评估。本地重跑结果与官方结果基本一致（如maxcdcl-openwbo300在本地900s与官方1200s求解数相同），说明局部对比合理。
  - 但作者也指出不同机器和超时设置导致对比仅为近似。CoreForge的VBS（虚拟最佳）比任何单个配置多解24个实例，表明各配置互补，性能差距主要来自实例级开销控制不足，而非根本限制。
  - 总体而言实验设计较为充分，能回答主要问题，但未进行严格的统计显著性检验。

## 6. 论文的主要结论与发现

- **LLM能否从论文构建求解器？** 可以，但非一次性或全自动。需要迭代引导、外部验证和人类决策。LLM能很好地翻译高层算法思想为代码，但低层级求解器工程能力不足。
- **是否观察到错误答案？** 在测试配置中未发现错误答案，但强调开发过程中模糊测试和LLM审计发现了问题并修复，因此外部验证必要。
- **竞争力如何？** CoreForge单个配置求解308/306/302个实例（Baseline/Lookahead/ILP），VBS求解332个，显著低于最强求解器（maxcdcl-openwbo300求解354个）。但已超过多个已建立求解器（如EvalMaxSAT-SBVA的340个）。性能差距集中在少数基准族，通过改进实例级资源策略有潜力缩小。
- **关键经验**：LLM辅助的论文到代码工作流已能支持求解器开发，但信任和自主性仍是核心挑战。未来应朝更自主的求解器智能体发展。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：提出并实现了“核心序列前瞻”这一新特征，展示了LLM不仅可复现现有算法，还能帮助将研究直觉转化为具体功能并集成到代码库。
- **工作流设计**：将规划（ChatGPT）与代码生成（Codex）分离，既控制成本又发挥各自优势。迭代式开发、LLM辅助审计、模糊测试和基准测试形成闭环。
- **实验设计**：分析了性能差距的根源（实例级资源策略缺失），并给出具体改进方向（如为ILP设置大小阈值、为Lookahead添加墙上时钟预算），提供了可操作的下一步。
- **透明性**：详细报告了开发过程、配置细节、审计机制和局限性，体现了严谨。

## 8. 不足与局限

- **性能差距**：整体性能仍明显落后于顶级手工求解器，尤其在frb和minimize-5gons等基准族上差距大。核心序列前瞻表现为有限，仍缺乏成熟实例级资源策略。
- **实验局限**：对比环境不完全相同（本地机器 vs MSE’2024官方，超时不同），虽然趋势可靠但非严格公平。仅测试了无权重MaxSAT，未涉及加权或部分MaxSAT。
- **代码可靠性**：尽管测试未发现错误，但作者未进行详细手动代码审查，依赖LLM审计和模糊测试，存在遗漏细微错误的可能。
- **自主性不足**：工作流仍依赖大量人工干预（选择论文、设计实验、决定是否采纳LLM生成代码），离完全自主求解器开发有距离。
- **通用性**：仅针对MaxSAT领域，结论能否推广到其他约束求解器或更大规模软件工程任务尚不清楚。

---

（完）
